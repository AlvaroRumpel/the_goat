import type { MinigameProps } from './types'

// ARREMESSO — stub temporário. A UI v2 (repertório, closeout, salto, tabela, lances livres)
// é reconstruída numa tarefa seguinte a partir do engine v2 (engine/minigames/shot.ts).
export function ShotGame(_: MinigameProps) {
  return <div className="mg mg-shot" />
}
