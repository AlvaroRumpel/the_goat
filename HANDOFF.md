# The GOAT — Handoff

**Data:** 2026-07-31 · **Estado:** v1 + draft fenomeno + carreira viva + rebalance/humanização + calibração de médias + liga viva + motor de momentos + redesign C1 + redesign C2 + redesign C3 (cerimônia e aposentadoria retemadas, veredito em camadas e persistido, gameplay do playtest revisado; ver decisões 14/15) + ciclo jogo-vivo (jogo-chave assistido reconstruído: 2-5 momentos de decisão por jogo sorteados de um pool de 25 situações, play-by-play revelado linha a linha e pausado em cada decisão, cronômetro de 8s no lance final, tutorial de 1 uso; ver decisão 16) + play-by-play na cadência do relógio (log denso a cada 3' em vez de uma linha entre decisões; ver decisão 17) — **tudo mergeado no `master`, pushado para `origin/master` e deployado em produção** (deploy `27d8f781`, commit `e64a5a0`, 2026-07-31; anteriores `9f18b3de`/`96863fc`, `eb2d2cb7`/`5e6fb2b`, `9ffc534b`/`994eb03`; o bundle já era o mesmo do deploy `af52802e`/`e8e32f9` — os commits seguintes só mexeram em config de teste). Suite verde e e2e playthrough OK. Nenhum ciclo aberto. **Pendência aberta com o dono (herdada do C1, adiada de novo em 2026-07-31):** URL de produção "mais bonita" — `the-goat.pages.dev` está tomado (daí o sufixo `-3xw`); opções apresentadas: domínio próprio (ex.: thegoat.game, que já é o texto do share card) via Custom Domains, ou renomear/recriar o projeto Pages com nome livre (ex.: `thegoat-nba`) sabendo que a URL antiga morre sem redirect.

## O que é

Jogo de carreira de basquete no navegador (inspirado no The Fenomeno). Rouba atributos de lendas da NBA → vive carreira completa → veredito de "Peladeiro de Quadra" até "THE GOAT". Grátis, sem cadastro, PT/EN, mobile-first.

- **Produção:** https://the-goat-3xw.pages.dev (Cloudflare Pages, projeto `the-goat`, conta alvarorumpel@gmail.com)
- **Design:** claude-design projeto "The GOAT" `5a193998-626d-46ba-84f5-fd9a7d164ca5` — 3 canvases aprovados (mobile core, fluxo completo 13 telas, desktop)
- **Spec v1:** `docs/superpowers/specs/2026-07-28-the-goat-design.md` · **Plano v1:** `docs/superpowers/plans/2026-07-28-the-goat-v1.md`
- **Iterações posteriores** (cada uma com spec+plano em `docs/superpowers/` ou `.superpowers/sdd/`): draft-fenomeno, carreira-viva, rebalance-humanizacao, calibracao-medias, liga-viva, motor-momentos, redesign-c1, redesign-c2, redesign-c3, jogo-vivo — detalhes nas Decisões-chave 7–16.
- **Design do redesign**: pacote `design_handoff_the_goat_redesign/` (fora do repo do jogo) ganhou `DECISOES-C1.md` — decisões de implementação pós-handoff, tomadas no brainstorm do ciclo C1 (dono aprovou), complementam o `README.md` do pacote. Spec e plano do C1: `docs/superpowers/specs/2026-07-29-redesign-c1-design.md` · `docs/superpowers/plans/2026-07-29-redesign-c1.md`. Spec e plano do C2: `docs/superpowers/specs/2026-07-29-redesign-c2-design.md` · `docs/superpowers/plans/2026-07-30-redesign-c2.md`. Spec e plano do C3: `docs/superpowers/specs/2026-07-30-redesign-c3-design.md` · `docs/superpowers/plans/2026-07-30-redesign-c3.md`. Spec e plano do jogo-vivo: `docs/superpowers/specs/2026-07-30-jogo-vivo-design.md` · `docs/superpowers/plans/2026-07-30-jogo-vivo.md`; ledger completo do ciclo (achados, rulings do dono, números medidos) em `.superpowers/sdd/2026-07-30-jogo-vivo/progress.md`.

## Arquitetura

```
src/
├── engine/    # TS puro, zero React, testado. RNG injetado (seed) — Math.random proibido.
│   ├── rng.ts       createRng (mulberry32)
│   ├── draft.ts     drawPlayer/weakestSlot/malusAmount/resolveBuild (1 jogador sorteado/rodada, malus = clamp(round((v−71)/6),1,5), piso 40)
│   ├── offers.ts    draftPickNumber, makeOffers (contender/rebuild/bigmarket)
│   ├── events.ts    8 eventos, 2 interativos, máx 2/temporada (lesão 18%/9% c/ foco saúde, rivalidade, viral, fase fria, eventDecision)
│   ├── season.ts    simRegularSeason + computeWinPct/computeTitleProb/finishSeason (league.ts orquestra); ageMultiplier(age, physical), performanceRatio (fórmulas = contrato)
│   ├── league.ts    liga viva: 29 NPCs simulados/temporada — tabela, corridas de prêmio, bracket, trades, draft/aposentadoria NPC
│   ├── moments.ts   motor de momentos: selectKeyGames (3-4/temporada), startWatchedGame/applyMoment/autoResolveGame (probabilidade-alvo), icônicos, `gameRngCalls(n) = 4n+4` (era `6n+4` até a decisão 17, e a constante fixa `GAME_RNG_CALLS = 21` pré-ciclo jogo-vivo); `SITUATIONS` = pool de situações por slot (5 slots × 5 situações = 25), peso `momentWeight(n) = 3/n` por momento (ver decisão 16)
│   ├── schedule.ts  (C2) calendário como verdade: buildCalendar (posiciona key games + deadline no slot 55), simStretch (walk jogo a jogo do trecho entre key games, REGISTRO LITERAL), projectedSeed; CALENDAR_RNG_CALLS = 4, STRETCH_CALLS_PER_GAME = 4
│   ├── playoffs.ts  bracket NPC (npcRound/advancePlayer/resolveRest), bo7WinProb/pGameForSeries (finais), SERIES_SHIFT/PLAYER_OUT_MARGIN
│   └── verdict.ts   score + tiers + GATE icônico do GOAT + iconicPoints (cap 100)
├── data/      players.ts (~200 jogadores, 8 eras), teams.ts (30), i18n/{pt,en}.json (paridade testada)
├── state.ts   gameReducer (15 fases, incl. seasonAdvance/keyGame/playoffGame/gameResult — ver decisão 13), localStorage 'thegoat:v6' (loadState descarta saves v1-v5 — ver decisão 16), campo `timePressure: boolean` (liga/desliga o cronômetro de 8s do lance final, toggle na Home), RNG replay via rngCalls
├── styles/    tokens.css + base.css — tema 2B "Jornal de Tinta Vermelha" (Archivo Black + IBM Plex Mono, radius 0, sombra única); tokens/primitivas novas substituem o "legado dourado", COMPAT removido na task 14 (C1)
└── ui/        screens/{Home,AttrDraft,NbaDraft,Season,SeasonAdvance,SeasonResult,Game,GameResult,Hub,Verdict} + components (CareerBar.tsx + LeaguePanels: StandingsTable/RacesPanel/StandingsTop4/RaceBars/TrajectoryBars — `CeremonyPanel` removido no C3 — + ClutchTimer.tsx, cronômetro de 8s do lance clutch) + hooks/usePlayReveal.ts (relógio virtual do play-by-play, decisões 16/17b) + share.ts (canvas card)
```

## Decisões-chave (não reabrir sem motivo)

1. **GOAT gate** (decisão do dono): tier `goat` = score ≥ 1950 **E** (5+ anéis && 4+ MVPs [Jordan] OU 38k+ pontos && 4+ anéis [LeBron]). Score alto sem troféus → `legend`. Calibrado: 1.33% de 300 carreiras aleatórias (teste `verdict.test.ts` trava < 2%).
2. **Trade "meio de temporada"** = deadline trade: regular season simula → oferta → decisão → playoffs com time final.
3. **RNG replay**: reducer persiste `rngCalls`; load recria rng do seed e avança N calls. Engine determinístico por seed — NUNCA consumir rng fora do fluxo contado.
4. **Aposentadoria**: fase própria quando idade ≥31 OU produção em queda (ver item 8); botão Aposentar na Free Agency quando 31+ ou declínio pesado (ratio < 0.55).
5. **Veredito computado na UI** (`computeVerdict(state.career)`), não armazenado.
6. **Sem fotos/logos reais** — nomes ok, imagens são monogramas/ilustração própria.
7. **Draft fenomeno** (2026-07-28): 1 jogador sorteado/rodada, roubo de atributo livre, reroll 1x, malus = fraqueza do jogador roubado (excluindo slot roubado) com `clamp(round((v−71)/6),1,5)`, piso 40. Save key `thegoat:v2` (v1 descartado). Ver spec `docs/superpowers/specs/2026-07-28-the-goat-design.md` e plano `docs/superpowers/plans/2026-07-28-draft-fenomeno.md`. Ids de jogadores em `players.ts` são contrato do save v2 — renomear/remover id exige bump de save key.
8. **Carreira viva** (2026-07-28): curva 0.78→1.0 (26–29)→declínio por físico (rate clamp 0.012–0.035), piso 0.60; aposentadoria por queda (ratio <0.75 oferece, <0.55 narrativa, mín. 5 temporadas; idade ≥31 mantida); eventos interativos via fase `eventDecision` (escolha a/b antes do sim); `injuryProne` 1 temporada após volta antecipada. Spec `2026-07-28-carreira-viva-design.md`.
9. **Rebalance** (2026-07-28): titleProb recalibrado (constantes finais: `clamp((winPct − 0.5) × 0.22 + (effClutch − 75) × 0.0015, 0.01, 0.16)` em `simPostseason`, `src/engine/season.ts`; forma intocada, só constantes), alvos: 83 ovr ≈ mediana 1 anel (p90 3)/legend 2.5% (<10%), 95 ovr legend 68.5% (>20%). `verdict.ts` não precisou mudar (legendRate de 83 já ficou bem abaixo do teto só com titleProb). GOAT gate (1950 + gate icônico) intocado. Trava em `tests/engine/calibration.test.ts`. Spec `2026-07-28-rebalance-humanizacao-design.md`.
9b. **Calibração rodada 2 — médias por jogo** (2026-07-28): piso 79 ovr rendia 21+ ppg (superstar num build mediano) porque as 3 fórmulas de `simRegularSeason` usavam referência `− 40`. Constantes finais: `ppg = clamp((scoringRt − 61) × 0.78 + ruído, 4, 38)` (era `− 40 × 0.55`), `apg = clamp((0.60×passing + 0.40×handles − 47) × 0.18, 1, 12)` (era `− 40`); `rpg` manteve `− 40 × 0.22` (já batia o alvo, não precisou mudar). Cascata de rodada 1 (rings/legend/GOAT) caiu de score com as médias menores, então os thresholds de awards em `simPostseason` também foram escalados: `allstar` ppg≥19 (era 20; passou por 18 na 1ª iteração), `scoring` ppg≥26 (era 29), `mvp` ppg≥23 (era 26) — `rpg≥11`/`apg≥8` do allstar e o gate de winPct/rng do mvp ficaram iguais. `verdict.ts` não precisou mudar. Harness (`tests/engine/calibration.test.ts`) ganhou `peak.{ppg,rpg,apg}` (média das temporadas 26–29) e `allstarRate`; build do harness é SF flat (+1 ppg, sem bônus reb/ast) então os alvos já assumem esse arquétipo. Resultado (N=200, flat build, foco scoring, política fixa): 79 ovr → peakPpg 15.6 / rpg 8.6 / apg 5.8, allstarRate 7.5% (raro); 83 → 18.4; 90 → 23.3, allstarRate 100% (comum); 99 → 29.6. Travas da rodada 1 seguem verdes (83 legend 0.5% <15%, 95 legend 43.5% >20%, 99 goat 0.5% <2%). Suite 85/85 + typecheck verdes. **Decisão do dono (revisão final):** aceito 83 ovr com tier mediano "starter" (realista para overall de titular sólido; alvo de cascata da rodada 2 fica superado por essa decisão), e threshold de allstar subiu de 18 para 19 para remover a dissonância de 83 ovr virar All-Star praticamente todo ano (allstarRate caiu de 99% para 83.5%; 79 seguiu raro a 0.5%, 90+ seguiu comum a 100%).
10. **Liga viva** (2026-07-28): 29 times NPC simulados junto com o do jogador — tabela (`teamStrength` = `clamp(round(68 + (weighted − 74) × 1.7), 45, 90)`, dataset real com weighted médio ~74, não ~83; recalibrado das âncoras do brief), corridas de prêmio (MVP/DPOY/ROY/MIP), bracket de playoffs, trade deadline, draft/aposentadoria de NPCs entre temporadas. Constantes calibradas: NPC ppg `clamp((eff − 58) × 0.70 + bônus + jitter ±4, 2, 36)`; DPOY favorece tag `defender` (×1.06 vs ×0.86); rookie NPC `ovr = 55 + round(42 × r²)` (skew pra 55–70, raros 90+); `ROY_NPC_BOOST = 1.8` (soma ppg+rpg+apg × 1.8 pra competir com ppg puro do jogador). Save key `thegoat:v3` (v1/v2 removidos no load) — `data/teams.ts` (30 times, ids) e o dataset de `league.ts` são contrato do save; renomear/remover team id exige bump de save key. RNG em `runSeasonSim`/`concludeSeason` (`state.ts`) segue ordem fixa comentada no código (linha ~120): `simRegularSeason → winPct → standings → lines → [pausa tradeDecision] → concludeSeason: [winPct do time final, só se trocou] → bracket → awards → finishSeason` — replay do save depende dessa ordem, não reordenar. **Superada pelo item 11**: essa ordem virou `... → concludeSeason: [winPct do time final] → simAwards → bracket/playoffs jogo a jogo → finishSeason` (awards antes dos playoffs, não depois). Decisões do dono (revisão final): MVP raro para 90 ovr mantido como está (não afrouxar gate); mediana de anéis 0 em 75–79 ovr mantida (realista pra overall sub-titular) — **parcialmente revogada pelo item 11** (drift de anéis do 79 aceito). Spec `docs/superpowers/specs/2026-07-28-liga-viva-design.md` · plano `docs/superpowers/plans/2026-07-28-liga-viva.md`.
11. **Motor de momentos** (2026-07-29): jogos-chave assistidos (3-4/temporada: rivalidade, corrida de seed, especial — natal/revanche/showcase — +1 se houve evento de rivalidade) e playoffs jogo a jogo, com decisões de risco (safe/bold/reckless) em 3 momentos por jogo (`q2tactic`, `q4pressure`, `clutch`) e momentos icônicos no score. Contrato de RNG por jogo: `GAME_RNG_CALLS = 10` (1 `baseMargin` + 3 `makeMoments` + 3×2 `resolveMoment`); pular = jogar a política padrão/safe (`autoResolveGame`), mesmo contrato de calls — replay não distingue skip de assistido. `selectKeyGames` consome 3 calls fixas (1 sorteio do jogo especial + 2 de folga fixa, contrato). Save key `thegoat:v4` (v1-v3 removidos no load); `Phase` ganhou `keyGame`/`playoffGame` (13 fases). **Awards agora resolvidos ANTES dos playoffs** em `concludeSeason` (`simAwards` não depende de resultado de playoff; só `finishSeason` liga `fmvp`/`ring` ao desfecho). **Rounds 0-2**: 1 jogo pivotal por série decide a série via shift CENTRADO na expectativa — `shifted = clamp(seriesProb + 2×SERIES_SHIFT×((venceu?1:0) − seriesProb), 0.05, 0.95)`, `SERIES_SHIFT = 0.20` — de forma que `E[shifted] = seriesProb` quando `P(pivotal) = seriesProb` (política auto); jogar bold desloca `P(pivotal)` para cima e SHIFT é o multiplicador direto desse edge na prob. de série (não é decoração narrativa). **Finais**: bo7 de verdade, tela de série (`series.next`/`series.simAll`) entre jogos; cada jogo usa margem por **probabilidade-alvo** (`targetWinP = pGame`, de `pGameForSeries(seriesProb)` — inverso numérico de `bo7WinProb` por bisseção) resolvida analiticamente sem viés de força de elenco duplicado: `margin = c + U + S` com `U~U(−N,+N)` (`MARGIN_NOISE = 16`), `won ⟺ c+U+S ≥ 0.5`, `c = 2N·p − N + 0.5 − E[S]`, `E[S] = expectedAutoDelta(build,age)` (política padrão, determinístico). **Icônicos** (`ICONIC_VALUES`, cap `iconicPoints = min(100, Σ)`): todo icônico de arremate exige jogada NÃO-safe bem-sucedida (`dagger`) — exceção ratificada pelo dono: `sweep` (série 4-0) vale para qualquer política, é resultado de série, não de jogada. **Jogos-chave reconectados** ao winPct/ppg da temporada: `keyGameEffects` centrado na própria expectativa (não no saldo bruto) — `winPctDelta = clamp(Σ((venceu?1:0) − r.winP) × 0.02, −0.04, 0.04)`, `ppgDelta = clamp(swing × 0.03, −0.5, 0.5) − lesões`, `swing = Σdeltas − jogos×expectedAutoDelta`; `winP` guardado no `PendingGame`/`WatchedGameResult` (mesma equação do `targetWinP` invertida). **Decisões do dono (revisão final)**: drift de anéis do 79 ovr aceito sem recalibrar (`ringsAvg` 0.62→0.74, revoga parcialmente a nota do item 10 sobre mediana 0 em 75-79); `goatRate(99)` = 0.0167 medido com N=600 (trava `< 0.02` mantida — N=200 é ruído demais pra essa cauda). `computeWinPct`/`computeTitleProb`/fórmulas de stats/pesos e thresholds do `verdict.ts`/gate do GOAT intocados — só constantes novas do motor de momentos. Spec `docs/superpowers/specs/2026-07-29-motor-momentos-design.md` · plano `docs/superpowers/plans/2026-07-29-motor-momentos.md` · números/constantes finais na seção "Pós-B" de `docs/superpowers/plans/2026-07-28-calibracao-baseline-liga.md`.
12. **Redesign C1** (2026-07-29): redesign completo dividido em 3 ciclos pelo dono — este ciclo entrega tema visual + navegação + telas estáticas, **zero mudança de engine, zero bump de save**. Decisões (`design_handoff_the_goat_redesign/DECISOES-C1.md`): (a) balanço mobile usa o condensado do desktop (standings top-4/conf, corrida de prêmios em 4 barras, trajetória em 9 barras) — tabela completa de 30 times só no Hub; (b) cerimônia não ganha tela própria neste ciclo, dados (campeão + honras + desfecho) viram seção "DEMAIS HONRAS" dentro do balanço; (c) boot sempre cai na Home — save válido mostra "RETOMAR →", "Nova carreira" apaga o save com aviso; resume direto para a fase salva morreu (mecanismo: `resumePhase: Phase | null` só em memória até o primeiro dispatch, ação `RESUME` restaura); (d) barra de carreira (`CareerBar`) aparece só de `preseason` em diante — pré-carreira (home/attrDraft/draftDone/nbaDraft) mantém cabeçalho próprio; (e) `hubOpen: boolean` persiste no save como campo opcional do shape `thegoat:v4` (default `false`, **sem bump**), ações `OPEN_HUB`/`CLOSE_HUB`, Hub não é fase (não entra no grafo de `Phase`, camada full-screen de leitura pura sobre a tela atual); (f) foco de pré-temporada virou selecionar + confirmar no CTA (antes o tap já disparava). Débitos fechados neste ciclo: `home.lang` → toggle usa `lang.pt`/`lang.en`; `archetype` não depende mais de `.split(' (')` (nome/posição em chaves i18n separadas); `nbadraft.picked` removido (frasing morta). Bloco `COMPAT` (tokens/CSS legados + aliases pra inline styles) criado na task 1 para permitir migração tela-a-tela sem quebrar telas ainda não migradas, removido por completo na task 14 (zero hits de `COMPAT`/`.card`/`.kicker`/`goldtext` em `src` no final). Spec `docs/superpowers/specs/2026-07-29-redesign-c1-design.md` · plano `docs/superpowers/plans/2026-07-29-redesign-c1.md` (16 tasks).

13. **Redesign C2** (2026-07-30): calendário como verdade — a temporada regular deixou de ser um resultado agregado só e virou um walk jogo a jogo via `src/engine/schedule.ts`: `buildCalendar` posiciona os jogos-chave (rivalidade/corrida de seed/especial) e o deadline de trade num único calendário de 82 jogos; `simStretch` resolve cada jogo do trecho entre âncoras como um Bernoulli independente (`won = rng.chance(p)`) e grava um **REGISTRO LITERAL** (`TickerGame[]` acumulado em `SeasonCalendar.ticker`) — sem efeito residual sobre `winPct`; o antigo `winPctDelta` (agregado pós-hoc dos jogos-chave, item 11) morreu, o walk já É o resultado. `winPct` realizado = `wins/82`, contado do registro, não mais recalculado por fórmula no fim. **Deadline de trade** fica fixo no **slot 55** do calendário (`DEADLINE_GAME`), com **dois segmentos de `computeWinPct`** — um para o trecho pré-deadline (roster original) e outro pós-deadline (roster final, se trocou) — isso **revisa a decisão-chave #2** ("regular season simula agregada → oferta → decisão → playoffs com time final"): o motivo do registro é que agora o calendário é *visível* jogo a jogo, então a troca de roster no meio da temporada precisa se refletir no próprio walk a partir do slot 55, não só no agregado final. **`gameResult`** (fase nova, tela 6a) aparece depois de **todo** jogo-chave e jogo de playoff — exceção: séries resolvidas via `SKIP_SERIES` vão direto ao desfecho da série, sem tela de jogo individual. Fase nova `seasonAdvance` (tela 6b) mostra o avanço da temporada entre jogos-chave (posição projetada via `projectedSeed`, linha de stats do walk) — nota: usa uma linha de stats em vez de uma corrida de prêmios parcial (trocada por uma corrida de MVP projetada no C3, ver item 14). `Game.tsx` foi reescrito (teclas 1/2/3 para as decisões de risco) e ganhou par com `GameResult.tsx`. Save key `thegoat:v5` (v1-v4 descartados no load). Contratos de RNG novos/revisados: `GAME_RNG_CALLS = 21` (1 `baseMargin` + 3 `makeMoments` + 4×2 ambientação + 3×(2 `resolveMoment` + 1 variante do lance) — subiu de 10 para 21 por causa da ambientação e da variante do lance por momento); `CALENDAR_RNG_CALLS = 4` (1 jitter por posição de âncora, sempre consumidas mesmo com folga); `STRETCH_CALLS_PER_GAME = 4` (won/margem/pontos/adversário). 15 fases no total (`Phase` em `state.ts`): as 13 anteriores + `seasonAdvance` + `gameResult`. **Recalibração pós-registro-literal**: a variância do walk jogo a jogo empurrou `goatRate(99, N=600)` para 0.0217 (trava < 0.02, vermelha); ajuste em `computeTitleProb` (`src/engine/season.ts`) — coeficiente de `winPct` `0.22 → 0.195`, coeficiente de `effClutch` `0.0015 → 0.0013`, floor (0.01) e cap (0.16) intocados, forma da fórmula intocada. Pós-ajuste: `goatRate(99, N=600) = 0.0117` (verde), `legendRate(95) = 0.295` (>0.2), `mvpRate(95) = 0.855` (>0.5), demais travas de calibração inalteradas — **suite 184/184 verde (travas de calibração incluídas)**. GOAT gate (`verdict.ts`) intocado. Spec `docs/superpowers/specs/2026-07-29-redesign-c2-design.md` · plano `docs/superpowers/plans/2026-07-30-redesign-c2.md`.

14. **Redesign C3** (2026-07-30): ciclo final do redesign — cerimônia (3b) virou sub-view local (`useState`) dentro de `seasonResult`, sem fase nova; aposentadoria (3c) retemada com papel `--paper-2` e 10 barras de declínio de overall efetivo; veredito (3d) ganhou `GameState.verdict` computado uma vez nos dois pontos que entram em `phase: 'verdict'` (`RETIRE_DECISION` e aposentadoria por idade em `case 'ADVANCE'`), com backfill em `loadState` pra saves antigos (sem bump — mesmo padrão do `hubOpen` no C1), fundo vermelho full-bleed e revelação em camadas via CSS (`~200ms` de stagger). `SeasonAdvance` trocou a linha de stats por uma corrida de MVP projetada: duas funções determinísticas novas (`projectedNpcLines`/`projectedStandings`, sem rng) alimentam o `partialMvpRace` — achado morto no levantamento deste ciclo, definido e calibrado mas nunca chamado, agora resgatado. Fechados: `pendingChoices` (campo persistido nunca lido de volta — a variável local que ele guardava continua em uso normal em `simRegularSeason`), guardas de fase faltantes em `CONFIRM_BUILD`/`CHOOSE_OFFER` (o resto do reducer já tinha; não virou mapa central), whitelist de `phase`/`resumePhase` em `loadState` + `ErrorBoundary` novo em `main.tsx`, tiebreak determinístico de standings (`sortStandings`, por `teamId`), `GameResult` mostra todos os `iconics` (não só o primeiro). Dois débitos do HANDOFF já estavam obsoletos antes deste ciclo (corrigidos só na documentação): botão de compartilhar (já tinha clipboard/Web Share) e o botão "simular" duplicado do `Game.tsx` (já corrigido no commit `8e721a0`, fix wave final do C2). Fora de escopo: histórico local de runs (depende de leaderboard/Supabase, backlog v2).

15. **Gameplay do playtest** (2026-07-30, dono): três achados investigados por causa raiz antes de corrigir (nenhum mexeu em fórmula travada). (a) Perder o jogo mesmo com os 3 momentos "OK" é matematicamente correto — `margin = baseMargin (força + ruído ±MARGIN_NOISE) + Σ deltas dos momentos`, o resto do time pode pesar mais — só faltava comunicar; `GameResult` ganhou a linha "SEU IMPACTO/RESTO DO TIME". (b) Oferta de aposentadoria por queda aparecendo aos 24 anos era bug real: `performanceRatio` compara contra o pico histórico de todas as temporadas sem gate de idade — uma temporada de estreia sortuda vira "pico" e engana mesmo com o jogador ainda subindo a curva (`ageMultiplier` só bate 1.0 aos 26). Fix: `RETIRE_MIN_AGE = 28` (nova constante, `engine/season.ts`), aplicada em `case 'ADVANCE'` (`state.ts`) e no botão "Pendurar as chuteiras" da `FreeAgency` (`Season.tsx`) — forma de `performanceRatio` intocada. (c) O ícone "Cesta que fechou a série" nas rodadas 0-2 dos playoffs prometia uma certeza que o mecanismo não entrega — só um jogo pivotal decide, e vencê-lo apenas desloca `seriesProb` (`SERIES_SHIFT`, decisão 11, travada), o sorteio real da série roda depois no `CONTINUE`. Fix: copy renomeada pra "Cesta decisiva" + nota nova em `GameResult` avisando que o resultado só muda as chances, não decide — `SERIES_SHIFT`/mecanismo de shift intocados (decisão do dono: não recalibrar agora).

16. **Jogo vivo** (2026-07-30): reconstrução do jogo-chave assistido — `momentCount(context, rng)` sorteia `baseCount(kind) ± 1` clampado em `[2,5]` (`baseCount`: `finals` = 5, `playoff` = 4, temporada regular = 3), e o catálogo de situações (`SITUATIONS`) virou um pool de 5 slots (`openTone`/`q2tactic`/`q3swing`/`q4pressure`/`clutch`) × 5 situações cada = 25, indexadas por `moment.<slot>.s<i>` (i18n PT/EN). `momentWeight(n) = 3/n` (novo neste ciclo — o motor-momentos da decisão 11 tinha `delta: success ? cfg.hit : cfg.miss` sem peso nenhum; a ponderação nasce no commit `5a7b094`, ver review final `.superpowers/sdd/2026-07-30-jogo-vivo/final-review.md`) é a trava que preserva `E[Σ deltas]` invariante em `n` — sem ela, mais momentos por jogo inflaria a margem e destravaria as travas de calibração de novo. `expectedAutoDelta(build, age, moments)` passou a receber os momentos JÁ sorteados (não mais um `n` genérico) e é calculada dentro de `startWatchedGame` — a ordem importa: `makeMoments` roda antes da margem porque `expectedDelta` depende de quais momentos caíram, o que é o que mantém `P(vitória) = targetWinP` nos playoffs. Contrato de RNG por jogo virou função de `n`: `gameRngCalls(n) = 6n+4` (era a constante fixa `GAME_RNG_CALLS = 21`, presumindo n=3). Novo na UI: `usePlayReveal` revela o play-by-play linha a linha (25ms/char + 600ms de pausa entre linhas, instantâneo com `prefers-reduced-motion`) e PARA exatamente na primeira linha cujo `at` é ≥ o `at` do momento pendente — como o primeiro momento é sempre em `FIRST_AT=10` e as duas únicas linhas de ambientação antes disso ficam fixas nos minutos 3 e 6 (`FIRST_AT-4`), o primeiro segmento revelado é **sempre exatamente 2 linhas**, para qualquer `n` (2 a 5) — invariante usado no e2e (`tests/e2e-playthrough.mjs`) em vez de contar a decidir por um número mágico. `ClutchTimer` (8s, cronômetro só no lance `clutch`) é opcional via `state.timePressure` (toggle na Home, persiste no save, default `true`; expira e joga a opção `safe` default). Tutorial de 1 uso (tarja no primeiro jogo da carreira) via `firstGame = career.seasons.length === 0 && keyGameResults.length === 0`. Save key `thegoat:v6` (v1-v5 descartados no load) — **carreiras em andamento não sobrevivem à atualização, aceito pelo dono**.
    **Sete bugs reais fechados no ciclo** (nenhum mexeu em fórmula travada fora do item de calibração abaixo): (1) `state.ts:573` tinha `pending.momentIndex < 3` literal — um jogo `n=4/5` chegaria ao fim com 3+ outcomes e o dagger novo leria `q4pressure` como se fosse o `clutch`, carimbando `finalsBuzzer`/`seriesWinner` errado; trocado por `pending.moments.length`. (2) `Math.round` de `margin`/`jitter` persistia `-0` no save (1.9% dos jogos, 76/4000, round-trip de JSON) — sem impacto de gameplay (`won = margin > 0` trata `-0 == 0`), mas quebrava fidelidade do save; normalizado. (3) regressão de `goatRate(99)` — ver calibração abaixo. (4) a trava de regressão do `goatRate` estava silenciosamente inoperante — ver achado sério abaixo. (5) o relógio do cabeçalho do jogo podia mostrar `4Q 00:21` junto de linhas de 1Q (seguia o momento pendente, não a última linha revelada) — corrigido pra seguir `reveal.shown`. (6) `NEW_GAME` resetava `timePressure` para `true` via `initialState` mesmo com o toggle desligado na Home — achado só pelo teste manual em browser, corrigido preservando `state.timePressure` no case `NEW_GAME` (mesmo padrão já usado pra `lang`). (7) a cópia do tutorial (rodada 1 de fix) chegou a afirmar que a opção `reckless` era "o único caminho pro momento icônico" — falso: `dagger()` exige só que o último lance seja não-safe bem-sucedido, `bold` vale igual, e pior, `bold` é a aposta matematicamente melhor em atributo alto, então a cópia empurrava o jogador pra opção pior; reescrita (rodada 2) enquadrando o que o `safe` não consegue fazer, verificada contra os EVs reais (`safe` 0.52+0.048x, `bold` −0.92+0.121x, `reckless` −3.44+0.128x, nunca compensa em média com teto de atributo 99).
    **Calibração — a história do peso `3/n` nos playoffs**: o peso preserva a MÉDIA de `Σ` deltas dos momentos em `n`, mas não a VARIÂNCIA (`Var(Σ) = (9/n)·Var₁` — cai de `3·Var₁` em playoffs `n=3` pra `2.25·Var₁`/`1.8·Var₁` em `n=4/5`). Menos variância = menos saturação na janela de ruído assimétrica da margem de playoff (`c + U + S ≥ 0.5`, só a cauda ruim bate no piso) = resultado segue mais a força do time e menos o ruído — times fortes ganham mais, fracos ganham menos ("afiamento", não "ficou mais fácil": stats praticamente congeladas em todas as faixas, só a taxa de título mudou). Medido: `goatRate(99, N=600)` `0.0117 → 0.0217` (regressão real do ciclo, determinística, não pré-existente nem flake). **Decisão do dono**: MANTER o afiamento (consequência de design aceita) e recalibrar só o teto via nudge de `computeTitleProb` — mesmo movimento do C2 (rejeitadas: corrigir a variância, fixar playoffs em 3 momentos, afrouxar a trava). Constantes finais em `computeTitleProb` (`season.ts`): coeficiente de `winPct` `0.195 → 0.1775`, coeficiente de `effClutch` `0.0013 → 0.00118` (floor 0.01/cap 0.16 e forma intocados). Pós-nudge: `goatRate(99, N=600) = 0.0133` (8/600, verde com folga), `ringsAvg(99) = 1.56` (era 1.76 pré-nudge, 1.63 no baseline pré-ciclo), `ringsMedian` `2 → 1`, `legendRate(99) = 0.9517` (== baseline). Suite de calibração 7/7 verde em 3 runs isoladas.
    **Achado sério — trava de regressão silenciosamente inoperante**: o corpo do teste `'99 overall: elite pontua como elite'` é síncrono, então o vitest não consegue interrompê-lo por timeout — ele roda até o fim e SÓ DEPOIS avalia o tempo decorrido; se as asserções passam OU falham, o comportamento no timeout é o mesmo, reportar "Test timed out", mascarando os dois casos igualmente. Com o timeout inline de 120000ms contra um runtime medido de ~230s nesta máquina, a trava não travava nada — passava por timeout mesmo quando a asserção de `goatRate < 0.02` já tinha estourado. Timeouts inline subidos neste ciclo (o do 99 foi a 600000; outros no arquivo a 250000-350000, ver diff de `calibration.test.ts`) pra que a trava reporte de verdade. **Qualquer ciclo futuro que adicionar/alterar um teste síncrono longo precisa saber disso** — o padrão "timeout genérico" do vitest não é uma rede de segurança contra teste síncrono lento, ele mascara falha real.

17c. **Fix do relógio vivo + organicidade** (2026-07-31, dois achados do dono no playtest, validado ao vivo): (a) **bug real**: na máquina do dono o jogo saltava direto pro momento — Windows com "Efeitos de animação" OFF (`SPI_GETCLIENTAREAANIMATION = False`, verificado) → browser reporta `prefers-reduced-motion: reduce` → o hook saltava `t` pro alvo; o sanity em Playwright mascarou porque **Playwright força `reducedMotion: 'no-preference'` por default**. Decisão de produto: o ritmo do play-by-play é CONTEÚDO, não animação decorativa — reduced-motion NÃO pula mais o relógio (só desliga fade/cursor via CSS); pular é o tap no feed. `usePrefersReducedMotion` deletado. **Digitação voltou**, agora subordinada ao relógio: linha entra na fila quando `t` passa o `at` dela e é escrita a `CHAR_MS = 14` (sem pausa entre linhas), painel de decisão só abre com relógio parado E fila toda escrita. E2e adaptado: fast-forward por clique no feed (`.game-plays` = skip) em vez de depender do reveal instantâneo. (b) **organicidade**: cadência fixa de 3' parecia metrônomo e o placar só mudava com linha nova — `ambientAts` ganhou jitter determinístico ±1.2' derivado do `base` do passeio (**zero calls novas** — contrato `4n+4` intacto; abertura de quarto detectada por posição nominal `round(at/3)*3`), e o placar do cabeçalho interpola continuamente entre a última linha e a próxima conforme o relógio anda (lerp na UI, `Game.tsx`). Invariante do 1º segmento mudou: **2-3 linhas** pré-decisão (nominais 3 e 6 sempre; a 9 só com jitter negativo) — e2e e `moments.test.ts` atualizados (gap máximo do log ≤ 6.5'). Sem mudança de consumo de RNG → calibração não re-rodada; snapshot da seed 42 inalterado.

17b. **Relógio vivo** (2026-07-31, refinamento do 17 pedido pelo dono no playtest): o relógio virou o mestre do reveal — `usePlayReveal` reescrito como relógio virtual `t` (minuto contínuo 0-48, `GAME_MIN_PER_SEC = 1.8` → trecho entre decisões ~8s, jogo ~30s + decisões; velocidade constante, sem drama de 4Q — decisões do dono via AskUserQuestion), derivado de `performance.now()` com âncora (sem drift), clampado no `at` do momento pendente; cronômetro do cabeçalho roda contínuo (`clockOf(reveal.t)`, contagem regressiva dentro do quarto) e PARA exato no minuto do momento; linhas surgem quando `t` passa pelo `at` delas (animação CSS `play-in`, fade+slide 250ms) — digitação por caractere morreu (`CHAR_MS`/`LINE_PAUSE_MS`/`typing`/cursor `▌` removidos); `revealTarget` virou `visibleCount(ats, t)`; decisão tomada → reancora em `t` e volta a correr, `t` nunca anda pra trás. Reduced-motion salta pro alvo (contrato do e2e preservado: 2 linhas pré-decisão). Mudança 100% UI (hook + `Game.tsx` + `base.css`), zero engine/RNG/save — calibração não re-rodada. Sanity manual em browser real (sem reduced-motion): relógio ticando `1Q 11:16 → 08:23 → 03:47`, painel abrindo com relógio parado em `1Q 02:00` (minuto 10).

17. **Play-by-play na cadência do relógio** (2026-07-31, achado do dono no playtest): o jogo assistido *saltava* de momento decisivo em momento decisivo — o log tinha `n+1` linhas de ambientação (uma de abertura + uma antes de cada momento), então entre duas decisões passava **uma** linha só e o relógio pulava de 6' para 24.8'. `ambientAts` virou uma cadência fixa: uma linha a cada `PBP_STEP = 3` minutos de relógio (3, 6, 9…45), pulando as que caem a menos de 1.5' de um momento (o momento tem linha própria). ~12-15 linhas de ambientação por jogo, nenhum buraco > 4.5' no log final (trava em `moments.test.ts`). Catálogo novo `play.pbp.v0..v19` (PT/EN) de falas de lance curtas; `play.ambient.<q>` sobrou só para a abertura de quarto (12/24/36 → q=1/2/3) e `play.ambient.open` para o tap-off — as 3 chaves `play.ambient.0.*` morreram (a primeira linha, em 3', é sempre a de abertura). **Custo de rng CAIU e parou de crescer com a densidade do log**: em vez de 2 calls por linha, o log inteiro consome 2 (início e passo do passeio `base + p·stride` pelo pool, `stride` coprimo com 20 = zero fala repetida no jogo) e o jitter do placar é derivado do mesmo par — `gameRngCalls(n) = 6n+4 → 4n+4`. `usePlayReveal` acelerou (`CHAR_MS` 25→16, `LINE_PAUSE_MS` 600→320) porque agora há 5× mais linhas por segmento, e `Game.tsx` auto-scrolla `.game-plays` (o feed passou a encher a caixa). **Invariante do e2e preservado de graça**: o primeiro segmento continua sendo exatamente as linhas de 3' e 6' (a de 9' sempre cai dentro de 1.5' do primeiro momento, em `FIRST_AT=10`). Sem bump de save (shape de `PlayEntry` intocado). Snapshot da seed 42 em `state-calendar.test.ts` regravado (log 5 → 12 linhas). Fórmulas de margem/momento/veredito intocadas; calibração re-rodada só por causa da mudança de consumo de RNG.

## Comandos

```
npm run dev / build
npm test                    # projeto `unit` — tudo menos calibração (~30s)
npm run test:calibration    # projeto `calibration` — travas de distribuição (~15min)
npm run test:all            # os dois projetos
npx tsc -p tsconfig.app.json --noEmit     # typecheck
node tests/e2e-playthrough.mjs             # playthrough Playwright completo (manual, fora do vitest)
CALIBRATE=1 npm run test:calibration -- --disableConsoleIntercept  # relatório de distribuições (bash)
npx wrangler pages deploy dist --project-name=the-goat --branch=master
```

## Débitos deferidos (nenhum bloqueante)

**Revisados com o dono em 2026-07-31 — todos aceitos como estão, nenhum entra em ciclo.** A oferta de fechar o terceiro (hook de pre-commit condicional pra calibração) foi recusada; a disciplina manual do `CLAUDE.md` fica sendo a mitigação.

- Histórico local de runs (spec §4) não implementado — sem UI que o use; juntar com leaderboard na v2.
- **(C2, regravado no ciclo jogo-vivo)** `tests/state-calendar.test.ts` (registro literal do walk) está ancorado na seed 42 — se o RNG upstream mudar de ordem/contagem em qualquer engine, o teste pode quebrar por drift de seed em vez de regressão real; não é uma trava de comportamento, é uma trava de snapshot. O snapshot foi regravado neste ciclo (contrato de RNG por jogo mudou de `GAME_RNG_CALLS=21` fixo pra `gameRngCalls(n)=6n+4`) — o débito em si continua de pé, só a âncora mudou de valor.
- `tests/engine/calibration.test.ts` tem flake de infraestrutura (worker do vitest crasha/expira de forma intermitente, independente do first-run flake já documentado) — se aparecer sozinho numa suite, não é regressão de código. Ver também a decisão 16 sobre a trava de `goatRate` ter ficado silenciosamente inoperante por causa do design síncrono do teste combinado com timeout curto — problema distinto do flake de infraestrutura, já mitigado subindo os timeouts inline neste ciclo.
- ~~**Novo (ciclo jogo-vivo):** `npm test` foi de ~8min para ~17min por causa do timeout inline correto da trava de `goatRate`~~ **FECHADO (2026-07-31)**: suíte separada em dois projetos vitest (`test.projects` em `vite.config.ts`) — `unit` (exclui `**/calibration.test.ts`, 31 arquivos/213 testes, ~27s) e `calibration` (só `calibration.test.ts`). `npm test` roda só o `unit`; `npm run test:calibration` e `npm run test:all` cobrem o resto. **Consequência: `npm test` não é mais rede contra regressão de calibração** — mexeu em constante de `season.ts`/`verdict.ts` ou em ordem/contagem de consumo de RNG, rode `test:calibration` antes de commitar (regra também no `CLAUDE.md`).
- **Residual aceito (ciclo jogo-vivo):** com `n=5` momentos, as linhas de ambientação perto dos minutos 24.8 e 34.2 ainda caem as duas no quarto 2 (`quarterOf`) e podem sortear o mesmo texto — resíduo herdado da decisão do motor-momentos (item 5 do plano C2/motor-momentos) de dar chave própria só à linha de abertura, não a todas as colisões possíveis; `n=2,3,4` ficam limpos. O dono aceitou esse resíduo ao escolher chaves de abertura próprias em vez de deduplicação de variante.

## Backlog v2 (fora do escopo v1, spec §Escopo)

- **Rival de carreira** (v3): NPC recorrente que segue a carreira do jogador temporada a temporada (rivalidade com história, não só o `event.rivalry` pontual de hoje).
- **Elenco ofertante** visível nas ofertas (spec §4, não entregue no ciclo liga viva)
- **All-Star assistido**: hoje é só um award calculado no fim de temporada; podia virar um jogo-chave extra ou tela própria
- `mvpRate(90)` estrutural ~0.11 contra alvo desejado de ~10-15% — é ranking contra 270 NPCs, não dá pra fazer 90 raro e 99 quase-certo ao mesmo tempo sem mexer na fórmula (fora do escopo de calibração por constante)
- **Leaderboard global** (Supabase — usuário já tem conta; submissão por nickname, sem login) + histórico de runs
- **Mini-jogos de habilidade** em momentos-chave (era desejo original do dono, adiado no MVP)
- EN landing/SEO, custom domain
- Calibração fina contínua com playtest real — infra pronta: harness em `calibration.test.ts` (rodar com `CALIBRATE=1 ... --disableConsoleIntercept`), ajustar só constantes, nunca forma das fórmulas
- ~~Feedback pós-jogo nos playoffs rounds 0-2~~ **FECHADO no C2**: `gameResult` (tela 6a) entrega feedback jogo a jogo em playoffs, incl. rounds 0-2.
- Política reckless no harness de calibração (playHurt/playerOut/bigNight sem cobertura; lock sugerido reckless.goatRate < 0.04)
- ~~**Redesign C3** (ciclo final do redesign): cerimônia como tela própria (3b), aposentadoria como tela própria (3c), veredito em camadas e persistido (3d), dívidas restantes do redesign~~ **FECHADO no C3** (ver decisões 14/15).

## Processo usado

Superpowers em ciclos: brainstorming → spec aprovado → plano TDD → subagent-driven development (implementer+reviewer por task, fix loops, worktree isolado) → review final de branch → fix wave → merge → deploy. Onze ciclos completos até aqui (v1 com 15 tasks; depois draft-fenomeno, carreira-viva, rebalance-humanizacao, calibracao-medias, liga-viva com 14 tasks; motor-momentos com 11 tasks; redesign-c1 com 16 tasks; redesign-c2 com 12 tasks; redesign-c3 com 16 tasks; jogo-vivo com 12 tasks). Design/mockups via claude-design com aprovação do usuário (requisito explícito). Calibração de gameplay sempre via harness + travas de regressão, nunca chute. Redesign C3 e jogo-vivo fecharam o ciclo completo (review final → fix wave → merge → push → deploy) em 2026-07-31.
