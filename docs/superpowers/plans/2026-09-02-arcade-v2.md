# Modo Arcade v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir os três minigames v1 do modo arcade pelos v2 aprovados: JOGADA (prancheta 2D com rotas desenhadas, esquemas de defesa escondidos, ações ao vivo, rebote), ARREMESSO (3D three.js: repertório, closeout, mira + salto, pressão, lances livres) e MURALHA (3D: duelo 1x1 contínuo estilo Basketball Stars com swipes e cartão de tendências), sem tocar o contrato de RNG do engine.

**Architecture:** Engine puro em `src/engine/minigames/` (rng injetado, zero React) devolve `MinigameResult { optionId, quality, turnover? }` ao reducer existente (`DECIDE_MOMENT` com `exec`). Uma camada comum (`common.ts`) traduz liga real (NPCs), atributos do jogador e contexto do jogo em números dos minigames. A UI 3D mora em `src/ui/minigames/three/` (port do protótipo `docs/superpowers/assets/arcade-v2/arena-scene-prototype.html`), com um hook de cena e um rig humano com poses; a JOGADA continua SVG. Os componentes v1 viram fallback sem WebGL.

**Tech Stack:** TypeScript, React 19, Vite 8, vitest (projeto `unit`), three r160+ (npm, ES modules), SVG. Sem novas deps além de `three` + `@types/three`.

**Spec:** `docs/superpowers/specs/2026-09-02-arcade-v2-design.md` (mecânica §1–4, tela §5, estilo §8) + spec base `2026-09-02-arcade-e-vida-visual-design.md` (contrato com o engine) + referências visuais em `docs/superpowers/assets/arcade-v2/`.

## Global Constraints

- `src/engine/**` é TS puro: sem React, sem `Math.random`, sem `Date`/`performance`. Aleatoriedade só via `Rng` injetado. Os minigames usam `createRng(minigameSeed(seed, rngCalls))` LOCAL (já existe em `src/engine/minigames/index.ts`) — **nunca** o rng do save.
- Contrato de RNG intocado: `applyMoment`/`resolveMoment` continuam consumindo 3 calls por momento; `SKILL_W = 0.5` fica; `autoResolveGame` nunca passa `exec`. Testes de `tests/engine/moments.test.ts` e `tests/state-arcade.test.ts` continuam verdes.
- Toda string visível passa por `t(lang, key)` com chave em `src/data/i18n/pt.json` E `en.json` (paridade testada em `tests/i18n.test.ts`).
- A animação do desfecho OBEDECE `outcome.success` do engine (contrato em `src/ui/minigames/types.ts`).
- Ritmo/tempo de jogo é conteúdo: loops e timers **não** respeitam `prefers-reduced-motion` (decisão 17c do HANDOFF); só decoração CSS.
- Paleta do jogo (tokens em `src/styles/tokens.css`): papel `#EDE6D6`, tinta `#1C1A16`, vermelho `#A8231C`, âmbar `#E8B24A`; madeira sépia `#D8C59E`/`#CBB68C`. Sem logos/fotos reais.
- Duração-alvo por momento: 30–45s; nenhuma fase passa de 15s.
- Antes de cada commit: `npm test` (projeto unit) + `npx tsc -p tsconfig.app.json --noEmit`. Calibração (`npm run test:calibration`) **não** precisa rodar: consumo de RNG do save não muda.
- Mobile-first: quadra/cena usável a 346px de largura; alvos de toque ≥ 44px.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `src/engine/minigames/common.ts` (novo) | adversário real (`opponentFive`/`opponentStar`/`tendencies`), `difficulty`, `attrMods`, `timingHit`, `freeThrowP` |
| `src/engine/minigames/defense.ts` (reescrito) | duelo 1x1 contínuo: IA do atacante por tendências, sombra, roubo, toco, falta, relógio, `resultOf` |
| `src/engine/minigames/shot.ts` (estendido) | repertório de 8 tipos, closeout, timing do salto, pressão/fadiga, tabela (reflexão), lances livres |
| `src/engine/minigames/playbook.ts` (reescrito) | fases leitura/desenhar/rodar/rebote, rotas, 5 esquemas, ações (finta, bloqueio, finta de arremesso, passe de risco), turnovers |
| `src/engine/moments.ts` (modificado) | catálogo `MG` ganha `mgFreeThrow` |
| `src/ui/minigames/three/arena.ts` (novo) | quadra, tabela, arquibancada instanciada, luz (port do protótipo) |
| `src/ui/minigames/three/human.ts` (novo) | rig low-poly + poses + `setPose(rig, pose, k)` (interpolação) |
| `src/ui/minigames/three/useThreeScene.ts` (novo) | renderer/câmera/loop/resize/dispose + `hasWebGL()` |
| `src/ui/minigames/TimingBar.tsx` (novo) | barra de timing reutilizável (rAF, `onTap`) |
| `src/ui/minigames/useSwipe.ts` (novo) | gestos: swipe esquerda/direita/cima e toque; teclado |
| `src/ui/minigames/Shot.tsx` (reescrito) | ARREMESSO 3D + HUD + `FreeThrows` |
| `src/ui/minigames/Defense.tsx` (reescrito) | MURALHA 3D + HUD (cartão, contenção, posse) |
| `src/ui/minigames/Playbook.tsx` (reescrito) | JOGADA SVG: desenho de rotas, modelos, barra de ações, rebote |
| `src/ui/minigames/ShotFallback.tsx`, `DefenseFallback.tsx` (renomeados dos v1) | sem WebGL |
| `src/ui/minigames/Minigame.tsx` (modificado) | cabeçalho de fase/adversário, roteamento com fallback |
| `src/styles/mg-*.css` | estilos por minigame |
| `tests/engine/minigames/{common,defense,shot,playbook}.test.ts` | testes do engine |

---

### Task 1: Dependência `three` + infra da cena (arena, rig, hook)

**Files:**
- Modify: `package.json` (deps)
- Create: `src/ui/minigames/three/arena.ts`, `src/ui/minigames/three/human.ts`, `src/ui/minigames/three/useThreeScene.ts`
- Reference: `docs/superpowers/assets/arcade-v2/arena-scene-prototype.html` (port 1:1 da geometria, materiais, luz e rig; trocar `THREE.` global por imports ES)

**Interfaces:**
- Produces:
  ```ts
  // arena.ts
  export const COURT_HZ = 12.725            // z da cesta do fundo (|z|); a de perto é -COURT_HZ
  export interface Arena { group: THREE.Group; farHoop: THREE.Vector3; nearHoop: THREE.Vector3; dispose(): void }
  export function buildArena(opts: { crowdRows?: number; teamShort?: string }): Arena   // quadra + 2 cestas + 2 arquibancadas + faixa LED + luzes
  export function buildBall(): THREE.Group                                              // bola com gomos
  // human.ts
  export type Pose = 'stand' | 'shoot' | 'closeout' | 'defend' | 'crossover' | 'dribble' | 'jump'
  export interface HumanRig { root: THREE.Group; setPose(pose: Pose, k?: number): void; dispose(): void }
  export function buildHuman(opts: { jersey: string; shorts: string; number: number | null; name: string | null; skin?: string; hair?: string; you?: boolean }): HumanRig
  // useThreeScene.ts
  export function hasWebGL(): boolean
  export function useThreeScene(canvasRef: RefObject<HTMLCanvasElement>, setup: (ctx: SceneCtx) => (() => void) | void, deps: unknown[]): void
  export interface SceneCtx { scene: THREE.Scene; camera: THREE.PerspectiveCamera; renderer: THREE.WebGLRenderer; onFrame(fn: (dt: number, t: number) => void): void }
  ```
- `setPose(pose, k)`: `k` em 0..1 interpola da pose atual pra `pose` (usa `THREE.MathUtils.lerp` nos ângulos guardados por junta); `k` omitido = pose instantânea.

- [ ] **Step 1: Instalar three**

Run: `npm i three@^0.169.0 && npm i -D @types/three@^0.169.0`
Expected: `package.json` com `three` em dependencies e `@types/three` em devDependencies.

- [ ] **Step 2: Escrever `useThreeScene.ts`**

```ts
import { useEffect, type RefObject } from 'react'
import * as THREE from 'three'

export function hasWebGL(): boolean {
  try {
    const c = document.createElement('canvas')
    return !!(c.getContext('webgl2') || c.getContext('webgl'))
  } catch { return false }
}

export interface SceneCtx {
  scene: THREE.Scene
  camera: THREE.PerspectiveCamera
  renderer: THREE.WebGLRenderer
  onFrame(fn: (dt: number, t: number) => void): void
}

// Monta renderer/câmera/loop no <canvas>; `setup` constrói a cena e devolve um cleanup.
// Loop de rAF é gameplay: não respeita prefers-reduced-motion. Pausa quando a aba esconde.
export function useThreeScene(
  canvasRef: RefObject<HTMLCanvasElement | null>,
  setup: (ctx: SceneCtx) => (() => void) | void,
  deps: unknown[],
): void {
  useEffect(() => {
    const canvas = canvasRef.current
    if (!canvas) return
    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, powerPreference: 'high-performance' })
    renderer.setPixelRatio(Math.min(2, window.devicePixelRatio))
    renderer.shadowMap.enabled = true
    renderer.shadowMap.type = THREE.PCFSoftShadowMap
    renderer.outputColorSpace = THREE.SRGBColorSpace
    renderer.toneMapping = THREE.ACESFilmicToneMapping
    renderer.toneMappingExposure = 1.05
    const scene = new THREE.Scene()
    const camera = new THREE.PerspectiveCamera(38, 1, 0.1, 140)
    const frames: Array<(dt: number, t: number) => void> = []
    const ctx: SceneCtx = { scene, camera, renderer, onFrame: fn => { frames.push(fn) } }
    const cleanup = setup(ctx)
    const resize = () => {
      const w = canvas.clientWidth, h = canvas.clientHeight
      if (w === 0 || h === 0) return
      renderer.setSize(w, h, false)
      camera.aspect = w / h
      camera.updateProjectionMatrix()
    }
    resize()
    const ro = new ResizeObserver(resize)
    ro.observe(canvas)
    let raf = 0, last = performance.now(), t = 0, running = true
    const tick = (now: number) => {
      if (!running) return
      const dt = Math.min(0.05, (now - last) / 1000)
      last = now; t += dt
      for (const f of frames) f(dt, t)
      renderer.render(scene, camera)
      raf = requestAnimationFrame(tick)
    }
    raf = requestAnimationFrame(tick)
    const vis = () => { running = !document.hidden; if (running) { last = performance.now(); raf = requestAnimationFrame(tick) } }
    document.addEventListener('visibilitychange', vis)
    return () => {
      running = false
      cancelAnimationFrame(raf)
      ro.disconnect()
      document.removeEventListener('visibilitychange', vis)
      cleanup?.()
      renderer.dispose()
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, deps)
}
```

- [ ] **Step 3: Portar `arena.ts` do protótipo**

Copiar do protótipo as funções `woodTexture`, `line`/`arc`/`halfCourt`, `stands` e as luzes ("luz meio-termo") para `buildArena`, adicionando tudo a um `THREE.Group` em vez de `scene`. Assinatura e diferenças obrigatórias:

```ts
import * as THREE from 'three'
export const PAPER = '#EDE6D6', INK = '#1C1A16', RED = '#A8231C', WARM = '#E8B24A'
export const COURT_HZ = 14.3 - 1.575
export interface Arena { group: THREE.Group; farHoop: THREE.Vector3; nearHoop: THREE.Vector3; dispose(): void }
export function buildArena(opts: { crowdRows?: number; teamShort?: string } = {}): Arena {
  const group = new THREE.Group()
  const add = (o: THREE.Object3D) => group.add(o)
  // ... woodTexture(), floor, apron, lines, halfCourt(+1)/halfCourt(-1), stands(±), band com opts.teamShort ?? 'THE GOAT ARENA'
  // luzes: HemisphereLight('#FFF1D8', '#4A3E30', 0.9), AmbientLight('#6B5E4E', 0.45), SpotLight key (1400, castShadow, mapSize 1024) + fill (900) + DirectionalLight rim (0.5) + PointLight nas arquibancadas (160)
  // As luzes key/fill apontam pra cesta do FUNDO por padrão; quem usa a cesta de perto chama aimLights(group, -1)
  const disposables: Array<{ dispose(): void }> = []  // texturas, geometrias, materiais criados aqui
  return { group, farHoop: new THREE.Vector3(0, 3.05, COURT_HZ), nearHoop: new THREE.Vector3(0, 3.05, -COURT_HZ), dispose: () => disposables.forEach(d => d.dispose()) }
}
export function aimLights(arena: Arena, side: 1 | -1): void   // reposiciona key/fill/rim/point pro lado `side` (mesmas fórmulas do protótipo com D = side)
export function buildBall(): THREE.Group   // esfera '#C77E2C' r=0.121 + 3 gomos (torus 0.005) — castShadow
```
Regras do port: `scene.fog` e `scene.background` ficam por conta de quem monta a cena (`Shot.tsx`/`Defense.tsx`): `new THREE.Color('#2E2A24')` e `new THREE.Fog('#2E2A24', 26, 60)`. Torcida: `crowdRows` default 9, `cols` 32, InstancedMesh como no protótipo.

- [ ] **Step 4: Portar `human.ts` com poses interpoláveis**

O protótipo monta a pose no `arm()`/`leg()` no ato; aqui as juntas ficam guardadas para `setPose` reposicionar:

```ts
import * as THREE from 'three'
import { PAPER, INK, RED } from './arena'
export type Pose = 'stand' | 'shoot' | 'closeout' | 'defend' | 'crossover' | 'dribble' | 'jump'
interface Joints { hips: THREE.Group; armR: [THREE.Group, THREE.Group]; armL: [THREE.Group, THREE.Group]; legR: [THREE.Group, THREE.Group]; legL: [THREE.Group, THREE.Group] }
// [ombro.z, ombro.x, cotovelo.x, ombro.y] por braço; [quadril.x, joelho.x] por perna; [hipsY, hipsRotX, hipsRotY]
const POSES: Record<Pose, { armR: number[]; armL: number[]; legR: number[]; legL: number[]; hips: number[] }> = {
  stand:     { armR: [0.25, 0, -0.3, 0],       armL: [0.25, 0, -0.3, 0],        legR: [0, 0],        legL: [0, 0],         hips: [0, 0, 0] },
  shoot:     { armR: [2.6, -0.5, -1.2, 0.2],   armL: [2.2, -0.9, -1.45, -0.2],  legR: [0.55, -1.25], legL: [0.25, -0.5],   hips: [0.6, 0, 0] },
  jump:      { armR: [3.0, 0.1, -0.15, 0],     armL: [3.0, 0.1, -0.15, 0],      legR: [0.4, -0.9],   legL: [0.4, -0.9],    hips: [0.7, 0, 0] },
  closeout:  { armR: [3.05, 0.1, -0.15, 0],    armL: [1.1, 0.5, -0.4, 0],       legR: [0.65, -0.95], legL: [-0.55, -0.15], hips: [0, 0.1, 0] },
  defend:    { armR: [1.4, 0.4, -0.55, 0],     armL: [1.4, 0.4, -0.55, 0],      legR: [0.4, -0.8],   legL: [0.4, -0.8],    hips: [-0.15, 0.22, 0] },
  crossover: { armR: [0.35, -1.0, -0.3, 0.1],  armL: [0.5, -0.6, -1.25, 0.3],   legR: [0.6, -0.95],  legL: [-0.3, -0.25],  hips: [-0.1, 0.32, -0.3] },
  dribble:   { armR: [0.35, -0.8, -0.4, 0.1],  armL: [0.6, 0.1, -0.5, 0],       legR: [0.3, -0.5],   legL: [-0.1, -0.2],   hips: [-0.05, 0.2, 0] },
}
export interface HumanRig { root: THREE.Group; setPose(pose: Pose, k?: number): void; dispose(): void }
export function buildHuman(opts: { jersey: string; shorts: string; number: number | null; name: string | null; skin?: string; hair?: string; you?: boolean }): HumanRig {
  // construir exatamente como `human()` do protótipo (tronco, deltoides, colarinho, calção com listras, cabeça com cabelo/olhos/nariz/boca/orelhas, faixa se `you`, número frente e nome+número costas via labelTex),
  // mas cada arm()/leg() devolve seus grupos de junta para `joints`; `setPose` aplica POSES[pose] (lerp com a pose atual quando k < 1).
}
```
`setPose(p, k)`: `cur = lerp(cur, POSES[p], k ?? 1)` campo a campo e escreve `rotation`/`position` nas juntas. Guardar `cur` no closure.

- [ ] **Step 5: Typecheck**

Run: `npx tsc -p tsconfig.app.json --noEmit`
Expected: sem erros. (Se `@types/three` reclamar de `CapsuleGeometry`, a versão está antiga — usar `three@^0.169`.)

- [ ] **Step 6: Smoke visual manual (opcional, não bloqueia)**

Criar temporariamente `src/ui/minigames/three/__preview.tsx` que monta `buildArena()` + dois `buildHuman()` numa `useThreeScene`, abrir via `npm run dev` e conferir com a referência `docs/superpowers/assets/arcade-v2/ref-arremesso.jpg`. Apagar antes do commit.

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json src/ui/minigames/three
git commit -m "feat(arcade): infra three.js — arena, rig humano com poses, hook de cena"
```

---

### Task 2: Camada comum (`common.ts`)

**Files:**
- Create: `src/engine/minigames/common.ts`
- Test: `tests/engine/minigames/common.test.ts`

**Interfaces:**
- Consumes: `LeagueState`, `LeaguePlayer`, `Build`, `WatchedGameKind` de `src/engine/types.ts`; `ageMultiplier(age, physical)` de `src/engine/season.ts`; `clamp01` de `src/engine/minigames/index.ts`.
- Produces:
  ```ts
  export interface OppPlayer { id: string; name: string; short: string; pos: Archetype; ovr: number; tags: LeagueTag[]; speed: number; reach: number }
  export function opponentFive(league: LeagueState, teamId: string): OppPlayer[]      // 5 maiores ovr do time; short = último sobrenome em maiúsculas
  export function opponentStar(league: LeagueState, teamId: string): OppPlayer         // = opponentFive()[0]
  export function bestDefender(five: OppPlayer[]): OppPlayer                           // tag 'defender' com maior ovr, senão maior ovr
  export interface Tendencies { shoot: number; drive: number; pass: number; side: 'left' | 'right' }   // soma 1
  export function tendencies(p: OppPlayer): Tendencies
  export function difficulty(kind: WatchedGameKind, starOvr: number): number
  export interface AttrMods {
    speed: number; feint: number; stripResist: number; laneSafety: number; jumpWindow: number
    reactMs: number; stealWindow: number; boxWindow: number; tremor: number; fatigue: number
    tol: Record<'layup' | 'floater' | 'mid' | 'three' | 'dunk', number>
  }
  export function attrMods(build: Build, age: number, quarter = 1): AttrMods
  export function timingHit(t: number, center: number, halfWidth: number): 'perfect' | 'hit' | 'miss'   // |t-center| ≤ 0.25·halfWidth = perfect; ≤ halfWidth = hit
  export function freeThrowP(ovr: number): number   // 0.62 + (ovr − 60) × 0.006, clamp 0.4..0.92
  ```

- [ ] **Step 1: Escrever os testes**

```ts
// tests/engine/minigames/common.test.ts
import { describe, expect, test } from 'vitest'
import { initLeague } from '../../../src/data/league'
import { createRng } from '../../../src/engine/rng'
import { SLOT_ORDER, type Build, type SlotId } from '../../../src/engine/types'
import { attrMods, bestDefender, difficulty, freeThrowP, opponentFive, opponentStar, tendencies, timingHit } from '../../../src/engine/minigames/common'

const build = (ovr: number, over: Partial<Record<SlotId, number>> = {}): Build => ({
  attributes: { ...Object.fromEntries(SLOT_ORDER.map(s => [s, ovr])), ...over } as Record<SlotId, number>,
  picks: [], archetype: 'SF', overall: ovr,
})
const league = initLeague(createRng(1))

describe('adversário real', () => {
  test('opponentFive: 5 do time, ordenados por ovr desc, com short e speed em faixa', () => {
    const five = opponentFive(league, 'bos')
    expect(five).toHaveLength(5)
    expect(five.every(p => league.players.find(l => l.id === p.id)?.teamId === 'bos')).toBe(true)
    for (let i = 1; i < 5; i++) expect(five[i - 1].ovr).toBeGreaterThanOrEqual(five[i].ovr)
    for (const p of five) { expect(p.speed).toBeGreaterThanOrEqual(3.0); expect(p.speed).toBeLessThanOrEqual(4.4); expect(p.short).toBe(p.short.toUpperCase()) }
  })
  test('opponentStar é o primeiro; bestDefender prefere tag defender', () => {
    const five = opponentFive(league, 'bos')
    expect(opponentStar(league, 'bos').id).toBe(five[0].id)
    const d = bestDefender(five)
    if (five.some(p => p.tags.includes('defender'))) expect(d.tags).toContain('defender')
    else expect(d.id).toBe(five[0].id)
  })
  test('tendencies somam 1 e shooter arremessa mais', () => {
    const base = opponentStar(league, 'bos')
    const sh = tendencies({ ...base, tags: ['shooter'] }), pm = tendencies({ ...base, tags: ['playmaker'] })
    expect(sh.shoot + sh.drive + sh.pass).toBeCloseTo(1, 6)
    expect(sh.shoot).toBeGreaterThan(pm.shoot)
    expect(pm.pass).toBeGreaterThan(sh.pass)
  })
})

describe('dificuldade e mods', () => {
  test('difficulty cresce com kind e ovr', () => {
    expect(difficulty('finals', 90)).toBeGreaterThan(difficulty('playoff', 90))
    expect(difficulty('playoff', 90)).toBeGreaterThan(difficulty('rivalry', 90))
    expect(difficulty('rivalry', 95)).toBeGreaterThan(difficulty('rivalry', 70))
    expect(difficulty('rivalry', 60)).toBeCloseTo(0.85, 5)
  })
  test('attrMods: atributo alto melhora cada mod na direção certa', () => {
    const lo = attrMods(build(55), 27), hi = attrMods(build(95), 27)
    expect(hi.speed).toBeGreaterThan(lo.speed)
    expect(hi.feint).toBeGreaterThan(lo.feint)
    expect(hi.stripResist).toBeGreaterThan(lo.stripResist)
    expect(hi.laneSafety).toBeGreaterThan(lo.laneSafety)
    expect(hi.jumpWindow).toBeGreaterThan(lo.jumpWindow)
    expect(hi.reactMs).toBeGreaterThan(lo.reactMs)
    expect(hi.stealWindow).toBeGreaterThan(lo.stealWindow)
    expect(hi.boxWindow).toBeGreaterThan(lo.boxWindow)
    expect(hi.tremor).toBeLessThan(lo.tremor)
    expect(hi.tol.three).toBeGreaterThan(lo.tol.three)
  })
  test('fadiga só no 4Q com físico baixo', () => {
    expect(attrMods(build(80, { physical: 55 }), 27, 4).fatigue).toBeGreaterThan(0)
    expect(attrMods(build(80, { physical: 55 }), 27, 2).fatigue).toBe(0)
    expect(attrMods(build(80, { physical: 80 }), 27, 4).fatigue).toBe(0)
  })
  test('idade entra via ageMultiplier (35 anos < 27)', () => {
    expect(attrMods(build(85), 35).speed).toBeLessThan(attrMods(build(85), 27).speed)
  })
})

describe('helpers', () => {
  test('timingHit', () => {
    expect(timingHit(0.5, 0.5, 0.1)).toBe('perfect')
    expect(timingHit(0.58, 0.5, 0.1)).toBe('hit')
    expect(timingHit(0.7, 0.5, 0.1)).toBe('miss')
  })
  test('freeThrowP clampa', () => {
    expect(freeThrowP(60)).toBeCloseTo(0.62, 5)
    expect(freeThrowP(20)).toBe(0.4)
    expect(freeThrowP(120)).toBe(0.92)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run --project unit tests/engine/minigames/common.test.ts`
Expected: FAIL — módulo `common` não existe.

- [ ] **Step 3: Implementar `common.ts`**

```ts
import { ageMultiplier } from '../season'
import type { Archetype, Build, LeaguePlayer, LeagueState, LeagueTag, WatchedGameKind } from '../types'

const clamp = (v: number, lo: number, hi: number) => Math.min(hi, Math.max(lo, v))

export interface OppPlayer { id: string; name: string; short: string; pos: Archetype; ovr: number; tags: LeagueTag[]; speed: number; reach: number }

function toOpp(p: LeaguePlayer): OppPlayer {
  const speed = clamp(3.2 + (p.ovr - 55) / 45 * 1.2, 3.0, 4.4) * (p.tags.includes('defender') ? 1.08 : 1)
  const reach = p.pos === 'C' ? 2.9 : p.pos === 'PF' ? 2.8 : 2.7
  return { id: p.id, name: p.name, short: (p.name.split(' ').at(-1) ?? p.name).toUpperCase(), pos: p.pos, ovr: p.ovr, tags: p.tags, speed, reach }
}
export function opponentFive(league: LeagueState, teamId: string): OppPlayer[] {
  return league.players.filter(p => p.teamId === teamId).sort((a, b) => b.ovr - a.ovr || a.id.localeCompare(b.id)).slice(0, 5).map(toOpp)
}
export function opponentStar(league: LeagueState, teamId: string): OppPlayer { return opponentFive(league, teamId)[0] }
export function bestDefender(five: OppPlayer[]): OppPlayer {
  const d = five.filter(p => p.tags.includes('defender')).sort((a, b) => b.ovr - a.ovr)[0]
  return d ?? five[0]
}

export interface Tendencies { shoot: number; drive: number; pass: number; side: 'left' | 'right' }
export function tendencies(p: OppPlayer): Tendencies {
  let shoot = 0.4, drive = 0.35, pass = 0.25
  if (p.tags.includes('shooter')) { shoot = 0.6; drive = 0.2; pass = 0.2 }
  else if (p.tags.includes('playmaker')) { shoot = 0.3; drive = 0.25; pass = 0.45 }
  else if (p.tags.includes('rebounder') || p.tags.includes('defender')) { shoot = 0.25; drive = 0.55; pass = 0.2 }
  const side = p.id.charCodeAt(p.id.length - 1) % 2 === 0 ? 'right' : 'left'   // determinístico por jogador
  return { shoot, drive, pass, side }
}

export function difficulty(kind: WatchedGameKind, starOvr: number): number {
  const base = kind === 'finals' ? 1.22 : kind === 'playoff' ? 1.12 : 1.0
  return base * (0.85 + (starOvr - 60) / 100)
}

export interface AttrMods {
  speed: number; feint: number; stripResist: number; laneSafety: number; jumpWindow: number
  reactMs: number; stealWindow: number; boxWindow: number; tremor: number; fatigue: number
  tol: Record<'layup' | 'floater' | 'mid' | 'three' | 'dunk', number>
}
// x = atributo efetivo − 60 (−20..39); cada mod é linear em x, clampado
export function attrMods(build: Build, age: number, quarter = 1): AttrMods {
  const m = ageMultiplier(age, build.attributes.physical)
  const a = (s: keyof Build['attributes']) => build.attributes[s] * m - 60
  const lin = (x: number, at0: number, per: number, lo: number, hi: number) => clamp(at0 + x * per, lo, hi)
  const physical = build.attributes.physical * m
  const fatigue = quarter >= 4 && physical < 65 ? clamp((65 - physical) / 65, 0, 0.3) : 0
  return {
    speed: lin(a('physical'), 1.0, 0.006, 0.8, 1.22),
    feint: lin(a('handles'), 0.8, 0.02, 0.5, 1.6),
    stripResist: lin(a('handles'), 1.0, 0.012, 0.7, 1.45),
    laneSafety: lin(a('passing'), 1.0, 0.014, 0.7, 1.5),
    jumpWindow: lin(a('physical'), 0.1, 0.0025, 0.06, 0.2) * (1 - fatigue * 0.6),
    reactMs: lin(a('defense'), 0, 3, -60, 120),
    stealWindow: lin(a('defense'), 0.12, 0.003, 0.07, 0.24),
    boxWindow: lin(a('rebounding'), 0.15, 0.003, 0.08, 0.26),
    tremor: lin(a('clutch'), 1.0, -0.015, 0.3, 1.3),
    fatigue,
    tol: {
      layup: lin(a('finishing'), 1.0, 0.008, 0.8, 1.3), floater: lin(a('finishing'), 0.95, 0.008, 0.75, 1.25),
      mid: lin(a('handles'), 1.0, 0.008, 0.8, 1.3), three: lin(a('three'), 1.0, 0.008, 0.8, 1.3), dunk: lin(a('finishing'), 1.0, 0.008, 0.8, 1.3),
    },
  }
}

export function timingHit(t: number, center: number, halfWidth: number): 'perfect' | 'hit' | 'miss' {
  const d = Math.abs(t - center)
  return d <= halfWidth * 0.25 ? 'perfect' : d <= halfWidth ? 'hit' : 'miss'
}
export function freeThrowP(ovr: number): number { return clamp(0.62 + (ovr - 60) * 0.006, 0.4, 0.92) }
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run --project unit tests/engine/minigames/common.test.ts`
Expected: PASS (todos). Se `initLeague` não aceitar `createRng(1)` como único argumento, olhar `src/data/league.ts` e ajustar a chamada no teste (não o engine).

- [ ] **Step 5: Commit**

```bash
git add src/engine/minigames/common.ts tests/engine/minigames/common.test.ts
git commit -m "feat(arcade): camada comum — adversario real, dificuldade, mods de atributo, timing"
```

---

### Task 3: `mgFreeThrow` no catálogo + i18n do catálogo

**Files:**
- Modify: `src/engine/moments.ts` (objeto `MG`)
- Modify: `src/data/i18n/pt.json`, `src/data/i18n/en.json`
- Test: `tests/engine/minigames/index.test.ts` (ajustar contagem), `tests/state-arcade.test.ts`

**Interfaces:**
- Produces: id `'mgFreeThrow'` (safe, `attr: 'clutch'`) aceito por `minigameOption` e pelo reducer no arcade.

- [ ] **Step 1: Ajustar o teste de contagem**

Em `tests/engine/minigames/index.test.ts`, trocar `expect(MINIGAME_OPTION_IDS).toHaveLength(8)` por `toHaveLength(9)` e acrescentar:

```ts
  test('mgFreeThrow é safe em clutch', () => {
    expect(minigameOption('mgFreeThrow')).toMatchObject({ risk: 'safe', attr: 'clutch' })
  })
```
Em `tests/state-arcade.test.ts` acrescentar dentro do `describe`:
```ts
  test('mgFreeThrow resolve no arcade', () => {
    let s = playToKeyGame(47, 'arcade')
    s = gameReducer(s, { type: 'DECIDE_MOMENT', optionId: 'mgFreeThrow', exec: { quality: 0.9 } })
    expect(s.pendingGame!.outcomes[0].optionId).toBe('mgFreeThrow')
  })
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run --project unit tests/engine/minigames/index.test.ts tests/state-arcade.test.ts tests/i18n.test.ts`
Expected: FAIL (contagem 8, opção inexistente, chaves i18n faltando).

- [ ] **Step 3: Adicionar ao catálogo e às traduções**

Em `src/engine/moments.ts`, dentro de `const MG = {`, após `mgSteal`:
```ts
  mgFreeThrow: { id: 'mgFreeThrow', attr: 'clutch', risk: 'safe' },
```
Adicionar em `pt.json` (antes da última chave, mantendo JSON válido) e o espelho em `en.json`:
```json
  "option.mgFreeThrow": "Lances livres",
  "play.mgFreeThrow.hit.v0": "Dois lances livres, dois pontos — frio como gelo.",
  "play.mgFreeThrow.hit.v1": "Converte os lances livres com o ginásio inteiro gritando.",
  "play.mgFreeThrow.miss.v0": "Erra o segundo lance livre e o {opp} pega o rebote.",
  "play.mgFreeThrow.miss.v1": "A bola gira no aro e sai — lance livre perdido."
```
EN:
```json
  "option.mgFreeThrow": "Free throws",
  "play.mgFreeThrow.hit.v0": "Two free throws, two points — ice cold.",
  "play.mgFreeThrow.hit.v1": "Sinks the free throws with the whole arena screaming.",
  "play.mgFreeThrow.miss.v0": "Misses the second free throw and {opp} grabs the board.",
  "play.mgFreeThrow.miss.v1": "The ball spins around the rim and out — free throw missed."
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run --project unit tests/engine/minigames/index.test.ts tests/state-arcade.test.ts tests/i18n.test.ts tests/engine/moments.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/engine/moments.ts src/data/i18n/pt.json src/data/i18n/en.json tests/engine/minigames/index.test.ts tests/state-arcade.test.ts
git commit -m "feat(arcade): mgFreeThrow no catalogo dos minigames"
```

---

### Task 4: Engine da MURALHA v2 — duelo contínuo (`defense.ts`)

**Files:**
- Rewrite: `src/engine/minigames/defense.ts` (o v1 de batidas morre; nada mais importa dele além de `Defense.tsx`, que será reescrito na Task 10)
- Rewrite: `tests/engine/minigames/defense.test.ts`

**Interfaces:**
- Consumes: `Tendencies`, `AttrMods` (Task 2); `MinigameResult`, `clamp01` de `./index`; `Rng`.
- Produces:
  ```ts
  export type Move = 'hesi' | 'crossL' | 'crossR' | 'spin' | 'legs' | 'driveL' | 'driveR' | 'pumpFake' | 'shoot' | 'pass'
  export interface DuelInput { tend: Tendencies; difficulty: number; mods: AttrMods; starOvr: number }
  export interface DuelState {
    t: number; clock: number                       // segundos; posse começa em 8
    attX: number; defX: number                     // deslocamento lateral em metros (−2.5..2.5), atacante e você
    attDist: number                                // distância atacante→cesta (m); começa 7.5, infiltração reduz
    move: { kind: Move; at: number; dur: number } | null
    exposed: number                                // >0 = bola exposta por esse tempo restante (s)
    airborne: number                               // >0 = você no ar por esse tempo restante
    contain: number; live: number                  // tempo contido / tempo total de duelo
    stealTries: number; fouled: boolean; bitFake: boolean
    contestDist: number | null                     // distância da sua mão no release (m) ou null
    phase: 'live' | 'shot' | 'drive' | 'steal' | 'block' | 'foul' | 'clock' | 'done'
    log: Move[]
    result?: MinigameResult
    freeThrows?: [boolean, boolean]               // lances livres dele, quando falta sua
  }
  export const CONE_HALF = 0.62          // 40° de cone de contenção (rad/2 ≈ 0.35) expresso em metros a 1.8m: |attX − defX| < 0.62
  export function createDuel(input: DuelInput): DuelState
  export function step(s: DuelState, dt: number, rng: Rng, input: DuelInput): DuelState
  export function slide(s: DuelState, dir: -1 | 1, input: DuelInput): DuelState        // swipe esq/dir: defX += dir × 0.9 × mods.speed (clamp ±2.5)
  export function trySteal(s: DuelState, rng: Rng, input: DuelInput): DuelState       // toque
  export function jump(s: DuelState, input: DuelInput): DuelState                     // swipe cima
  export function inFront(s: DuelState): boolean
  export function resultOf(s: DuelState): MinigameResult                              // usado internamente; exportado pra teste
  ```
- Mecânica (spec §4): a IA escolhe o próximo `move` quando `move === null`: `hesi`/`crossL`/`crossR`/`spin`/`legs` com pesos base e `driveL/R` pelo `tend.drive` (lado por `tend.side` 70%), `pumpFake` e `shoot` pelo `tend.shoot` (só depois de ≥ 2 movimentos ou `clock < 3`), `pass` pelo `tend.pass` (encerra: `phase 'done'` com `mgLock` e quality = contenção — ele desistiu de você). Durações: `hesi` 0.5s, cruzadas 0.45s (deslocam `attX` ±0.9), `spin` 0.7s (±1.1), `legs` 0.5s (bola exposta 0.35s), `drive` 0.8s (se você não está na frente ao final → `phase 'drive'` = bandeja, result `mgLock` quality 0.15; se está → ele para e volta a driblar), `pumpFake` 0.6s, `shoot` 0.9s (release em 0.45s: `contestDist = |attX − defX| + (airborne > 0 ? 0 : 0.8)` → `phase 'shot'`). Velocidade da IA ÷ `difficulty` nas durações.
- Contenção: por tick, `live += dt`; se `inFront` (`|attX − defX| < CONE_HALF`) e `airborne === 0` → `contain += dt`.
- Roubo: `trySteal` com `exposed > 0` → janela = `mods.stealWindow` (s): sucesso se `exposed ≤ janela` (apertou dentro do fim da exposição) → `phase 'steal'`; senão `stealTries++`, ele passa por você (`defX` é empurrado 1.2m pro lado oposto do atacante... implementar como `attX += sign × 1.2` e `contain` não conta por 1.2s via `airborne = 1.2`); 2ª tentativa errada → `phase 'foul'`, `fouled = true`, `freeThrows` sorteados com `freeThrowP(starOvr)` (2 calls do rng local).
- Salto: `jump` → `airborne = 0.7`; se `move.kind === 'shoot'` e o release ainda não passou (`t − move.at < 0.45`) e `|attX − defX| < 0.9` → no release o toco: `timingHit(t − move.at, 0.45, mods.jumpWindow × 2)` = `'perfect'|'hit'` → `phase 'block'`; se `move.kind === 'pumpFake'` → `bitFake = true`, `airborne = 1.5`, e a IA emenda `driveL/R` (se você está no ar, drive sempre passa) — ou, 30% (rng), ele sobe em cima de você: `phase 'foul'`.
- `resultOf`: `steal` → `{ optionId: 'mgSteal', quality: 1 }`; `block` → `{ 'mgContest', 1 }`; `foul` → `{ 'mgLock', quality: 0.35, turnover: freeThrows[0] && freeThrows[1] }`; `drive` → `{ 'mgLock', 0.15 }`; `clock` → `{ 'mgLock', clamp01(contain/live) }`; `shot`: `c = contain/live`, `k = contestDist === null ? 0 : clamp01(1 − contestDist / 1.5)`, `q = 0.5c + 0.3k + 0.2(bitFake ? 0 : 1)`; optionId = `k ≥ 0.6 ? 'mgContest' : 'mgLock'`; `done` (passe) → `{ 'mgLock', clamp01(contain/live) }`.

- [ ] **Step 1: Escrever os testes**

```ts
// tests/engine/minigames/defense.test.ts
import { describe, expect, test } from 'vitest'
import { createRng } from '../../../src/engine/rng'
import { createDuel, inFront, jump, resultOf, slide, step, trySteal, type DuelInput, type DuelState } from '../../../src/engine/minigames/defense'
import { attrMods } from '../../../src/engine/minigames/common'
import { SLOT_ORDER, type Build, type SlotId } from '../../../src/engine/types'

const build = (ovr: number): Build => ({ attributes: Object.fromEntries(SLOT_ORDER.map(s => [s, ovr])) as Record<SlotId, number>, picks: [], archetype: 'SF', overall: ovr })
const input = (over: Partial<DuelInput> = {}): DuelInput => ({
  tend: { shoot: 0.4, drive: 0.35, pass: 0.25, side: 'right' }, difficulty: 1, mods: attrMods(build(80), 27), starOvr: 85, ...over,
})
const run = (s: DuelState, secs: number, rng = createRng(1), inp = input()) => { for (let i = 0; i < secs * 20; i++) { if (s.phase !== 'live') break; s = step(s, 0.05, rng, inp) } return s }

describe('duelo', () => {
  test('determinístico por seed', () => {
    const a = run(createDuel(input()), 8, createRng(5)), b = run(createDuel(input()), 8, createRng(5))
    expect(a).toEqual(b)
  })
  test('termina sempre (arremesso, infiltração, passe ou relógio) em ≤ 9s, com resultado mg*', () => {
    for (let seed = 0; seed < 100; seed++) {
      const s = run(createDuel(input()), 12, createRng(seed))
      expect(s.phase).not.toBe('live')
      expect(s.result?.optionId).toMatch(/^mg(Lock|Contest|Steal)$/)
      expect(s.result!.quality).toBeGreaterThanOrEqual(0); expect(s.result!.quality).toBeLessThanOrEqual(1)
      expect(s.t).toBeLessThanOrEqual(9.5)
    }
  })
  test('shooter arremessa mais que playmaker, que passa mais', () => {
    const count = (tend: DuelInput['tend']) => { let shots = 0, passes = 0; for (let seed = 0; seed < 80; seed++) { const s = run(createDuel(input({ tend })), 12, createRng(seed), input({ tend })); if (s.phase === 'shot') shots++; if (s.log.at(-1) === 'pass') passes++ } return { shots, passes } }
    const sh = count({ shoot: 0.6, drive: 0.2, pass: 0.2, side: 'left' }), pm = count({ shoot: 0.3, drive: 0.25, pass: 0.45, side: 'left' })
    expect(sh.shots).toBeGreaterThan(pm.shots); expect(pm.passes).toBeGreaterThan(sh.passes)
  })
  test('sombrear certo mantém contenção alta; parado, a cruzada tira você da frente', () => {
    let s = createDuel(input()); const rng = createRng(3)
    // política: a cada tick, deslize pro lado do atacante se saiu do cone
    for (let i = 0; i < 160 && s.phase === 'live'; i++) { if (!inFront(s)) s = slide(s, s.attX > s.defX ? 1 : -1, input()); s = step(s, 0.05, rng, input()) }
    const active = s.contain / s.live
    let p = createDuel(input()); const rng2 = createRng(3)
    for (let i = 0; i < 160 && p.phase === 'live'; i++) p = step(p, 0.05, rng2, input())
    expect(active).toBeGreaterThan(p.contain / p.live)
    expect(active).toBeGreaterThan(0.6)
  })
  test('roubo dentro da janela da bola exposta = mgSteal quality 1; fora = passa por você; 2ª errada = falta', () => {
    let s = createDuel(input()); const rng = createRng(11)
    let stole = false
    for (let i = 0; i < 400 && s.phase === 'live'; i++) {
      if (s.exposed > 0 && s.exposed <= input().mods.stealWindow) { s = trySteal(s, rng, input()); if (s.phase === 'steal') { stole = true; break } }
      s = step(s, 0.05, rng, input())
    }
    if (stole) expect(resultOf(s)).toEqual({ optionId: 'mgSteal', quality: 1 })
    let f = createDuel(input()); const rng3 = createRng(12)
    f = trySteal(f, rng3, input()); expect(f.stealTries).toBe(1); expect(f.phase).toBe('live')
    f = trySteal(f, rng3, input()); expect(f.phase).toBe('foul'); expect(f.freeThrows).toHaveLength(2)
    const r = resultOf(f); expect(r.optionId).toBe('mgLock'); expect(r.quality).toBeCloseTo(0.35, 5)
    expect(r.turnover).toBe(f.freeThrows![0] && f.freeThrows![1])
  })
  test('salto no arremesso real com timing = toco; salto na finta = bitFake', () => {
    const inp = input({ tend: { shoot: 1, drive: 0, pass: 0, side: 'left' } })
    let s = createDuel(inp); const rng = createRng(21)
    let blocked = false, bit = false
    for (let i = 0; i < 400 && s.phase === 'live'; i++) {
      if (s.move?.kind === 'shoot' && Math.abs(s.t - (s.move.at + 0.45)) < 0.03 && s.airborne === 0) { s = jump(s, inp) }
      if (s.move?.kind === 'pumpFake' && s.airborne === 0 && !bit) { s = jump(s, inp); bit = s.bitFake }
      s = step(s, 0.05, rng, inp)
      if (s.phase === 'block') blocked = true
    }
    expect(blocked || bit).toBe(true)
    if (blocked) expect(resultOf(s)).toEqual({ optionId: 'mgContest', quality: 1 })
  })
  test('quality do arremesso contestado > não contestado', () => {
    const inp = input({ tend: { shoot: 1, drive: 0, pass: 0, side: 'left' } })
    const near = { ...createDuel(inp), phase: 'shot' as const, contain: 6, live: 8, contestDist: 0.3 }
    const far = { ...near, contestDist: 1.6 }
    expect(resultOf(near).quality).toBeGreaterThan(resultOf(far).quality)
    expect(resultOf(near).optionId).toBe('mgContest'); expect(resultOf(far).optionId).toBe('mgLock')
  })
  test('difficulty maior = movimentos mais curtos', () => {
    const slow = run(createDuel(input()), 2, createRng(2), input({ difficulty: 0.9 })), fast = run(createDuel(input()), 2, createRng(2), input({ difficulty: 1.3 }))
    expect(fast.log.length).toBeGreaterThanOrEqual(slow.log.length)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run --project unit tests/engine/minigames/defense.test.ts`
Expected: FAIL (exports novos inexistentes).

- [ ] **Step 3: Implementar `defense.ts`**

```ts
import type { Rng } from '../types'
import { clamp01, type MinigameResult } from './index'
import { freeThrowP, timingHit, type AttrMods, type Tendencies } from './common'

export type Move = 'hesi' | 'crossL' | 'crossR' | 'spin' | 'legs' | 'driveL' | 'driveR' | 'pumpFake' | 'shoot' | 'pass'
export interface DuelInput { tend: Tendencies; difficulty: number; mods: AttrMods; starOvr: number }
export interface DuelState {
  t: number; clock: number; attX: number; defX: number; attDist: number
  move: { kind: Move; at: number; dur: number } | null
  exposed: number; airborne: number; contain: number; live: number
  stealTries: number; fouled: boolean; bitFake: boolean; contestDist: number | null
  phase: 'live' | 'shot' | 'drive' | 'steal' | 'block' | 'foul' | 'clock' | 'done'
  log: Move[]; result?: MinigameResult; freeThrows?: [boolean, boolean]
}
export const CONE_HALF = 0.62
const POSSESSION = 8
const RELEASE = 0.45
const DUR: Record<Move, number> = { hesi: 0.5, crossL: 0.45, crossR: 0.45, spin: 0.7, legs: 0.5, driveL: 0.8, driveR: 0.8, pumpFake: 0.6, shoot: 0.9, pass: 0.3 }
const clamp = (v: number, lo: number, hi: number) => Math.min(hi, Math.max(lo, v))

export function createDuel(_input: DuelInput): DuelState {
  return { t: 0, clock: POSSESSION, attX: 0, defX: 0, attDist: 7.5, move: null, exposed: 0, airborne: 0, contain: 0, live: 0, stealTries: 0, fouled: false, bitFake: false, contestDist: null, phase: 'live', log: [] }
}
export function inFront(s: DuelState): boolean { return Math.abs(s.attX - s.defX) < CONE_HALF }

function pickMove(s: DuelState, rng: Rng, input: DuelInput): Move {
  const { tend } = input
  const late = s.clock < 3 || s.log.length >= 2
  const w: Array<[Move, number]> = [['hesi', 0.9], ['crossL', 0.9], ['crossR', 0.9], ['spin', 0.4], ['legs', 0.8]]
  const sideR = tend.side === 'right'
  w.push(['driveR', tend.drive * 3 * (sideR ? 0.7 : 0.3)], ['driveL', tend.drive * 3 * (sideR ? 0.3 : 0.7)])
  if (late) { w.push(['shoot', tend.shoot * 4], ['pumpFake', tend.shoot * 1.6], ['pass', tend.pass * 2.5]) }
  if (s.airborne > 0) return sideR ? 'driveR' : 'driveL'          // você no ar: ele vai
  const total = w.reduce((n, [, x]) => n + x, 0)
  let r = rng.next() * total
  for (const [m, x] of w) { r -= x; if (r <= 0) return m }
  return w[w.length - 1][0]
}

function finishMove(s: DuelState, m: Move, input: DuelInput): DuelState {
  switch (m) {
    case 'crossL': return { ...s, attX: clamp(s.attX - 0.9, -2.5, 2.5) }
    case 'crossR': return { ...s, attX: clamp(s.attX + 0.9, -2.5, 2.5) }
    case 'spin': return { ...s, attX: clamp(s.attX + (s.attX <= 0 ? 1.1 : -1.1), -2.5, 2.5) }
    case 'driveL': case 'driveR': {
      const dir = m === 'driveR' ? 1 : -1
      const beaten = !inFront(s) || s.airborne > 0
      if (beaten) return { ...s, attX: clamp(s.attX + dir * 0.6, -2.5, 2.5), attDist: 1.2, phase: 'drive' }
      return { ...s, attX: clamp(s.attX + dir * 0.3, -2.5, 2.5) }
    }
    case 'pass': return { ...s, phase: 'done' }
    case 'shoot': return { ...s, phase: 'shot' }
    default: return s
  }
}

export function step(s0: DuelState, dt: number, rng: Rng, input: DuelInput): DuelState {
  if (s0.phase !== 'live') return s0
  let s: DuelState = { ...s0, t: s0.t + dt, clock: s0.clock - dt, live: s0.live + dt }
  s.exposed = Math.max(0, s.exposed - dt); s.airborne = Math.max(0, s.airborne - dt)
  if (inFront(s) && s.airborne === 0) s.contain += dt
  if (s.clock <= 0) return finalize({ ...s, phase: 'clock' })
  if (!s.move) {
    const kind = pickMove(s, rng, input)
    s.move = { kind, at: s.t, dur: DUR[kind] / input.difficulty }
    s.log = [...s.log, kind]
    if (kind === 'legs') s.exposed = 0.35
    if (kind === 'crossL' || kind === 'crossR') s.exposed = 0.2
  }
  const m = s.move!
  if (m.kind === 'shoot' && s.contestDist === null && s.t - m.at >= RELEASE) {
    s.contestDist = Math.abs(s.attX - s.defX) + (s.airborne > 0 ? 0 : 0.8)
  }
  if (s.t - m.at >= m.dur) {
    s = finishMove(s, m.kind, input)
    s.move = null
    if (s.phase !== 'live') return finalize(s)
  }
  return s
}

export function slide(s: DuelState, dir: -1 | 1, input: DuelInput): DuelState {
  if (s.phase !== 'live' || s.airborne > 0) return s
  return { ...s, defX: clamp(s.defX + dir * 0.9 * input.mods.speed, -2.5, 2.5) }
}
export function trySteal(s: DuelState, rng: Rng, input: DuelInput): DuelState {
  if (s.phase !== 'live' || s.airborne > 0) return s
  if (s.exposed > 0 && s.exposed <= input.mods.stealWindow) return finalize({ ...s, phase: 'steal' })
  const tries = s.stealTries + 1
  if (tries >= 2) {
    const p = freeThrowP(input.starOvr)
    return finalize({ ...s, stealTries: tries, fouled: true, phase: 'foul', freeThrows: [rng.chance(p), rng.chance(p)] })
  }
  return { ...s, stealTries: tries, airborne: 1.2, attX: clamp(s.attX + (s.attX >= s.defX ? 1.2 : -1.2), -2.5, 2.5) }
}
export function jump(s: DuelState, input: DuelInput): DuelState {
  if (s.phase !== 'live' || s.airborne > 0) return s
  const m = s.move
  if (m?.kind === 'shoot' && s.t - m.at < RELEASE + 0.05 && Math.abs(s.attX - s.defX) < 0.9) {
    const hit = timingHit(s.t - m.at, RELEASE, input.mods.jumpWindow * 2)
    if (hit !== 'miss') return finalize({ ...s, phase: 'block' })
    return { ...s, airborne: 0.7 }
  }
  if (m?.kind === 'pumpFake') return { ...s, airborne: 1.5, bitFake: true }
  return { ...s, airborne: 0.7 }
}

export function resultOf(s: DuelState): MinigameResult {
  const c = s.live > 0 ? clamp01(s.contain / s.live) : 0
  switch (s.phase) {
    case 'steal': return { optionId: 'mgSteal', quality: 1 }
    case 'block': return { optionId: 'mgContest', quality: 1 }
    case 'foul': return { optionId: 'mgLock', quality: 0.35, turnover: !!(s.freeThrows && s.freeThrows[0] && s.freeThrows[1]) }
    case 'drive': return { optionId: 'mgLock', quality: 0.15 }
    case 'shot': {
      const k = s.contestDist === null ? 0 : clamp01(1 - s.contestDist / 1.5)
      return { optionId: k >= 0.6 ? 'mgContest' : 'mgLock', quality: clamp01(0.5 * c + 0.3 * k + 0.2 * (s.bitFake ? 0 : 1)) }
    }
    default: return { optionId: 'mgLock', quality: c }
  }
}
function finalize(s: DuelState): DuelState { return { ...s, result: resultOf(s) } }
```

Nota sobre `turnover` em `foul`: `MinigameResult.turnover` é `boolean | undefined`; `false` é aceito pelo reducer (`!exec?.turnover`).

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run --project unit tests/engine/minigames/defense.test.ts`
Expected: PASS. Se "termina em ≤ 9.5s" falhar por `pass` só depois de 2 movimentos, é esperado que o relógio (8s) encerre — verificar que `phase` vira `'clock'`.

- [ ] **Step 5: Typecheck (Defense.tsx v1 vai quebrar — esperado)**

Run: `npx tsc -p tsconfig.app.json --noEmit`
Expected: erros SÓ em `src/ui/minigames/Defense.tsx` (imports v1). Renomear já agora `src/ui/minigames/Defense.tsx` → `src/ui/minigames/DefenseFallback.tsx` e trocar seu conteúdo por um stub que reusa a mecânica v1? NÃO — o fallback da Task 12 usa a mecânica v2 com HUD 2D. Neste passo: substituir o conteúdo de `Defense.tsx` por um stub mínimo que compila:
```tsx
import type { MinigameProps } from './types'
// stub: reescrito na Task 10
export function DefenseGame(_: MinigameProps) { return <div className="mg mg-defense" /> }
```
Run de novo o typecheck: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/engine/minigames/defense.ts tests/engine/minigames/defense.test.ts src/ui/minigames/Defense.tsx
git commit -m "feat(arcade): engine da muralha v2 — duelo 1x1 continuo por tendencias"
```

---

### Task 5: Engine do ARREMESSO v2 (`shot.ts`)

**Files:**
- Modify: `src/engine/minigames/shot.ts` (mantém `idealSpeed`, `trajectory`, `crossX`, `meterValue`, `angleValue`, `scenarioFor` do v1; troca `evaluate`, `availableTypes`, `resultFor` e acrescenta closeout, salto, tabela, lances livres)
- Modify: `tests/engine/minigames/shot.test.ts` (adaptar aos tipos novos; manter os testes de física)

**Interfaces:**
- Consumes: `AttrMods`, `timingHit`, `OppPlayer` (Task 2).
- Produces:
  ```ts
  export type ShotType = 'layup' | 'floater' | 'mid' | 'stepback' | 'fadeaway' | 'three' | 'bank' | 'dunk'
  export interface ShotSpec { d: number; releaseH: number; tol: number; sep: number; optionId: string; tolKey: keyof AttrMods['tol'] }
  export const SHOTS: Record<ShotType, ShotSpec>
  //  layup {1.5, 2.3, 0.88, 0, mgLayup, layup} · floater {2.8, 2.35, 0.5, 0.4, mgLayup, floater} · mid {5.0, 2.05, 0.55, 0, mgMid, mid}
  //  stepback {7.6, 2.05, 0.44, 1.2, mgThree, three} · fadeaway {5.2, 2.15, 0.47, 0.8, mgMid, mid} · three {7.24, 2.05, 0.55, 0, mgThree, three}
  //  bank {4.2, 2.05, 0.6, 0, mgMid, mid} · dunk {0.6, 2.3, 0.3, 0, mgDunk, dunk}
  export function availableTypes(build: Build, age: number): ShotType[]   // floater fin≥60, stepback handles≥70, fadeaway clutch≥70, dunk physical≥75 (efetivos via ageMultiplier)
  export interface Closeout { x: number; speed: number; handUp: boolean; arrived: boolean; handX: number; handH: number }
  export function createCloseout(rng: Rng, opts: { difficulty: number; defender: OppPlayer; sep: number }): Closeout   // x0 = 2.5 + rng.next()×1.5 + sep, speed = defender.speed × difficulty, handH = defender.reach
  export function stepCloseout(c: Closeout, dt: number): Closeout   // x −= speed·dt; handUp quando x ≤ 1.2; arrived quando x ≤ 0.5
  export type Jump = 'perfect' | 'hit' | 'miss' | null
  export interface EvalOpts { closeout: Closeout | null; jump: Jump; mods: AttrMods; bankAim?: boolean }
  export interface ShotEval { quality: number; err: number; entryAngle: number; verdict: 'swish' | 'bank' | 'short' | 'long' | 'rimOut' | 'blocked'; blocked: boolean; contested: boolean }
  export function evaluate(type: ShotType, angleRad: number, speed: number, opts: EvalOpts): ShotEval
  export function bankCrossX(angleRad: number, speed: number, releaseH: number, d: number): number | null   // reflexão na tabela (plano em d+0.15, restituição 0.7) → x onde cruza 3.05 descendo
  export function resultFor(type: ShotType, ev: ShotEval): MinigameResult   // optionId de SHOTS[type], quality = ev.quality
  export function freeThrowQuality(q: [number, number]): number             // média
  export function jumpTiming(t: number, apex: number, mods: AttrMods): Jump  // timingHit(t, apex, mods.jumpWindow)
  ```
- `evaluate`: `tol = SHOTS[type].tol × mods.tol[tolKey] × (jump === 'perfect' ? 1.15 : jump === 'hit' ? 1.0 : jump === 'miss' ? 0.7 : 1.0)`; contestado (`closeout?.arrived`) → `tol × 0.6`; **bate na mão**: `closeout?.handUp` e a altura da bola em `x = closeout.x` (via `trajectory(...).pointAt` resolvido pra x) `< closeout.handH` → `quality 0`, `blocked true`, `verdict 'blocked'`; `bankAim` usa `bankCrossX` em vez de `crossX` e `tol × 1.1`; dunk = só velocidade (v1). Fadiga: `speed × (1 − mods.fatigue × 0.5)` antes de avaliar.

- [ ] **Step 1: Adaptar/escrever testes**

Substituir em `tests/engine/minigames/shot.test.ts` os testes de `availableTypes`/`evaluate`/`resultFor` por:

```ts
import { attrMods, opponentFive } from '../../../src/engine/minigames/common'
import { initLeague } from '../../../src/data/league'
import { availableTypes, bankCrossX, createCloseout, evaluate, idealSpeed, jumpTiming, resultFor, SHOTS, stepCloseout, freeThrowQuality, type ShotType } from '../../../src/engine/minigames/shot'
// (manter build(), createRng imports já existentes)
const mods = attrMods(build(85), 27)
const five = opponentFive(initLeague(createRng(1)), 'bos')
const rad = (deg: number) => deg * Math.PI / 180

describe('repertório', () => {
  test('availableTypes por atributo', () => {
    expect(availableTypes(build(55), 27)).toEqual(['layup', 'mid', 'three', 'bank'])
    expect(availableTypes(build(95), 27)).toEqual(['layup', 'floater', 'mid', 'stepback', 'fadeaway', 'three', 'bank', 'dunk'])
  })
  test('velocidade ideal acerta o aro para todo tipo (exceto dunk)', () => {
    for (const t of Object.keys(SHOTS) as ShotType[]) {
      if (t === 'dunk') continue
      const s = SHOTS[t]; const v = idealSpeed(rad(50), s.d, s.releaseH)
      const ev = evaluate(t, rad(50), v, { closeout: null, jump: null, mods })
      expect(ev.err).toBeLessThan(0.02); expect(ev.quality).toBeGreaterThan(0.95)
    }
  })
  test('salto perfeito > sem salto > salto errado; contestado reduz', () => {
    const v = idealSpeed(rad(50), SHOTS.three.d, SHOTS.three.releaseH) * 1.03
    const q = (jump: 'perfect' | 'hit' | 'miss' | null, closeout = null as ReturnType<typeof createCloseout> | null) => evaluate('three', rad(50), v, { closeout, jump, mods }).quality
    expect(q('perfect')).toBeGreaterThan(q(null)); expect(q(null)).toBeGreaterThan(q('miss'))
    let c = createCloseout(createRng(1), { difficulty: 1, defender: five[0], sep: 0 }); for (let i = 0; i < 200; i++) c = stepCloseout(c, 0.05)
    expect(c.arrived).toBe(true); expect(q(null, c)).toBeLessThan(q(null))
  })
  test('arco baixo com a mão levantada = bloqueado', () => {
    let c = createCloseout(createRng(2), { difficulty: 1, defender: five[0], sep: 0 }); while (!c.handUp) c = stepCloseout(c, 0.05)
    const flat = evaluate('three', rad(18), idealSpeed(rad(18), 7.24, 2.05) || 12, { closeout: c, jump: null, mods })
    expect(flat.blocked).toBe(true); expect(flat.quality).toBe(0); expect(flat.verdict).toBe('blocked')
  })
  test('step-back/fadeaway afastam o closeout (sep)', () => {
    const a = createCloseout(createRng(3), { difficulty: 1, defender: five[0], sep: SHOTS.stepback.sep }), b = createCloseout(createRng(3), { difficulty: 1, defender: five[0], sep: 0 })
    expect(a.x).toBeGreaterThan(b.x)
  })
  test('tabela: bankCrossX devolve x perto do aro para o tiro certo e evaluate usa bankAim', () => {
    const d = SHOTS.bank.d, v = idealSpeed(rad(52), d + 0.6, 2.05)   // mira 0.6m "além" do aro → reflete de volta
    const x = bankCrossX(rad(52), v, 2.05, d)
    expect(x).not.toBeNull(); expect(Math.abs(x! - d)).toBeLessThan(0.5)
    const ev = evaluate('bank', rad(52), v, { closeout: null, jump: null, mods, bankAim: true })
    expect(ev.quality).toBeGreaterThan(0.5)
  })
  test('fadiga reduz a força efetiva (mesmo input, mais curto)', () => {
    const tired = attrMods(build(80, { physical: 50 }), 27, 4)
    const v = idealSpeed(rad(50), 7.24, 2.05)
    expect(evaluate('three', rad(50), v, { closeout: null, jump: null, mods: tired }).verdict).toBe('short')
  })
  test('resultFor mapeia optionId; freeThrowQuality é média; jumpTiming usa a janela', () => {
    const ev = evaluate('layup', rad(55), idealSpeed(rad(55), 1.5, 2.3), { closeout: null, jump: null, mods })
    expect(resultFor('layup', ev).optionId).toBe('mgLayup'); expect(resultFor('stepback', ev).optionId).toBe('mgThree'); expect(resultFor('bank', ev).optionId).toBe('mgMid')
    expect(freeThrowQuality([1, 0.5])).toBeCloseTo(0.75, 5)
    expect(jumpTiming(0.5, 0.5, mods)).toBe('perfect'); expect(jumpTiming(0.9, 0.5, mods)).toBe('miss')
  })
})
```
(Os testes v1 de `idealSpeed`, `trajectory`, `crossX`, `meterValue`, `angleValue`, `scenarioFor` ficam.) O `build()` do arquivo precisa aceitar overrides: `const build = (ovr: number, over: Partial<Record<SlotId, number>> = {}): Build => ({ attributes: { ...Object.fromEntries(SLOT_ORDER.map(s => [s, ovr])), ...over } as Record<SlotId, number>, picks: [], archetype: 'SF', overall: ovr })`.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run --project unit tests/engine/minigames/shot.test.ts`
Expected: FAIL nos testes novos (exports/assinaturas).

- [ ] **Step 3: Implementar**

Em `shot.ts`, manter física v1 e acrescentar/substituir:

```ts
import { ageMultiplier } from '../season'
import { timingHit, type AttrMods, type OppPlayer } from './common'
import type { Build, Rng } from '../types'
import type { MinigameResult } from './index'

export type ShotType = 'layup' | 'floater' | 'mid' | 'stepback' | 'fadeaway' | 'three' | 'bank' | 'dunk'
export interface ShotSpec { d: number; releaseH: number; tol: number; sep: number; optionId: string; tolKey: keyof AttrMods['tol'] }
export const SHOTS: Record<ShotType, ShotSpec> = {
  layup:    { d: 1.5,  releaseH: 2.3,  tol: 0.88, sep: 0,   optionId: 'mgLayup', tolKey: 'layup' },
  floater:  { d: 2.8,  releaseH: 2.35, tol: 0.5,  sep: 0.4, optionId: 'mgLayup', tolKey: 'floater' },
  mid:      { d: 5.0,  releaseH: 2.05, tol: 0.55, sep: 0,   optionId: 'mgMid',   tolKey: 'mid' },
  stepback: { d: 7.6,  releaseH: 2.05, tol: 0.44, sep: 1.2, optionId: 'mgThree', tolKey: 'three' },
  fadeaway: { d: 5.2,  releaseH: 2.15, tol: 0.47, sep: 0.8, optionId: 'mgMid',   tolKey: 'mid' },
  three:    { d: 7.24, releaseH: 2.05, tol: 0.55, sep: 0,   optionId: 'mgThree', tolKey: 'three' },
  bank:     { d: 4.2,  releaseH: 2.05, tol: 0.6,  sep: 0,   optionId: 'mgMid',   tolKey: 'mid' },
  dunk:     { d: 0.6,  releaseH: 2.3,  tol: 0.3,  sep: 0,   optionId: 'mgDunk',  tolKey: 'dunk' },
}
const ORDER: ShotType[] = ['layup', 'floater', 'mid', 'stepback', 'fadeaway', 'three', 'bank', 'dunk']
export function availableTypes(build: Build, age: number): ShotType[] {
  const m = ageMultiplier(age, build.attributes.physical); const a = build.attributes
  return ORDER.filter(t =>
    t === 'floater' ? a.finishing * m >= 60 : t === 'stepback' ? a.handles * m >= 70 : t === 'fadeaway' ? a.clutch * m >= 70 : t === 'dunk' ? a.physical * m >= 75 : true)
}
export interface Closeout { x: number; speed: number; handUp: boolean; arrived: boolean; handX: number; handH: number }
export function createCloseout(rng: Rng, opts: { difficulty: number; defender: OppPlayer; sep: number }): Closeout {
  const x = 2.5 + rng.next() * 1.5 + opts.sep
  return { x, speed: opts.defender.speed * opts.difficulty, handUp: false, arrived: false, handX: x, handH: opts.defender.reach }
}
export function stepCloseout(c: Closeout, dt: number): Closeout {
  const x = Math.max(0.3, c.x - c.speed * dt)
  return { ...c, x, handX: x, handUp: x <= 1.2, arrived: x <= 0.5 }
}
export type Jump = 'perfect' | 'hit' | 'miss' | null
export interface EvalOpts { closeout: Closeout | null; jump: Jump; mods: AttrMods; bankAim?: boolean }
export interface ShotEval { quality: number; err: number; entryAngle: number; verdict: 'swish' | 'bank' | 'short' | 'long' | 'rimOut' | 'blocked'; blocked: boolean; contested: boolean }
const G = 9.81, RIM_H = 3.05

// altura da bola quando passa por x (subindo ou descendo) — null se nunca chega
function heightAtX(angle: number, speed: number, releaseH: number, x: number): number | null {
  const vx = speed * Math.cos(angle); if (vx <= 0) return null
  const t = x / vx
  return releaseH + speed * Math.sin(angle) * t - 0.5 * G * t * t
}
export function bankCrossX(angle: number, speed: number, releaseH: number, d: number): number | null {
  const board = d + 0.15, vx = speed * Math.cos(angle), vy = speed * Math.sin(angle)
  if (vx <= 0) return null
  const tb = board / vx; const yb = releaseH + vy * tb - 0.5 * G * tb * tb
  if (yb < RIM_H - 0.1 || yb > RIM_H + 1.1) return null       // não acerta a tabela na zona útil
  const vyb = vy - G * tb; const vxr = -vx * 0.7                // reflexão: x inverte com restituição 0.7
  // após refletir: y(t) = yb + vyb t − ½g t², x(t) = board + vxr t; cruza RIM_H descendo
  const a = -0.5 * G, b = vyb, c = yb - RIM_H
  const disc = b * b - 4 * a * c; if (disc < 0) return null
  const t1 = (-b - Math.sqrt(disc)) / (2 * a), t2 = (-b + Math.sqrt(disc)) / (2 * a)
  const t = Math.max(t1, t2); if (t <= 0) return null
  return board + vxr * t
}
export function evaluate(type: ShotType, angle: number, speed0: number, opts: EvalOpts): ShotEval {
  const s = SHOTS[type]; const speed = speed0 * (1 - opts.mods.fatigue * 0.5)
  const co = opts.closeout
  if (co?.handUp) {
    const h = heightAtX(angle, speed, s.releaseH, co.handX)
    if (h !== null && co.handX < s.d && h < co.handH) return { quality: 0, err: Infinity, entryAngle: 0, verdict: 'blocked', blocked: true, contested: true }
  }
  if (type === 'dunk') {
    const ref = refSpeed('dunk'); const q = Math.max(0, 1 - Math.max(0, Math.abs(speed - ref) / ref - 0.3) / 0.7)
    return { quality: q, err: Math.abs(speed - ref), entryAngle: 90, verdict: q > 0.5 ? 'swish' : 'rimOut', blocked: false, contested: !!co?.arrived }
  }
  const cross = opts.bankAim ? bankCrossX(angle, speed, s.releaseH, s.d) : crossX(angle, speed, s.releaseH)
  let tol = s.tol * opts.mods.tol[s.tolKey] * (opts.jump === 'perfect' ? 1.15 : opts.jump === 'miss' ? 0.7 : 1.0) * (opts.bankAim ? 1.1 : 1)
  const contested = !!co?.arrived; if (contested) tol *= 0.6
  if (cross === null) return { quality: 0, err: Infinity, entryAngle: 0, verdict: 'short', blocked: false, contested }
  const err = Math.abs(cross - s.d)
  const entry = entryAngleOf(angle, speed, s.releaseH)   // v1 já calcula em `evaluate`; extrair pra helper
  if (entry < 32) tol *= 0.5
  const quality = Math.max(0, 1 - err / tol)
  const verdict = quality > 0.7 ? (opts.bankAim ? 'bank' : 'swish') : err < 0.12 ? 'rimOut' : cross < s.d ? 'short' : 'long'
  return { quality, err, entryAngle: entry, verdict, blocked: false, contested }
}
export function resultFor(type: ShotType, ev: ShotEval): MinigameResult { return { optionId: SHOTS[type].optionId, quality: ev.quality } }
export function freeThrowQuality(q: [number, number]): number { return (q[0] + q[1]) / 2 }
export function jumpTiming(t: number, apex: number, mods: AttrMods): Jump { return timingHit(t, apex, mods.jumpWindow) }
```
`refSpeed('dunk')` e `entryAngleOf` vêm do v1 (`refSpeed` já existe; extrair o cálculo de ângulo de entrada do `evaluate` v1 para `entryAngleOf(angle, speed, releaseH)`: `atan2(|vy no cruzamento|, vx)` em graus). Remover o `evaluate`/`availableTypes`/`resultFor` v1 (assinaturas antigas).

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run --project unit tests/engine/minigames/shot.test.ts`
Expected: PASS. Se o teste da tabela falhar por `null`, ajustar a mira do teste (`d + 0.6`) — a física deve refletir; não afrouxar a tolerância do teste abaixo de 0.5.

- [ ] **Step 5: Stub em `Shot.tsx`** (o v1 usa `evaluate` antigo): substituir o conteúdo de `src/ui/minigames/Shot.tsx` por um stub que compila (`export function ShotGame(_: MinigameProps) { return <div className="mg mg-shot" /> }`). Typecheck limpo.

- [ ] **Step 6: Commit**

```bash
git add src/engine/minigames/shot.ts tests/engine/minigames/shot.test.ts src/ui/minigames/Shot.tsx
git commit -m "feat(arcade): engine do arremesso v2 — repertorio, closeout, salto, tabela, fadiga"
```

---

### Task 6: Engine da JOGADA v2 — fases, rotas e esquemas (`playbook.ts` parte 1)

**Files:**
- Rewrite: `src/engine/minigames/playbook.ts` (manter `COURT`, `Pos`, `distToBasket`, `inPaint`, `beyondArc`, `openness`, `shotOptionFor`, `FORMATIONS` do v1; reescrever estado/step)
- Rewrite: `tests/engine/minigames/playbook.test.ts`

**Interfaces:**
- Consumes: `OppPlayer` (Task 2), `AttrMods`.
- Produces:
  ```ts
  export type Scheme = 'man' | 'zone' | 'switch' | 'trap' | 'press'
  export type Phase = 'read' | 'draw' | 'run' | 'rebound' | 'shooting' | 'turnover' | 'done'
  export type Template = 'pnr' | 'horns' | 'doubleScreen' | 'iso' | 'fiveOut' | 'transition'
  export interface Route { points: Pos[]; screen: boolean }          // rota desenhada; screen = termina em bloqueio
  export interface PlaybookState {
    phase: Phase; t: number; clock: number; scheme: Scheme
    attackers: Pos[]; defenders: Defender[]; you: 0
    routes: Route[]; routeProgress: number[]                             // 5 rotas (vazia = parado), progresso em metros
    ball: { holder: number; flying: { from: number; to: number; progress: number } | null }
    lastPassAt: number; passes: number; screenedUntil: number[]        // por defensor: preso até t
    helpUntil: number; trapUntil: number; mismatch: boolean
    feintUntil: number; pumpUntil: number; pressure: number
    turnover: 'intercept' | 'strip' | 'clock' | 'charge' | null
    firstShotOpenness: number | null                                    // preenchido no arremesso forçado (→ rebote)
    result?: MinigameResult
  }
  export interface PlaybookInput { kind: WatchedGameKind; five: OppPlayer[]; mods: AttrMods; difficulty: number }
  export function createPlaybook(rng: Rng, input: PlaybookInput): PlaybookState     // phase 'read', scheme sorteado (pesos por força do five), formação 'horns' por default
  export function applyTemplate(s: PlaybookState, tpl: Template): PlaybookState      // preenche routes+attackers com um dos 6 modelos (fase draw)
  export function setRoute(s: PlaybookState, idx: number, points: Pos[]): PlaybookState   // clamp na quadra, ≤ 6 pontos
  export function toggleScreen(s: PlaybookState, idx: number): PlaybookState
  export function startRun(s: PlaybookState): PlaybookState                          // read/draw → run, clock 12
  export function step(s: PlaybookState, dt: number, rng: Rng, input: PlaybookInput): PlaybookState
  export const SCHEME_SIGNAL: Record<Scheme, string>                                  // chave i18n do sinal ('mg.pb.scheme.<scheme>') — usado só no desfecho
  ```
- Esquemas (spec §2): alvo de cada defensor por tick:
  - `man`: entre seu homem e a cesta a 0.35 (0.15 no portador); ajuda quando portador no garrafão (v1).
  - `zone`: 5 áreas fixas (2 em cima nas asas do arco, 3 embaixo: cantos e meio do garrafão); defensor vai pro ponto mais próximo entre o centro da área e o atacante dentro dela; portador no garrafão → os 3 de baixo colapsam.
  - `switch`: `man`, mas quando um atacante com `screen` chega a < 1.0m do defensor do portador, os dois defensores trocam de homem (`man`), e se o pivô (defensor de maior `reach`) ficar em você → `mismatch = true` (sua velocidade ×1.15).
  - `trap`: `man`; após um bloqueio no portador, o defensor do bloqueador também vai no portador por 1.5s (`trapUntil`), deixando o bloqueador livre.
  - `press`: `man` com portador a 0.5m e desarme ×1.5; se você passa o marcador (dist > 1.2m atrás), ele fica pra trás 1s.
- `read` (2s) só conta tempo; `draw` não conta; `run` move ímãs pelas rotas (`speed = 4.2 × mods.speed`, defensores `d.speed × difficulty`), bloqueios prendem o defensor mais próximo por 0.8s (`screenedUntil`).
- Sorteio do esquema em `createPlaybook`: `strength = média ovr do five`; pesos `man 3, zone 2, switch 1 + (strength−70)/10, trap 1 + (strength−70)/10, press 1`.

- [ ] **Step 1: Testes**

```ts
// tests/engine/minigames/playbook.test.ts (parte 1)
import { describe, expect, test } from 'vitest'
import { createRng } from '../../../src/engine/rng'
import { initLeague } from '../../../src/data/league'
import { attrMods, opponentFive } from '../../../src/engine/minigames/common'
import { applyTemplate, COURT, createPlaybook, inPaint, setRoute, startRun, step, toggleScreen, type PlaybookInput, type PlaybookState, type Scheme } from '../../../src/engine/minigames/playbook'
import { SLOT_ORDER, type Build, type SlotId } from '../../../src/engine/types'

const build = (ovr: number): Build => ({ attributes: Object.fromEntries(SLOT_ORDER.map(s => [s, ovr])) as Record<SlotId, number>, picks: [], archetype: 'SF', overall: ovr })
const league = initLeague(createRng(1))
const input = (): PlaybookInput => ({ kind: 'rivalry', five: opponentFive(league, 'bos'), mods: attrMods(build(80), 27), difficulty: 1 })
const inside = (p: { x: number; y: number }) => p.x >= 0 && p.x <= COURT.w && p.y >= 0 && p.y <= COURT.d
const run = (s: PlaybookState, secs: number, rng: ReturnType<typeof createRng>) => { for (let i = 0; i < secs * 20; i++) { if (s.phase !== 'run' && s.phase !== 'read') break; s = step(s, 0.05, rng, input()) } return s }

describe('fases e rotas', () => {
  test('createPlaybook começa em read e passa a draw sozinho após 2s; startRun só de draw', () => {
    let s = createPlaybook(createRng(1), input()); expect(s.phase).toBe('read')
    for (let i = 0; i < 45; i++) s = step(s, 0.05, createRng(1), input())
    expect(s.phase).toBe('draw')
    s = startRun(s); expect(s.phase).toBe('run'); expect(s.clock).toBe(12)
  })
  test('scheme sorteado entre os 5, determinístico por seed', () => {
    const seen = new Set<Scheme>()
    for (let seed = 0; seed < 60; seed++) seen.add(createPlaybook(createRng(seed), input()).scheme)
    expect(seen.size).toBeGreaterThanOrEqual(4)
    expect(createPlaybook(createRng(7), input()).scheme).toBe(createPlaybook(createRng(7), input()).scheme)
  })
  test('setRoute clampa na quadra e corta em 6 pontos; toggleScreen alterna', () => {
    let s = applyTemplate(createPlaybook(createRng(1), input()), 'iso')
    s = setRoute(s, 1, Array.from({ length: 9 }, (_, i) => ({ x: -5 + i * 4, y: 30 - i * 5 })))
    expect(s.routes[1].points).toHaveLength(6); expect(s.routes[1].points.every(inside)).toBe(true)
    expect(toggleScreen(s, 1).routes[1].screen).toBe(!s.routes[1].screen)
  })
  test('rodando, os ímãs seguem a rota e ficam na quadra; sem rota, ficam parados', () => {
    let s = applyTemplate(createPlaybook(createRng(2), input()), 'fiveOut')
    s = setRoute(s, 2, [{ ...s.attackers[2] }, { x: 7.6, y: 3.0 }])
    s = setRoute(s, 3, [])
    const before3 = { ...s.attackers[3] }
    s = run(startRun(s), 3, createRng(2))
    expect(s.attackers[2].y).toBeLessThan(5); expect(s.attackers.every(inside) && s.defenders.every(inside)).toBe(true)
    expect(s.attackers[3]).toEqual(before3)
  })
  test('bloqueio prende o defensor mais próximo do portador por 0.8s', () => {
    let s = applyTemplate(createPlaybook(createRng(3), input()), 'pnr')
    s = { ...s, scheme: 'man' }
    const you = s.attackers[0]
    s = setRoute(s, 4, [{ ...s.attackers[4] }, { x: you.x + 0.6, y: you.y + 0.6 }]); s = toggleScreen(s, 4); if (!s.routes[4].screen) s = toggleScreen(s, 4)
    s = run(startRun(s), 2.5, createRng(3))
    expect(s.screenedUntil.some(u => u > 0)).toBe(true)
  })
  test('zona: defensores ocupam áreas (ninguém segue o corte); pressão: marcador a ≤ 0.6m', () => {
    let z = { ...applyTemplate(createPlaybook(createRng(4), input()), 'fiveOut'), scheme: 'zone' as const }
    z = setRoute(z, 2, [{ ...z.attackers[2] }, { x: 1.0, y: 12.0 }]); z = run(startRun(z), 2, createRng(4))
    const follower = z.defenders.find(d => Math.hypot(d.x - z.attackers[2].x, d.y - z.attackers[2].y) < 1.0)
    expect(follower).toBeUndefined()
    let p = { ...applyTemplate(createPlaybook(createRng(5), input()), 'iso'), scheme: 'press' as const }
    p = run(startRun(p), 1, createRng(5))
    const guard = p.defenders[p.defenders.findIndex(d => d.man === 0)]
    expect(Math.hypot(guard.x - p.attackers[0].x, guard.y - p.attackers[0].y)).toBeLessThanOrEqual(0.7)
  })
  test('ajuda: portador no garrafão puxa o defensor do companheiro mais perto da cesta', () => {
    let s = { ...applyTemplate(createPlaybook(createRng(6), input()), 'iso'), scheme: 'man' as const }
    s = setRoute(s, 0, [{ ...s.attackers[0] }, { x: COURT.basket.x, y: COURT.basket.y + 2.5 }])
    s = run(startRun(s), 3, createRng(6))
    expect(inPaint(s.attackers[0])).toBe(true); expect(s.helpUntil).toBeGreaterThan(0)
  })
  test('relógio zerado = turnover clock com mgMid', () => {
    let s = startRun(applyTemplate(createPlaybook(createRng(8), input()), 'horns'))
    for (let i = 0; i < 300 && s.phase === 'run'; i++) s = step(s, 0.05, createRng(8), input())
    expect(s.phase).toBe('turnover'); expect(s.turnover).toBe('clock'); expect(s.result).toEqual({ optionId: 'mgMid', quality: 0, turnover: true })
  })
})
```

- [ ] **Step 2: Rodar e ver falhar** — `npx vitest run --project unit tests/engine/minigames/playbook.test.ts` → FAIL.

- [ ] **Step 3: Implementar a parte 1 de `playbook.ts`**

Estrutura obrigatória (o restante segue o v1 onde marcado):

```ts
export const COURT = { w: 15.24, d: 14.0, basket: { x: 7.62, y: 1.575 }, arc: 7.24, cornerX: 0.9, cornerY: 4.3, paintW: 4.9, paintD: 5.8 }   // = v1
// Pos, Defender { x, y, speed, man, target }, distToBasket, inPaint, beyondArc, openness, shotOptionFor, FORMATIONS: manter do v1 (FORMATIONS vira base dos templates)
export type Scheme = 'man' | 'zone' | 'switch' | 'trap' | 'press'
export const SCHEME_SIGNAL: Record<Scheme, string> = { man: 'mg.pb.scheme.man', zone: 'mg.pb.scheme.zone', switch: 'mg.pb.scheme.switch', trap: 'mg.pb.scheme.trap', press: 'mg.pb.scheme.press' }
const TEMPLATES: Record<Template, { formation: keyof typeof FORMATIONS; routes: Array<{ to: Pos[]; screen: boolean }> }> = {
  pnr:          { formation: 'pnr',     routes: [{ to: [{ x: 6.2, y: 6.0 }], screen: false }, { to: [], screen: false }, { to: [], screen: false }, { to: [], screen: false }, { to: [{ x: 8.4, y: 8.6 }], screen: true }] },
  horns:        { formation: 'horns',   routes: [{ to: [{ x: 7.62, y: 7.0 }], screen: false }, { to: [{ x: 3.0, y: 5.0 }], screen: false }, { to: [{ x: 12.2, y: 5.0 }], screen: false }, { to: [{ x: 6.4, y: 8.2 }], screen: true }, { to: [{ x: 8.8, y: 8.2 }], screen: true }] },
  doubleScreen: { formation: 'fiveOut', routes: [{ to: [], screen: false }, { to: [{ x: 5.4, y: 4.0 }], screen: true }, { to: [{ x: 9.8, y: 4.0 }], screen: true }, { to: [{ x: 7.62, y: 3.2 }], screen: false }, { to: [], screen: false }] },
  iso:          { formation: 'iso',     routes: [{ to: [], screen: false }, { to: [{ x: 1.2, y: 4.3 }], screen: false }, { to: [{ x: 14.0, y: 4.3 }], screen: false }, { to: [{ x: 2.0, y: 9.0 }], screen: false }, { to: [{ x: 13.2, y: 9.0 }], screen: false }] },
  fiveOut:      { formation: 'fiveOut', routes: [{ to: [], screen: false }, { to: [{ x: 4.0, y: 2.6 }], screen: false }, { to: [], screen: false }, { to: [], screen: false }, { to: [{ x: 11.2, y: 2.6 }], screen: false }] },
  transition:   { formation: 'fiveOut', routes: [{ to: [{ x: 7.62, y: 4.0 }], screen: false }, { to: [{ x: 1.0, y: 4.3 }], screen: false }, { to: [{ x: 14.2, y: 4.3 }], screen: false }, { to: [{ x: 5.0, y: 2.0 }], screen: false }, { to: [{ x: 10.2, y: 2.0 }], screen: false }] },
}
const ZONES: Pos[] = [{ x: 4.6, y: 8.2 }, { x: 10.6, y: 8.2 }, { x: 2.4, y: 3.2 }, { x: 7.62, y: 3.6 }, { x: 12.8, y: 3.2 }]
```
`createPlaybook`: sorteia scheme (pesos acima, 1 call), formação inicial `horns`, defensores man-to-man como v1 com `speed = five[i].speed × difficulty` (ordem: five[i] marca attackers[i]), `phase 'read'`, `t 0`, `clock 12`, `routes` = 5 vazias, arrays zerados.
`step` em `read`: `t += dt`; aos 2s → `draw`. Em `draw`: no-op. Em `run`: relógio, mover atacantes ao longo das rotas (`routeProgress[i] += 4.2 × mods.speed × (mismatch && i===0 ? 1.15 : 1) × dt`, posição = ponto ao longo da polilinha), bola em voo (0.35s) como v1, bloqueios (atacante com `screen` a < 1.0m de um defensor ≠ o seu → `screenedUntil[d] = t + 0.8`; `switch`: troca `man`; `trap`: `trapUntil = t + 1.5`), alvos por esquema, movimento dos defensores (v1: em direção ao alvo a `speed × dt`, presos se `screenedUntil > t`), ajuda (`helpUntil`), desarme (v1 ×1.5 em `press`, ÷ `mods.stripResist`), relógio → turnover. Interceptação/passe/arremesso ficam na Task 7 (aqui `pass`/`shoot` ainda não existem — não exportar).

- [ ] **Step 4: Rodar e ver passar** — os testes da parte 1 verdes. Stub `Playbook.tsx` (como nas tasks 4/5) pra compilar.

- [ ] **Step 5: Commit**

```bash
git add src/engine/minigames/playbook.ts tests/engine/minigames/playbook.test.ts src/ui/minigames/Playbook.tsx
git commit -m "feat(arcade): engine da jogada v2 — fases, rotas desenhadas, 5 esquemas de defesa"
```

---

### Task 7: Engine da JOGADA v2 — ações ao vivo, arremesso, rebote (`playbook.ts` parte 2)

**Files:**
- Modify: `src/engine/minigames/playbook.ts`
- Modify: `tests/engine/minigames/playbook.test.ts` (acrescentar `describe('ações')`)

**Interfaces:**
- Produces:
  ```ts
  export function pass(s: PlaybookState, to: number, rng: Rng, input: PlaybookInput, risky = false): PlaybookState
  export function feint(s: PlaybookState, rng: Rng, input: PlaybookInput): PlaybookState        // drible: abre 0.8×mods.feint m por 0.6s; 6%/mods.stripResist de perder
  export function callScreen(s: PlaybookState): PlaybookState                                    // companheiro mais perto vem bloquear (rota nova de 1 ponto com screen)
  export function pumpFake(s: PlaybookState, rng: Rng, input: PlaybookInput): PlaybookState      // marcador a < 1.2m: pumpUntil = t+0.7 (defensor "no ar": não conta na abertura); 15% × difficulty de falta → phase 'done' com mgFreeThrow (quality vem da UI, ver Task 9)
  export function shoot(s: PlaybookState, finish?: 'layup' | 'dunk' | 'floater'): PlaybookState   // como v1 + fator do tipo + bônus de criação; se abertura < 0.4 → phase 'rebound' (guarda firstShotOpenness)
  export function reboundTap(s: PlaybookState, hit: 'perfect' | 'hit' | 'miss'): PlaybookState   // acerto → segunda posse (run, clock 5, sem rotas); erro → shooting com quality = firstShotOpenness
  export function moveTo(s: PlaybookState, x: number, y: number): PlaybookState                  // portador ao vivo (v1)
  export interface FoulResult { foul: true }   // marcador em `s.result = { optionId: 'mgFreeThrow', quality: -1 }` sinaliza que a UI roda FreeThrows e substitui quality
  ```
- Regras: passe de risco: abertura do destino +0.3 (aplicada em `shoot` via `riskyBonusUntil`), interceptação ×2. Interceptação (v1: defensor a < 0.9m da linha, `laneRisk × (1 − passing/140)` → aqui `laneRisk × 0.45 / mods.laneSafety`, ×2 se risky). Carga: `moveTo`/rota que leva o portador a < 0.5m de um defensor parado há > 0.6s (`still[d] ≥ 0.6`) dentro do garrafão → `turnover 'charge'`, `mgLayup` turnover. `quality` no `shoot`: `openness × typeFactor(layup 1, mid 0.95, three 0.9) × (clock < 2 ? 0.85 : 1) + (passes ≥ 2 || screenedRecently ? 0.1 : 0)` cap 1 (spec §2). Segunda posse: `shoot` normal; rebote só uma vez.

- [ ] **Step 1: Testes**

```ts
describe('ações', () => {
  const live = (seed: number, tpl: Parameters<typeof applyTemplate>[1] = 'horns') => { let s = applyTemplate(createPlaybook(createRng(seed), input()), tpl); s = { ...s, scheme: 'man' }; return startRun(s) }
  test('passe limpo chega; passe com defensor na linha pode ser interceptado; passe de risco intercepta mais', () => {
    let clean = 0, risky = 0
    for (let seed = 0; seed < 150; seed++) {
      let s = live(seed); const rng = createRng(seed)
      // coloca um defensor no meio da linha 0→2
      const mid = { x: (s.attackers[0].x + s.attackers[2].x) / 2, y: (s.attackers[0].y + s.attackers[2].y) / 2 }
      s = { ...s, defenders: s.defenders.map((d, i) => i === 1 ? { ...d, ...mid } : d) }
      if (pass(s, 2, rng, input()).turnover === 'intercept') clean++
      if (pass(s, 2, createRng(seed), input(), true).turnover === 'intercept') risky++
    }
    expect(clean).toBeGreaterThan(0); expect(risky).toBeGreaterThan(clean)
    let far = live(1); far = { ...far, defenders: far.defenders.map(d => ({ ...d, x: 0.5, y: 13.5 })) }
    expect(pass(far, 2, createRng(1), input()).turnover).toBeNull()
  })
  test('finta abre espaço temporário e pode perder a bola', () => {
    let s = live(2); const d0 = openness(s, 0); s = feint(s, createRng(2), input())
    expect(s.feintUntil).toBeGreaterThan(s.t); expect(openness(s, 0)).toBeGreaterThanOrEqual(d0)
    let lost = 0; for (let seed = 0; seed < 300; seed++) if (feint(live(3), createRng(seed), input()).turnover === 'strip') lost++
    expect(lost).toBeGreaterThan(0); expect(lost).toBeLessThan(60)
  })
  test('pedir bloqueio cria rota com screen no companheiro mais perto', () => {
    const s = callScreen(live(4)); const r = s.routes.findIndex((r, i) => i !== 0 && r.screen && r.points.length > 0)
    expect(r).toBeGreaterThan(0)
  })
  test('finta de arremesso com marcador perto: pumpUntil sobe; às vezes puxa falta (mgFreeThrow)', () => {
    let fouls = 0, pumps = 0
    for (let seed = 0; seed < 200; seed++) { let s = live(seed, 'iso'); s = { ...s, defenders: s.defenders.map(d => d.man === 0 ? { ...d, x: s.attackers[0].x + 0.5, y: s.attackers[0].y } : d) }; s = pumpFake(s, createRng(seed), input()); if (s.phase === 'done' && s.result?.optionId === 'mgFreeThrow') fouls++; else if (s.pumpUntil > s.t) pumps++ }
    expect(fouls).toBeGreaterThan(5); expect(pumps).toBeGreaterThan(100)
  })
  test('shoot: aberto = shooting com quality alta; fechado (< 0.4) = rebound; rebote acertado dá 2ª posse', () => {
    let open = live(5, 'iso'); open = { ...open, defenders: open.defenders.map(d => ({ ...d, x: 0.5, y: 13.5 })) }
    const o = shoot(open); expect(o.phase).toBe('shooting'); expect(o.result!.quality).toBeGreaterThan(0.8)
    let tight = live(6, 'iso'); tight = { ...tight, defenders: tight.defenders.map(d => ({ ...d, x: tight.attackers[0].x + 0.4, y: tight.attackers[0].y })) }
    let r = shoot(tight); expect(r.phase).toBe('rebound'); expect(r.firstShotOpenness).toBeLessThan(0.4)
    const again = reboundTap(r, 'hit'); expect(again.phase).toBe('run'); expect(again.clock).toBe(5)
    const miss = reboundTap(r, 'miss'); expect(miss.phase).toBe('shooting'); expect(miss.result!.quality).toBeCloseTo(r.firstShotOpenness!, 5)
  })
  test('bônus de criação: 2 passes antes do arremesso somam 0.1', () => {
    let s = live(7); s = { ...s, defenders: s.defenders.map(d => ({ ...d, x: 0.5, y: 13.5 })) }
    const solo = shoot(s).result!.quality
    let p = { ...s, passes: 2 }
    expect(shoot(p).result!.quality).toBeCloseTo(Math.min(1, solo + 0.1), 5)
  })
  test('carga: infiltrar em defensor parado no garrafão = turnover charge', () => {
    let s = live(8, 'iso'); s = { ...s, defenders: s.defenders.map((d, i) => i === 0 ? { ...d, x: COURT.basket.x, y: COURT.basket.y + 2.0, target: { x: COURT.basket.x, y: COURT.basket.y + 2.0 } } : d) }
    for (let i = 0; i < 20; i++) s = step(s, 0.05, createRng(8), input())   // defensor parado > 0.6s
    s = moveTo(s, COURT.basket.x, COURT.basket.y + 2.0)
    for (let i = 0; i < 60 && s.phase === 'run'; i++) s = step(s, 0.05, createRng(8), input())
    expect(s.turnover).toBe('charge'); expect(s.result?.turnover).toBe(true)
  })
})
```
(importar `pass, feint, callScreen, pumpFake, shoot, reboundTap, moveTo, openness` no topo do arquivo.)

- [ ] **Step 2: Rodar e ver falhar.**

- [ ] **Step 3: Implementar as ações** conforme as regras acima; `still[d]` = contador por defensor de tempo parado (alvo a < 0.05m), atualizado no `step`. `shoot` reaproveita `shotOptionFor` v1 e o cálculo de abertura (`openness`, ignorando defensores com `pumpUntil > t` presos no ar e somando `+0.3` se `riskyBonusUntil > t`).

- [ ] **Step 4: Rodar e ver passar** — todos os testes de `playbook.test.ts`.

- [ ] **Step 5: Commit**

```bash
git add src/engine/minigames/playbook.ts tests/engine/minigames/playbook.test.ts
git commit -m "feat(arcade): jogada v2 — passe de risco, finta, bloqueio, finta de arremesso, rebote, carga"
```

---

### Task 8: `TimingBar` e `useSwipe`

**Files:**
- Create: `src/ui/minigames/TimingBar.tsx`, `src/ui/minigames/useSwipe.ts`
- Test: `tests/ui/useSwipe.test.ts` (só a função pura `classifySwipe`)

**Interfaces:**
- Produces:
  ```ts
  // TimingBar.tsx
  export function TimingBar(props: { periodMs: number; window: { center: number; half: number }; vertical?: boolean; running: boolean; onTap(hit: 'perfect' | 'hit' | 'miss', t: number): void; label: string }): JSX.Element
  //  cursor varre 0→1→0 (triângulo) a cada periodMs via rAF/performance.now(); toque/clique/espaço chama onTap(timingHit(t, center, half), t)
  // useSwipe.ts
  export type Gesture = 'left' | 'right' | 'up' | 'tap'
  export function classifySwipe(dx: number, dy: number, dtMs: number): Gesture | null   // |d| < 12px e dtMs < 300 = tap; |dx| ≥ 28 e |dx| > |dy| → left/right; dy ≤ −28 → up; senão null
  export function useSwipe(ref: RefObject<HTMLElement | null>, onGesture: (g: Gesture) => void, enabled: boolean): void
  //  pointerdown/pointerup no elemento (pointer capture, touch-action none via CSS); teclado: ArrowLeft/Right/Up, Space/Enter = tap
  ```

- [ ] **Step 1: Teste**

```ts
// tests/ui/useSwipe.test.ts
import { describe, expect, test } from 'vitest'
import { classifySwipe } from '../../src/ui/minigames/useSwipe'
describe('classifySwipe', () => {
  test('tap curto e parado', () => { expect(classifySwipe(3, -4, 120)).toBe('tap') })
  test('esquerda/direita pelo eixo dominante', () => { expect(classifySwipe(-40, 5, 200)).toBe('left'); expect(classifySwipe(60, -20, 200)).toBe('right') })
  test('cima', () => { expect(classifySwipe(4, -50, 200)).toBe('up') })
  test('baixo ou pequeno lento = null', () => { expect(classifySwipe(0, 50, 200)).toBeNull(); expect(classifySwipe(5, 5, 800)).toBeNull() })
})
```

- [ ] **Step 2: Rodar e ver falhar.** — `npx vitest run --project unit tests/ui/useSwipe.test.ts`

- [ ] **Step 3: Implementar**

```ts
// useSwipe.ts
import { useEffect, type RefObject } from 'react'
export type Gesture = 'left' | 'right' | 'up' | 'tap'
export function classifySwipe(dx: number, dy: number, dtMs: number): Gesture | null {
  const ax = Math.abs(dx), ay = Math.abs(dy)
  if (ax < 12 && ay < 12) return dtMs < 300 ? 'tap' : null
  if (ax >= 28 && ax > ay) return dx < 0 ? 'left' : 'right'
  if (dy <= -28) return 'up'
  return null
}
export function useSwipe(ref: RefObject<HTMLElement | null>, onGesture: (g: Gesture) => void, enabled: boolean): void {
  useEffect(() => {
    const el = ref.current; if (!el || !enabled) return
    let start: { x: number; y: number; t: number } | null = null
    const down = (e: PointerEvent) => { el.setPointerCapture(e.pointerId); start = { x: e.clientX, y: e.clientY, t: performance.now() } }
    const up = (e: PointerEvent) => { if (!start) return; const g = classifySwipe(e.clientX - start.x, e.clientY - start.y, performance.now() - start.t); start = null; if (g) onGesture(g) }
    const key = (e: KeyboardEvent) => { const g = e.key === 'ArrowLeft' ? 'left' : e.key === 'ArrowRight' ? 'right' : e.key === 'ArrowUp' ? 'up' : e.key === ' ' || e.key === 'Enter' ? 'tap' : null; if (g) { e.preventDefault(); onGesture(g) } }
    el.addEventListener('pointerdown', down); el.addEventListener('pointerup', up); window.addEventListener('keydown', key)
    return () => { el.removeEventListener('pointerdown', down); el.removeEventListener('pointerup', up); window.removeEventListener('keydown', key) }
  }, [ref, onGesture, enabled])
}
```
`TimingBar.tsx`: `useEffect` com rAF lendo `performance.now()`; `t = tri((now − start) / periodMs)` onde `tri(u) = 1 − |2(u mod 1) − 1|`; render: trilha `.mg-timing` (horizontal ou `--vertical`) com janela âmbar (`left/top = (center − half) × 100%`, tamanho `2 × half × 100%`) e cursor tinta; `onPointerDown`/`Space` → `onTap(timingHit(t, center, half), t)`. Estilos em `src/styles/minigames.css` (`.mg-timing`, `.mg-timing__win`, `.mg-timing__cur`, `.mg-timing--vertical`). Importar `timingHit` de `../../engine/minigames/common`.

- [ ] **Step 4: Rodar e ver passar; typecheck.**

- [ ] **Step 5: Commit**

```bash
git add src/ui/minigames/TimingBar.tsx src/ui/minigames/useSwipe.ts src/styles/minigames.css tests/ui/useSwipe.test.ts
git commit -m "feat(arcade): TimingBar e gestos de swipe reutilizaveis"
```

---

### Task 9: ARREMESSO 3D (`Shot.tsx`) + lances livres + i18n

**Files:**
- Rewrite: `src/ui/minigames/Shot.tsx`; Create: `src/ui/minigames/FreeThrows.tsx`
- Modify: `src/styles/mg-shot.css`, `src/data/i18n/pt.json`, `src/data/i18n/en.json`
- Reference: `docs/superpowers/assets/arcade-v2/ref-arremesso.jpg` (composição), protótipo (câmera: `position (SX+3.6, 2.15, SZ−3.4)`, `lookAt (0.3, 2.5, HZ−1.0)` com `SX=1.6`, `SZ=HZ−8`)

**Interfaces:**
- Consumes: Task 1 (`buildArena`, `buildHuman`, `buildBall`, `useThreeScene`, `hasWebGL`), Task 5 (`SHOTS`, `availableTypes`, `createCloseout`, `stepCloseout`, `evaluate`, `resultFor`, `jumpTiming`, `trajectory`, `meterValue`, `angleValue`, `scenarioFor`), Task 2 (`attrMods`, `opponentFive`, `bestDefender`, `difficulty`, `opponentStar`), Task 8 (`TimingBar`).
- `MinigameProps` (existente) precisa de `league: LeagueState` e `quarter: number` — **adicionar os dois campos** em `src/ui/minigames/types.ts` e passá-los em `Minigame.tsx` (`state.league!`, `Math.min(4, Math.floor(moment.at / 12) + 1)`).
- Produces: `ShotGame(props: MinigameProps)`; `FreeThrows(props: { lang; build; age; onDone(q: [number, number]): void })` (mesma mira arrasto/medidor sem closeout, 2 arremessos, devolve as duas qualities).

Fluxo do componente (state machine local): `pick` → `aim` (closeout corre; arrasto ou medidor) → `jump` (TimingBar vertical, `periodMs 900`, janela `{center: 0.5, half: mods.jumpWindow}`) → `flight` (dispatch `onResolve` no início; bola voa 0.9s pela `trajectory`; últimos 20% obedecem `outcome`; toco = bola bate na mão do defensor) → overlay `.mg-result`.
Cena: `scene.background/fog` conforme Task 1; `buildArena({ teamShort })`; `you = buildHuman({ jersey: RED, shorts: RED, number, name: lastName, you: true })` em `(1.6, 0, HZ−8)` pose `stand` → `shoot` (k por frame, 0.25s) ao entrar em `jump`; `def = buildHuman({ jersey: INK, shorts: INK, number: null, name: defender.short })` que anda de `x0` até `0.5` seguindo `closeout.x` (posição `(0.7, 0, HZ − 6.3 − (closeout.x − 0.5))`) e muda pra `closeout` quando `handUp`. HUD (DOM sobre o canvas): tipo/distância, relógio do lance (`moment.clock` em segundos, corre em tempo real desde `aim`; zerou = `onResolve({ optionId: SHOTS[type].optionId, quality: 0, turnover: true })`), FORA/CASA (`scenarioFor`), ÂNGULO/FORÇA, barra CLOSEOUT, tremor: em arrasto/medidor fora de casa, `angle += sin(t×9) × 0.6° × tremor × mods.tremor`. Pressão da torcida = `noise = 0.6 × teamStrength/85 + 0.4 × (bigMarket ? 1 : 0)` do time adversário (`teamById`).
Mobile: canvas `width: 100%; aspect-ratio: 3/2`; botões ≥ 44px. Modo arrasto/medidor lembrado em `localStorage['thegoat:shotMode']` (v1). Sem WebGL (`!hasWebGL()`): renderiza `ShotFallback` (Task 12).

- [ ] **Step 1: i18n** — acrescentar PT/EN (paridade): `mg.shot.type.floater/stepback/fadeaway/bank` ("FLOATER"/"STEP-BACK"/"FADEAWAY"/"TABELA" · EN "FLOATER"/"STEP-BACK"/"FADEAWAY"/"BANK"), `mg.shot.cost.<type>` (8 chaves curtas: ex. `mg.shot.cost.three` "Longe, vale mais, erra mais" / "Deep, worth more, misses more"), `mg.shot.phase.pick/aim/jump/flight` ("ESCOLHA"/"MIRA"/"SALTO"/"VOO"), `mg.shot.jumpHint` ("Toque no ápice do salto" / "Tap at the top of the jump"), `mg.shot.blocked` ("TOCO NA MÃO" / "BLOCKED"), `mg.shot.contested` ("CONTESTADO" / "CONTESTED"), `mg.shot.clockOut` ("ESTOUROU O RELÓGIO" / "SHOT CLOCK"), `mg.ft.title` ("LANCES LIVRES" / "FREE THROWS"), `mg.ft.n` ("Lance {n} de 2" / "Free throw {n} of 2"), `mg.noWebgl` ("Seu navegador não tem WebGL: versão simplificada." / "No WebGL in this browser: simplified version."), `mg.away.noise` ("O ginásio vaia — a mira treme" / "The crowd boos — your aim shakes").

- [ ] **Step 2: Implementar `Shot.tsx` e `FreeThrows.tsx`** conforme o fluxo acima. Guardas obrigatórios: `onResolve` em ref e chamado UMA vez; `useThreeScene` deps `[seed]`; dispose de rigs/arena no cleanup; nenhum `Math.random` (usar `createRng(seed)` local para `scenarioFor`/`createCloseout`).

- [ ] **Step 3: Typecheck + lint** — `npx tsc -p tsconfig.app.json --noEmit && npx oxlint src/ui/minigames`.

- [ ] **Step 4: Sanity manual** — `npm run dev`, carreira em MODO ARCADE, primeiro jogo, "Simular" até chegar num clutch (ou usar a tecla de skip do feed), conferir: escolha → closeout andando → mira → salto → voo → resultado; comparar com `ref-arremesso.jpg`.

- [ ] **Step 5: Commit**

```bash
git add src/ui/minigames/Shot.tsx src/ui/minigames/FreeThrows.tsx src/ui/minigames/types.ts src/ui/minigames/Minigame.tsx src/styles/mg-shot.css src/data/i18n
git commit -m "feat(arcade): arremesso 3D — repertorio, closeout, mira+salto, lances livres"
```

---

### Task 10: MURALHA 3D (`Defense.tsx`) + i18n

**Files:**
- Rewrite: `src/ui/minigames/Defense.tsx`; Modify: `src/styles/mg-defense.css`, i18n
- Reference: `ref-muralha.jpg`; protótipo modo `wall` (câmera `(0.9, 2.5, HZ+0.7)` → `lookAt (0.5, 1.05, HZ+8.5)` com `HZ = −COURT_HZ`, `aimLights(arena, −1)`)

**Interfaces:**
- Consumes: Task 4 (`createDuel`, `step`, `slide`, `trySteal`, `jump`, `inFront`), Task 2 (`opponentStar`, `tendencies`, `difficulty`, `attrMods`), Task 8 (`useSwipe`), Task 1.
- Produces: `DefenseGame(props: MinigameProps)`.

Fluxo: `card` (1.2s: cartão de tendências grande + "ELE VEM") → `live` (loop 20Hz do engine em `setInterval`, render 3D por frame interpolando posições: você `x = defX`, atacante `x = attX`, `z` por `attDist`; poses: atacante `dribble`/`crossover` (troca pelo `move.kind`), `jump` no `shoot`/`pumpFake`; você `defend`, `jump` quando `airborne > 0`; bola no lado da mão, "exposta" = bola desce entre as pernas e brilha âmbar) → `end` (animação por `phase`: `steal` = bola vai pra sua mão e você corre pro outro lado 1.2s; `block` = bola cai; `shot` = bola voa pra cesta e obedece `outcome`; `drive` = ele passa e enterra/bandeja; `foul` = apito + 2 lances livres dele animados com `freeThrows`) → overlay `.mg-result`.
HUD: cartão (nome · pos · ovr, ARREMESSA/INFILTRA/PASSA em %, ÚLTIMAS = `log` como setas), barra CONTENÇÃO (`contain/live`), POSSE (`clock`), dica de gestos (3 células como na prancha aprovada). Gestos via `useSwipe` no canvas: left/right → `slide`, tap → `trySteal`, up → `jump`. Desktop: setas + espaço.

- [ ] **Step 1: i18n** — `mg.def.card.title` ("TENDÊNCIAS"/"TENDENCIES"), `mg.def.card.shoot/drive/pass` ("ARREMESSA"/"INFILTRA {side}"/"PASSA" · EN "SHOOTS"/"DRIVES {side}"/"PASSES"), `mg.def.side.left/right` ("ESQ."/"DIR." · "LEFT"/"RIGHT"), `mg.def.last` ("ÚLTIMAS"/"LAST"), `mg.def.contain` ("CONTENÇÃO"/"CONTAIN"), `mg.def.gest.slide/tap/up` ("deslizar = acompanhar"/"toque = roubo"/"pra cima = toco" · EN "swipe = shadow"/"tap = steal"/"up = block"), `mg.def.end.steal/block/drive/foul/clock/pass/shot` ("ROUBOU!"/"TOCO!"/"PASSOU POR VOCÊ"/"FALTA SUA"/"RELÓGIO"/"ELE PASSOU A BOLA"/"ELE ARREMESSOU" · EN "STEAL!"/"BLOCK!"/"BLOWN BY"/"YOUR FOUL"/"CLOCK"/"HE PASSED"/"HE SHOT"), `mg.def.ft` ("Lances livres dele: {made}/2" / "His free throws: {made}/2"), `mg.def.bitFake` ("CAIU NA FINTA"/"BIT THE FAKE"). Remover chaves v1 que ficarem órfãs (`mg.def.beat.*`, `mg.def.hit/miss/late/stealHit/stealMiss/beats`) — o teste de paridade só exige igualdade PT/EN.

- [ ] **Step 2: Implementar** conforme o fluxo; `onResolve` em ref, uma vez, chamado ao entrar em `end` com `state.result`.

- [ ] **Step 3: Typecheck + lint + sanity manual** (primeiro momento defensivo de um jogo arcade; jogar com setas e espaço).

- [ ] **Step 4: Commit**

```bash
git add src/ui/minigames/Defense.tsx src/styles/mg-defense.css src/data/i18n
git commit -m "feat(arcade): muralha 3D — duelo 1x1 por swipes com cartao de tendencias"
```

---

### Task 11: JOGADA SVG v2 (`Playbook.tsx`) + i18n

**Files:**
- Rewrite: `src/ui/minigames/Playbook.tsx`; Modify: `src/styles/mg-playbook.css`, i18n
- Reference: canvas página 1, "Direção 2 · Plano refinado" (ímãs com sombra `feDropShadow`, madeira `#DDD0B3/#D3C4A3`, rota tracejada vermelha, manchete)

**Interfaces:**
- Consumes: Task 6/7 (tudo de `playbook.ts`), Task 2, Task 8 (`TimingBar`).
- Produces: `PlaybookGame(props: MinigameProps)`.

Fluxo: `read` (2s: os 5 deles entram com nome curto; texto "LENDO A DEFESA…") → `draw` (relógio parado; arrastar ímã vermelho = `setRoute(idx, pontos)` amostrando o pointer a cada 0.6m; toque curto num ímã com rota = `toggleScreen`; botões MODELOS (6 chips → `applyTemplate`), LIMPAR, DESFAZER, RODAR → `startRun`) → `run` (loop 20Hz; barra de ações PASSE (toque no companheiro) · FINTA · BLOQUEIO · FINTA ARR. · PASSE RISCO (toggle que arma o próximo passe) · ARREMESSAR (BANDEJA/FLOATER/ENTERRAR perto do aro)) → `rebound` (TimingBar horizontal, `periodMs 1100`, janela `{center 0.5, half: mods.boxWindow}` → `reboundTap`) → `shooting`/`turnover`/`done` → `onResolve`. Caso `result.optionId === 'mgFreeThrow'` (falta puxada): montar `FreeThrows` (Task 9) e só então `onResolve({ optionId: 'mgFreeThrow', quality: freeThrowQuality(q) })`.
Manchete: `.mg-headline` (Archivo Black 40px vermelho, sombra de papel) 0.8s em: bloqueio que prende, roubo/interceptação, arremesso, esquema revelado no fim ("ERA ZONA" via `SCHEME_SIGNAL`).

- [ ] **Step 1: i18n** — `mg.pb.phase.read/draw/run/rebound` ("LENDO A DEFESA"/"DESENHAR"/"RODAR"/"REBOTE"), `mg.pb.template.<6>` ("PICK&ROLL"/"HORNS"/"CORTA-LUZ DUPLO"/"ISOLAMENTO"/"5-OUT"/"TRANSIÇÃO"), `mg.pb.clear/undo/run` ("LIMPAR"/"DESFAZER"/"RODAR"), `mg.pb.act.feint/screen/pump/risky` ("FINTA"/"BLOQUEIO"/"FINTA ARR."/"PASSE RISCO"), `mg.pb.scheme.man/zone/switch/trap/press` ("ERA HOMEM A HOMEM"/"ERA ZONA 2-3"/"ERA TROCA-TUDO"/"ERA ARMADILHA"/"ERA PRESSÃO"), `mg.pb.head.screen/steal/intercept/charge/foul/rebound` ("BLOQUEIO!"/"DESARME"/"INTERCEPTADO"/"CARGA"/"FALTA!"/"REBOTE!"), `mg.pb.turnover.charge` ("FALTA DE ATAQUE"), `mg.pb.drawHint` ("Arraste os ímãs vermelhos pra desenhar a jogada · toque num ímã = bloqueio no fim") e EN.

- [ ] **Step 2: Implementar.** SVG `viewBox 0 0 1524 1400`, ímãs `r=56` (nós) / `52` (eles), hit-area `r=90`, rota `stroke-width 12 dasharray 30 20`, marca de bloqueio (traço de 80 unidades perpendicular à rota no fim), nomes em mono 30px. Loop de `step` em `setInterval(50)` com estado em ref (padrão do v1).

- [ ] **Step 3: Typecheck + lint + sanity manual** (desenhar 2 rotas, rodar, passar, arremessar; forçar rebote arremessando marcado).

- [ ] **Step 4: Commit**

```bash
git add src/ui/minigames/Playbook.tsx src/styles/mg-playbook.css src/data/i18n
git commit -m "feat(arcade): jogada v2 — prancheta com rotas desenhadas, modelos, acoes ao vivo e rebote"
```

---

### Task 12: Fallbacks sem WebGL, moldura de fase e roteamento

**Files:**
- Create: `src/ui/minigames/ShotFallback.tsx`, `src/ui/minigames/DefenseFallback.tsx`
- Modify: `src/ui/minigames/Minigame.tsx`, `src/styles/minigames.css`

**Interfaces:**
- `ShotFallback`/`DefenseFallback` têm a MESMA assinatura `MinigameProps` e a mesma mecânica (engine v2), renderizando em SVG 2D: arremesso = vista lateral simples (chão, aro, arco previsto, defensor como silhueta com linha da mão em `closeout.x`); muralha = vista de cima (você e ele como ímãs, `attX/defX` no eixo x, cone de contenção desenhado).
- `Minigame.tsx`: `const Comp = kind === 'shot' ? (hasWebGL() ? ShotGame : ShotFallback) : kind === 'defense' ? (hasWebGL() ? DefenseGame : DefenseFallback) : PlaybookGame`; cabeçalho ganha o adversário (`opponentStar(state.league!, pending.context.opponentTeamId)`: `short · pos · ovr`) e, na muralha, o cartão vem do próprio componente. Passar `league` e `quarter` (Task 9).

- [ ] **Step 1: Implementar os fallbacks** (reaproveitar o HUD e a máquina de estados dos componentes 3D, trocando só o miolo visual; sem duplicar a lógica de fluxo: extrair `useShotFlow(props)` de `Shot.tsx` e `useDuelFlow(props)` de `Defense.tsx` que devolvem estado+ações, e os dois renderizadores (3D e SVG) consomem o mesmo hook).

- [ ] **Step 2: Typecheck; teste manual forçando fallback** (no DevTools: `WebGLRenderingContext = undefined` antes de carregar, ou `hasWebGL` retornar false via `localStorage['thegoat:noWebgl'] = '1'` — adicionar essa chave de override em `hasWebGL()` para teste).

- [ ] **Step 3: Commit**

```bash
git add src/ui/minigames
git commit -m "feat(arcade): fallback 2D sem WebGL e cabecalho com adversario real"
```

---

### Task 13: e2e, guias, HANDOFF, review final

**Files:**
- Modify: `tests/e2e-playthrough.mjs` (bloco arcade), `guia-modos.html`, `en/game-modes.html`, `HANDOFF.md`

- [ ] **Step 1: e2e** — no bloco arcade existente: após abrir o primeiro minigame, se `.mg-defense` (muralha): pressionar `ArrowLeft`/`ArrowRight` alternando a cada 300ms por 9s e esperar `.mg-result`; se `.mg-pb`: clicar `RODAR` (botão com texto), esperar 1s, clicar `ARREMESSAR` (ou `BANDEJA`), esperar `.mg-result`; se `.mg-shot`: clicar o primeiro tipo, clicar `MEDIDOR`, `Space` ×2, `Space` (salto), esperar `.mg-result`. Manter o assert de "Simular o resto do jogo" fecha o jogo. Rodar `node tests/e2e-playthrough.mjs` → `PLAYTHROUGH OK`.

- [ ] **Step 2: Guias** — em `guia-modos.html`/`en/game-modes.html`, atualizar o parágrafo do Modo Arcade: quadro tático com rotas desenhadas contra 5 esquemas; arremesso em 3D com closeout e salto; muralha como duelo 1x1 por gestos com cartão de tendências.

- [ ] **Step 3: HANDOFF** — decisão 24: resumo do ciclo (spec, estilo aprovado na canvas, `three` como dependência, contrato de RNG intocado, fallback sem WebGL, arquivos novos, testes, débitos: bonecos low-poly são primitivas — sem modelos externos; performance no celular medida em X ms/frame; calibração não re-rodada por construção).

- [ ] **Step 4: Suite completa + typecheck** — `npm test && npx tsc -p tsconfig.app.json --noEmit` verdes.

- [ ] **Step 5: Commit**

```bash
git add tests/e2e-playthrough.mjs guia-modos.html en/game-modes.html HANDOFF.md
git commit -m "docs(arcade): e2e v2, guias e HANDOFF do ciclo arcade v2"
```

---

## Self-review

- **Cobertura da spec**: §1 (common) → Task 2; §2 (JOGADA) → 6, 7, 11; §3 (ARREMESSO) → 5, 9; §4 (MURALHA) → 4, 10; §5 (tela: fase, adversário, swipes) → 8, 9, 10, 12; §8 (estilo: 3D, arena, rig, prancheta 2D, fallback) → 1, 9, 10, 11, 12; `mgFreeThrow` → 3; lances livres do NPC → 4 (`freeThrows`); e2e/guias/HANDOFF → 13. Lacuna conhecida e aceita: "placar vivo no fundo" do arremesso (§3.4) fica como o relógio de posse aceso + faixa de LED (já na arena), sem placar suspenso — YAGNI.
- **Placeholders**: nenhum "TBD"; Tasks 9–12 descrevem fluxo, cena, HUD e chaves i18n em vez de código integral de React (a UI segue o padrão dos componentes v1 existentes no repo e o protótipo).
- **Tipos**: `MinigameResult`/`MomentExec` já existem; `AttrMods.tol` usa as chaves `layup|floater|mid|three|dunk` em `SHOTS[type].tolKey` (bank/stepback/fadeaway mapeiam para `mid`/`three`); `OppPlayer.speed/reach` consumidos por `createCloseout`; `Tendencies` consumida por `createDuel`; `MinigameProps` ganha `league` e `quarter` na Task 9 e é usada nas Tasks 10–12.
