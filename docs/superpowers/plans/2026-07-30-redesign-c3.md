# Redesign C3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerimônia própria (3b), aposentadoria retemada (3c), veredito persistido em camadas (3d), corrida de MVP projetada no `seasonAdvance`, e fechamento de todos os débitos reais do HANDOFF — ciclo final do redesign.

**Architecture:** Sem fase nova, sem bump de save. Cerimônia vira sub-view local (`useState`) dentro de `seasonResult`. Veredito ganha um campo persistido (`GameState.verdict`) computado uma vez nos dois pontos que hoje entram em `phase: 'verdict'`; `loadState` faz backfill pra saves antigos. Dois pequenos helpers determinísticos novos em `engine/league.ts` (sem rng) alimentam o `partialMvpRace` já existente (achado morto, nunca chamado) pra dar ao `SeasonAdvance` uma corrida de MVP projetada de verdade, em vez da linha de stats atual. O resto é retheme (CSS/JSX) das três telas e limpeza pontual (débitos).

**Tech Stack:** React 18 + TS + Vite, CSS puro (tokens/primitivas do tema 2B), vitest (engine/state — testa UI só via e2e, convenção do projeto), Playwright manual (e2e).

**Spec:** `docs/superpowers/specs/2026-07-30-redesign-c3-design.md`

## Global Constraints

- `src/engine/` é TS puro: sem React, sem localStorage, sem `Math.random` — toda aleatoriedade via `Rng` injetado. Os dois helpers novos deste ciclo (`projectedNpcLines`, `projectedStandings`) não recebem `Rng` — são determinísticos por design, zero consumo de rng.
- Toda string visível passa por i18n (`src/data/i18n/pt.json` + `en.json`, paridade de chaves testada por `tests/i18n.test.ts`).
- Fórmulas-contrato intocadas: `computeWinPct`/`computeTitleProb`/pesos de `verdict.ts`/GOAT gate. Este ciclo não altera nenhuma delas.
- Sem router, sem store externa, sem lib de UI. Sem fotos/logos reais.
- Sem bump de save — `thegoat:v5` continua valendo. Campos novos/removidos em `GameState` são compatíveis via backfill (`verdict`) ou remoção segura de campo nunca lido (`pendingChoices`).
- Vitest só cobre engine/state/data (convenção do projeto, `CLAUDE.md`); tarefas de UI pura são verificadas por typecheck + e2e/manual, sem teste vitest de componente.
- Antes de cada commit: `rtk npm test` + `npx tsc -p tsconfig.app.json --noEmit`.

---

### Task 1: `GameState.verdict` persistido — computado uma vez, com backfill

**Files:**
- Modify: `src/state.ts` (interface `GameState` ~linha 81-114, `initialState` ~linha 163-198, `case 'ADVANCE'` ~linha 734-762, `case 'RETIRE_DECISION'` ~linha 764-769, `loadState` ~linha 805-836)
- Test: `tests/state.test.ts`

**Interfaces:**
- Consumes: `computeVerdict(career: Career): Verdict` de `./engine/verdict` (já existe, pura).
- Produces: `GameState.verdict: Verdict | null` — presente e não-nulo sempre que `phase === 'verdict'` (ou `resumePhase === 'verdict'` num save carregado).

- [ ] **Step 1: Failing test — as duas rotas de entrada em `verdict` guardam o cálculo**

```ts
// em tests/state.test.ts, dentro de describe('gameReducer', ...) — usar os helpers
// playToBuild/skipGames já definidos no topo do arquivo
test('RETIRE_DECISION e aposentadoria por idade computam e guardam verdict uma vez', () => {
  let s = playToBuild()
  s = gameReducer(s, { type: 'CHOOSE_OFFER', offer: s.offers[0] })
  let guard = 0
  while (s.phase !== 'retireDecision' && guard++ < 200) {
    if (s.phase === 'preseason') s = gameReducer(s, { type: 'PLAY_SEASON', focus: 'health' })
    else if (s.phase === 'seasonResult') s = gameReducer(s, { type: 'ADVANCE' })
    else if (s.phase === 'tradeDecision') s = gameReducer(s, { type: 'TRADE_DECISION', accept: false })
    else if (s.phase === 'eventDecision') s = gameReducer(s, { type: 'EVENT_DECISION', choice: 'b' })
    else if (s.phase === 'seasonAdvance' || s.phase === 'keyGame' || s.phase === 'gameResult' || s.phase === 'playoffGame') s = skipGames(s)
    else if (s.phase === 'freeAgency') s = gameReducer(s, { type: 'CHOOSE_OFFER', offer: s.offers[0] })
    else break
  }
  expect(s.phase).toBe('retireDecision')
  expect(s.verdict).toBeNull()
  const retired = gameReducer(s, { type: 'RETIRE_DECISION', retire: true })
  expect(retired.phase).toBe('verdict')
  expect(retired.verdict).not.toBeNull()
  expect(retired.verdict!.score).toBeGreaterThanOrEqual(0)
})

test('save legado sem verdict em phase verdict recebe backfill no loadState', () => {
  let s = playToBuild()
  const legacy: Record<string, unknown> = { ...s, phase: 'verdict' }
  delete legacy.verdict
  localStorage.setItem('thegoat:v5', JSON.stringify(legacy))
  const loaded = loadState()!
  expect(loaded.verdict).not.toBeNull()
  expect(loaded.verdict!.tier).toBeTruthy()
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
rtk npx vitest run tests/state.test.ts -t "verdict"
```
Esperado: FAIL — `verdict` não existe em `GameState`.

- [ ] **Step 3: Import + campo na interface + `initialState`**

Em `src/state.ts`, adicionar ao bloco de imports de tipos (perto de `Award, AwardRace, Build, ...` na import de `./engine/types`, ~linha 20-25) **não** é necessário — `Verdict` fica isolado num import próprio de `./engine/verdict`:

```ts
import { computeVerdict } from './engine/verdict'
```

Na interface `GameState` (após `resumePhase: Phase | null`, linha 113):

```ts
  resumePhase: Phase | null
  verdict: Verdict | null
```

Import do tipo `Verdict` junto aos outros tipos de `./engine/types` (ele é exportado de lá, `computeVerdict` que mora em `./engine/verdict`):

```ts
import type {
  Award, AwardRace, Build, CalendarSlot, Career, DraftPick, EventChoice, Focus, GameEventId, Headline,
  IconicMomentId, KeyGame, LeagueSeasonOutcome, LeagueState, NpcLine, Offer, PendingGame, PlayoffRun, RaceAward,
  RegularSeasonResult, Rng, SeasonResult, SlotId, TeamProfile, TeamStanding, TickerGame, Verdict, WatchedGameContext,
  WatchedGameResult,
} from './engine/types'
```

Em `initialState` (após `resumePhase: null,`, linha 196):

```ts
    resumePhase: null,
    verdict: null,
```

- [ ] **Step 4: Computar nos dois pontos de entrada em `verdict`**

No `case 'ADVANCE'` (linha 750), trocar:

```ts
      if (age > 40) return { ...base, phase: 'verdict' }
```

por:

```ts
      if (age > 40) return { ...base, phase: 'verdict', verdict: computeVerdict(base.career) }
```

No `case 'RETIRE_DECISION'` (linha 764-769), trocar:

```ts
    case 'RETIRE_DECISION': {
      // Reachable from 'retireDecision' (continue/stop) and 'freeAgency' (stop only,
      // when a contract-expiry year lands at age 31+ — see FreeAgency screen).
      if (!action.retire && state.phase === 'freeAgency') return state
      return { ...state, phase: action.retire ? 'verdict' : 'preseason' }
    }
```

por:

```ts
    case 'RETIRE_DECISION': {
      // Reachable from 'retireDecision' (continue/stop) and 'freeAgency' (stop only,
      // when a contract-expiry year lands at age 31+ — see FreeAgency screen).
      if (!action.retire && state.phase === 'freeAgency') return state
      if (!action.retire) return { ...state, phase: 'preseason' }
      return { ...state, phase: 'verdict', verdict: computeVerdict(state.career) }
    }
```

- [ ] **Step 5: Backfill em `loadState`**

Em `src/state.ts`, dentro de `loadState` (após `parsed.resumePhase = parsed.resumePhase ?? null`, linha 831), adicionar:

```ts
    parsed.resumePhase = parsed.resumePhase ?? null
    const effectiveVerdictPhase = (parsed.resumePhase ?? parsed.phase) === 'verdict'
    parsed.verdict = parsed.verdict ?? (effectiveVerdictPhase ? computeVerdict(parsed.career) : null)
    return parsed as GameState
```

(substitui o `return parsed as GameState` que já existe logo depois — não duplicar a linha.)

- [ ] **Step 6: Rodar e ver passar**

```bash
rtk npx vitest run tests/state.test.ts -t "verdict"
```
Esperado: PASS.

- [ ] **Step 7: Suite completa + typecheck + commit**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
rtk git add src/state.ts tests/state.test.ts
rtk git commit -m "feat(c3): verdict computado uma vez e persistido no state (3d, parte 1)"
```

---

### Task 2: Remove `pendingChoices` (campo morto)

**Files:**
- Modify: `src/state.ts` (interface `GameState` linha 100, `initialState` linha 183, `startSeasonCalendar` linha 275, `closeRegularSeason`-equivalente linha 382, `loadState` linha 824)
- Test: `tests/state.test.ts` (nenhum teste novo — a suite existente cobre a ausência de regressão; ver Step 3)

**Interfaces:**
- Consumes: nada novo.
- Produces: `GameState` sem o campo `pendingChoices`. A variável local `choices: EventChoice[]` dentro de `startSeasonCalendar` continua existindo e sendo usada em `simRegularSeason({..., choices, ...})` — só o campo **persistido** morre.

- [ ] **Step 1: Remover o campo da interface**

Em `src/state.ts`, remover a linha (perto de `pendingEvents`, linha 100):

```ts
  pendingChoices: EventChoice[] | null  // resolved event choices, held across the keyGame pause
```

- [ ] **Step 2: Remover as 4 escritas do campo**

`initialState` (linha 183): remover `pendingChoices: null,`.

`startSeasonCalendar` (linha 273-276): trocar

```ts
  return advanceCalendar({
    ...state, calendar, keyGameResults: [],
    pendingRegular: regular, pendingFocus: focus, pendingEvents: events, pendingChoices: choices,
  }, rng, calls)
```

por (o parâmetro `choices` da função continua recebido e usado antes, em `simRegularSeason`; só some do objeto de estado):

```ts
  return advanceCalendar({
    ...state, calendar, keyGameResults: [],
    pendingRegular: regular, pendingFocus: focus, pendingEvents: events,
  }, rng, calls)
```

`closeRegularSeason`-equivalente (linha 382, base de reset pós-temporada): trocar

```ts
  const base = { ...state, calendar: null, lastGame: null, pendingEvents: null, pendingRegular: null, pendingFocus: null, pendingChoices: null }
```

por:

```ts
  const base = { ...state, calendar: null, lastGame: null, pendingEvents: null, pendingRegular: null, pendingFocus: null }
```

`loadState` (linha 824): remover a linha `parsed.pendingChoices = parsed.pendingChoices ?? null`.

- [ ] **Step 2b: Checar por sobras**

```bash
rtk grep -rn "pendingChoices" src/
```
Esperado: nenhum resultado (o import de `EventChoice` em `./engine/types` continua — ainda é o tipo do parâmetro local `choices` de `startSeasonCalendar`).

- [ ] **Step 3: Rodar suite inteira (regressão) + typecheck**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
```
Esperado: PASS/verde — nenhum teste existente lê `state.pendingChoices` (conferido: `tests/state.test.ts` linha ~166-196 usa `pendingRegular`/`injuryProne`/`pendingEvents`, não `pendingChoices`).

- [ ] **Step 4: Commit**

```bash
rtk git add src/state.ts
rtk git commit -m "chore(c3): remove pendingChoices — campo persistido nunca lido de volta"
```

---

### Task 3: Guardas de fase faltantes (`CONFIRM_BUILD`, `CHOOSE_OFFER`) + comentário de RNG + nota de snapshot

**Files:**
- Modify: `src/state.ts` (`case 'CONFIRM_BUILD'` linha 612-618, `case 'CHOOSE_OFFER'` linha 620-621, comentário perto de `continuePlayoffs` linha 480)
- Modify: `tests/state-calendar.test.ts` (comentário de topo)
- Test: `tests/state.test.ts`

**Interfaces:**
- Consumes: nada novo.
- Produces: nenhuma API nova — só endurece dois `case`s que hoje aceitam o dispatch de qualquer fase.

- [ ] **Step 1: Failing test — dispatch fora de fase é no-op**

```ts
// em tests/state.test.ts, dentro de describe('gameReducer', ...)
test('CONFIRM_BUILD e CHOOSE_OFFER são no-op fora da fase esperada', () => {
  const home = initialState('pt')
  expect(gameReducer(home, { type: 'CONFIRM_BUILD' })).toEqual(home)
  const fakeOffer = { teamId: 'lal', profile: 'bigmarket' as const }
  expect(gameReducer(home, { type: 'CHOOSE_OFFER', offer: fakeOffer })).toEqual(home)
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
rtk npx vitest run tests/state.test.ts -t "no-op fora da fase"
```
Esperado: FAIL — hoje os dois cases executam de qualquer fase (`CONFIRM_BUILD` computaria um build de `state.picks` vazio; `CHOOSE_OFFER` aceitaria a oferta fake e iria pra `preseason`).

- [ ] **Step 3: Guardas, no mesmo estilo já usado em todo o resto do reducer**

```ts
    case 'CONFIRM_BUILD': {
      if (state.phase !== 'draftDone') return state
      const build = resolveBuild(state.picks)
      const { rng, calls } = makeCountedRng(state.seed, state.rngCalls)
      const pickNumber = draftPickNumber(build.overall, rng)
      const offers = makeOffers(rng)
      return { ...state, build, pickNumber, offers, age: 19, phase: 'nbaDraft', rngCalls: calls() }
    }

    case 'CHOOSE_OFFER':
      if (state.phase !== 'nbaDraft' && state.phase !== 'freeAgency') return state
      return { ...state, currentOffer: action.offer, contractYearsLeft: 4, phase: 'preseason' }
```

- [ ] **Step 4: Rodar e ver passar**

```bash
rtk npx vitest run tests/state.test.ts -t "no-op fora da fase"
```
Esperado: PASS.

- [ ] **Step 5: Comentário de contrato RNG incompleto**

Em `src/state.ts`, achar o comentário (linha 480, imediatamente acima de `function continuePlayoffs`):

```ts
// Metade 2 — CONTINUE: consome o rng do avanço (roll de série, npcRound, próximo jogo).
```

Trocar por:

```ts
// Metade 2 — CONTINUE: consome o rng do avanço (roll de série, advancePlayer avança o
// bracket, npcRound resolve o resto do round, próximo jogo).
```

- [ ] **Step 6: Nota de snapshot em `tests/state-calendar.test.ts`**

No topo do arquivo (após os imports, antes da primeira `describe`/`function`), adicionar:

```ts
// Nota: os testes abaixo são ancorados na seed 42 e conferem o registro literal do
// walk (soma de trechos + key games) byte a byte. Isso é uma trava de SNAPSHOT
// (depende da ordem/contagem exata de chamadas de rng), não uma trava de
// COMPORTAMENTO — se um teste aqui quebrar depois de uma mudança em qualquer engine
// upstream (schedule/moments/season), o primeiro suspeito é drift de ordem de rng,
// não necessariamente uma regressão real. Ver HANDOFF.md § Débitos.
```

- [ ] **Step 7: Suite completa + typecheck + commit**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
rtk git add src/state.ts tests/state.test.ts tests/state-calendar.test.ts
rtk git commit -m "fix(c3): guarda de fase em CONFIRM_BUILD/CHOOSE_OFFER, comentário de RNG completo, nota de snapshot"
```

---

### Task 4: `loadState` — whitelist de fases

**Files:**
- Modify: `src/state.ts` (`loadState`, linha ~805-836)
- Test: `tests/state.test.ts`

**Interfaces:**
- Consumes: `Phase` (union já definida, linha 27-31).
- Produces: `loadState` rejeita (retorna `null`) qualquer save cuja `phase`/`resumePhase` não seja um valor conhecido do union `Phase`.

- [ ] **Step 1: Failing test**

```ts
// em tests/state.test.ts, dentro de describe('gameReducer', ...)
test('loadState rejeita phase fora do union Phase (whitelist)', () => {
  const s = playToBuild()
  localStorage.setItem('thegoat:v5', JSON.stringify({ ...s, phase: 'totallyFakePhase' }))
  expect(loadState()).toBeNull()
})
test('loadState rejeita resumePhase fora do union Phase', () => {
  const s = playToBuild()
  localStorage.setItem('thegoat:v5', JSON.stringify({ ...s, phase: 'home', resumePhase: 'nonsense' }))
  expect(loadState()).toBeNull()
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
rtk npx vitest run tests/state.test.ts -t "whitelist"
```
Esperado: FAIL — hoje só checa `typeof parsed.phase === 'string'`, qualquer string passa.

- [ ] **Step 3: Whitelist**

Em `src/state.ts`, no topo do módulo (perto de `const STORAGE_KEY = 'thegoat:v5'`, linha 140), adicionar:

```ts
const VALID_PHASES = new Set<Phase>([
  'home', 'attrDraft', 'draftDone', 'nbaDraft', 'preseason',
  'seasonAdvance', 'seasonResult', 'tradeDecision', 'eventDecision',
  'keyGame', 'playoffGame', 'gameResult',
  'freeAgency', 'retireDecision', 'verdict',
])
```

Em `loadState`, trocar a linha de validação existente:

```ts
    if (!parsed || typeof parsed.phase !== 'string' || typeof parsed.seed !== 'number') return null
```

por:

```ts
    if (!parsed || typeof parsed.seed !== 'number' || !VALID_PHASES.has(parsed.phase)) return null
    if (parsed.resumePhase != null && !VALID_PHASES.has(parsed.resumePhase)) return null
```

- [ ] **Step 4: Rodar e ver passar**

```bash
rtk npx vitest run tests/state.test.ts -t "whitelist"
```
Esperado: PASS.

- [ ] **Step 5: Suite completa + typecheck + commit**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
rtk git add src/state.ts tests/state.test.ts
rtk git commit -m "fix(c3): loadState valida phase/resumePhase contra o union Phase"
```

---

### Task 5: `ErrorBoundary` — tela branca vira "recomeçar"

**Files:**
- Create: `src/ui/ErrorBoundary.tsx`
- Modify: `src/main.tsx`
- Test: sem vitest (componente de UI, convenção do projeto) — verificado manualmente (Step 4)

**Interfaces:**
- Consumes: `clearStorage`-like behavior — como `clearStorage` de `state.ts` não é exportado, o boundary limpa `localStorage.removeItem('thegoat:v5')` diretamente (mesma chave de `STORAGE_KEY`).
- Produces: `<ErrorBoundary>` — componente de classe React, `children: ReactNode`.

- [ ] **Step 1: Componente**

```tsx
// src/ui/ErrorBoundary.tsx
import { Component, type ReactNode } from 'react'

interface Props { children: ReactNode }
interface State { crashed: boolean }

export class ErrorBoundary extends Component<Props, State> {
  state: State = { crashed: false }

  static getDerivedStateFromError(): State {
    return { crashed: true }
  }

  componentDidCatch(error: unknown) {
    console.error('ErrorBoundary caught:', error)
    try {
      localStorage.removeItem('thegoat:v5')
    } catch {
      // storage unavailable — nothing to clear
    }
  }

  render() {
    if (!this.state.crashed) return this.props.children
    return (
      <div className="screen" style={{ justifyContent: 'center', alignItems: 'center', textAlign: 'center', gap: 16 }}>
        <div className="headline headline--red" style={{ fontSize: 30 }}>ALGO QUEBROU</div>
        <div className="hint">O save foi limpo. Recomece uma carreira nova.</div>
        <button type="button" className="btn btn--ink" style={{ width: '100%' }} onClick={() => window.location.reload()}>
          Recarregar
        </button>
      </div>
    )
  }
}
```

- [ ] **Step 2: Envolver `<App/>` em `main.tsx`**

```tsx
// src/main.tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './styles/tokens.css'
import './styles/base.css'
import App from './App.tsx'
import { ErrorBoundary } from './ui/ErrorBoundary.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <App />
    </ErrorBoundary>
  </StrictMode>,
)
```

- [ ] **Step 3: Typecheck + build**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
rtk npm run build
```

- [ ] **Step 4: Verificação manual — força um crash**

```bash
rtk npm run dev
```
No console do navegador, com o app aberto: `localStorage.setItem('thegoat:v5', JSON.stringify({phase:'seasonResult'}))` e recarregar a página com um estado que force acesso a campo `undefined` (ex.: uma tela que leia `state.seasonOutcome!.standings` sem esse dado) — confirmar que aparece a tela "ALGO QUEBROU" em vez de branco, e que `localStorage.getItem('thegoat:v5')` volta `null` depois. Reverter o `localStorage` de teste ao final.

- [ ] **Step 5: Commit**

```bash
rtk git add src/ui/ErrorBoundary.tsx src/main.tsx
rtk git commit -m "feat(c3): ErrorBoundary limpa o save e evita tela branca em crash de render"
```

---

### Task 6: Standings com tiebreak determinístico (extrai função pura + testa)

**Files:**
- Modify: `src/ui/components/LeaguePanels.tsx` (`StandingsTable` linha 27-47, `StandingsTop4` linha 113-143)
- Test: `tests/engine/league-standings-sort.test.ts` (novo)

**Interfaces:**
- Produces: `sortStandings(entries: TeamStanding[]): TeamStanding[]` — exportada de `LeaguePanels.tsx`, ordena por `wins` desc, empate por `teamId` asc (determinístico).

- [ ] **Step 1: Failing test**

```ts
// tests/engine/league-standings-sort.test.ts
import { describe, expect, test } from 'vitest'
import { sortStandings } from '../../src/ui/components/LeaguePanels'
import type { TeamStanding } from '../../src/engine/types'

describe('sortStandings', () => {
  test('ordena por wins desc; empate por teamId asc, determinístico', () => {
    const input: TeamStanding[] = [
      { teamId: 'lal', conf: 'west', wins: 50, seed: null },
      { teamId: 'bos', conf: 'east', wins: 55, seed: null },
      { teamId: 'den', conf: 'west', wins: 50, seed: null },
    ]
    const sorted = sortStandings(input)
    expect(sorted.map(s => s.teamId)).toEqual(['bos', 'den', 'lal'])
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
rtk npx vitest run tests/engine/league-standings-sort.test.ts
```
Esperado: FAIL — `sortStandings` não existe.

- [ ] **Step 3: Extrair e exportar a função; usar nos dois componentes**

Em `src/ui/components/LeaguePanels.tsx`, adicionar (antes de `StandingsTable`, linha 27):

```ts
export function sortStandings(entries: TeamStanding[]): TeamStanding[] {
  return [...entries].sort((a, b) => b.wins - a.wins || a.teamId.localeCompare(b.teamId))
}
```

Em `StandingsTable` (linha 29-30), trocar:

```ts
  const byConf = (conf: 'east' | 'west') =>
    standings.filter(s => s.conf === conf).sort((a, b) => b.wins - a.wins)
```

por:

```ts
  const byConf = (conf: 'east' | 'west') => sortStandings(standings.filter(s => s.conf === conf))
```

Em `StandingsTop4` (linha 115), trocar:

```ts
  const byConf = (conf: 'east' | 'west') => standings.filter(s => s.conf === conf).sort((a, b) => b.wins - a.wins)
```

por:

```ts
  const byConf = (conf: 'east' | 'west') => sortStandings(standings.filter(s => s.conf === conf))
```

- [ ] **Step 4: Rodar e ver passar**

```bash
rtk npx vitest run tests/engine/league-standings-sort.test.ts
```
Esperado: PASS.

- [ ] **Step 5: Suite completa + typecheck + commit**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
rtk git add src/ui/components/LeaguePanels.tsx tests/engine/league-standings-sort.test.ts
rtk git commit -m "fix(c3): tiebreak determinístico em StandingsTable/StandingsTop4"
```

---

### Task 7: `GameResult` mostra todos os momentos icônicos

**Files:**
- Modify: `src/ui/screens/GameResult.tsx` (linha 58-64)
- Test: sem vitest (UI pura) — verificado por typecheck + e2e (Task 13)

**Interfaces:**
- Consumes: `result.iconics: IconicMomentId[]` (já existe, sem mudança de tipo).

- [ ] **Step 1: Trocar `iconics[0]` por `.map`**

Trocar:

```tsx
        {result.iconics.length > 0 && (
          <div className="strip strip--red" style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            <div className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'result.iconic')}</div>
            <div className="headline" style={{ fontSize: 19 }}>{t(lang, 'iconic.' + result.iconics[0])}</div>
            <div className="mono" style={{ fontSize: 10, color: 'var(--on-red-dim)' }}>{t(lang, 'result.iconicPts')}</div>
          </div>
        )}
```

por:

```tsx
        {result.iconics.length > 0 && (
          <div className="strip strip--red" style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            <div className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'result.iconic')}</div>
            {result.iconics.map((id, i) => (
              <div key={i} className="headline" style={{ fontSize: 19 }}>{t(lang, 'iconic.' + id)}</div>
            ))}
            <div className="mono" style={{ fontSize: 10, color: 'var(--on-red-dim)' }}>{t(lang, 'result.iconicPts')}</div>
          </div>
        )}
```

- [ ] **Step 2: Typecheck**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
```

- [ ] **Step 3: Commit**

```bash
rtk git add src/ui/screens/GameResult.tsx
rtk git commit -m "fix(c3): GameResult renderiza todos os momentos icônicos, não só o primeiro"
```

---

### Task 8: `engine/league.ts` — `projectedNpcLines` + `projectedStandings` (determinísticos)

**Files:**
- Modify: `src/engine/league.ts` (após `simNpcLines`, ~linha 114)
- Test: `tests/engine/league-projected.test.ts` (novo)

**Interfaces:**
- Consumes: `LeagueState`, `TEAMS` (já importado no arquivo), `npcEffOvr`, `clamp` (já definidos no arquivo).
- Produces: `projectedNpcLines(league: LeagueState): NpcLine[]`, `projectedStandings(league: LeagueState): TeamStanding[]` — ambas puras, zero `rng`.

- [ ] **Step 1: Failing test**

```ts
// tests/engine/league-projected.test.ts
import { describe, expect, test } from 'vitest'
import { projectedNpcLines, projectedStandings } from '../../src/engine/league'
import { initLeague } from '../../src/data/league'

describe('projectedNpcLines/projectedStandings — determinísticas, sem rng', () => {
  test('mesma entrada produz sempre a mesma saída (sem jitter)', () => {
    const league = initLeague()
    const a = projectedNpcLines(league)
    const b = projectedNpcLines(league)
    expect(a).toEqual(b)
    expect(a).toHaveLength(league.players.length)
    for (const l of a) {
      expect(l.ppg).toBeGreaterThanOrEqual(2)
      expect(l.ppg).toBeLessThanOrEqual(36)
    }
  })
  test('projectedStandings cobre os 30 times, wins dentro de faixa plausível', () => {
    const league = initLeague()
    const standings = projectedStandings(league)
    expect(standings).toHaveLength(30)
    for (const s of standings) {
      expect(s.wins).toBeGreaterThanOrEqual(12)
      expect(s.wins).toBeLessThanOrEqual(68)
    }
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
rtk npx vitest run tests/engine/league-projected.test.ts
```
Esperado: FAIL — funções não existem.

- [ ] **Step 3: Implementar em `src/engine/league.ts`**

Após `simNpcLines` (linha 114), adicionar:

```ts
// Projeção determinística (sem rng) de NpcLine — mesma fórmula de simNpcLines sem o
// termo de jitter aleatório. Usada pelo SeasonAdvance (C3) pra alimentar partialMvpRace
// com uma linha "esperada" por NPC, sem consumir rng (zero impacto em replay/calibração).
export function projectedNpcLines(league: LeagueState): NpcLine[] {
  return league.players.map(p => {
    const eff = npcEffOvr(p)
    const has = (tag: LeagueTag) => p.tags.includes(tag)
    const ppg = clamp((eff - 58) * 0.70 + (has('shooter') ? 2 : 0), 2, 36)
    const rpg = clamp((eff - 60) * 0.28 + (has('rebounder') ? 3 : 0) + (p.pos === 'C' ? 2 : p.pos === 'PF' ? 1 : -1), 0.5, 16)
    const apg = clamp((eff - 60) * 0.22 + (has('playmaker') ? 3 : 0) + (p.pos === 'PG' ? 2.5 : 0), 0.3, 12)
    return { playerId: p.id, ppg: Math.round(ppg * 10) / 10, rpg: Math.round(rpg * 10) / 10, apg: Math.round(apg * 10) / 10 }
  })
}

// Projeção determinística (sem rng) de standings — mesma fórmula de simStandings sem o
// termo de jitter (rng.int(-6,6)). Usada só como proxy de força de time pro winPct que
// entra em partialMvpRace; nunca substitui simStandings real (que segue com rng).
export function projectedStandings(league: LeagueState): TeamStanding[] {
  const strengths = new Map(TEAMS.map(t => [t.id, rosterStrength(league, t.id)]))
  const mean = [...strengths.values()].reduce((n, v) => n + v, 0) / 30
  return TEAMS.map(t => ({
    teamId: t.id, conf: t.conf, seed: null,
    wins: clamp(Math.round(41 + (strengths.get(t.id)! - mean) * 1.3), 12, 68),
  }))
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
rtk npx vitest run tests/engine/league-projected.test.ts
```
Esperado: PASS.

- [ ] **Step 5: Suite completa + typecheck + commit**

```bash
rtk npm test
rtk npx tsc -p tsconfig.app.json --noEmit
rtk git add src/engine/league.ts tests/engine/league-projected.test.ts
rtk git commit -m "feat(c3): projectedNpcLines/projectedStandings — projeção determinística sem rng"
```

---

### Task 9: `SeasonAdvance` — corrida de MVP projetada substitui a linha de stats

**Files:**
- Modify: `src/ui/screens/SeasonAdvance.tsx` (bloco final, linha 61-67)
- Modify: `src/data/i18n/pt.json`, `en.json`
- Test: sem vitest (UI pura, a lógica determinística já foi testada na Task 8) — verificado por typecheck + e2e (Task 13)

**Interfaces:**
- Consumes: `projectedNpcLines`, `projectedStandings` de `../../engine/league` (Task 8); `partialMvpRace` de `../../engine/league` (já existe, nunca usado até aqui); `RaceBars` de `../components/LeaguePanels` (já existe).

- [ ] **Step 1: i18n**

```json
"advance.raceLabel": "CORRIDA DE MVP (PROJETADA)"
```
(remove `advance.note`, que sai de uso — conferir com `rtk grep '"advance.note"' src/` antes de apagar que não sobra nenhuma outra referência.)

en:
```json
"advance.raceLabel": "MVP RACE (PROJECTED)"
```
(remove `advance.note` também no `en.json`.)

- [ ] **Step 2: Import e cálculo em `SeasonAdvanceBody`**

No topo de `src/ui/screens/SeasonAdvance.tsx`, trocar o import:

```ts
import { projectedSeed } from '../../engine/schedule'
```

por:

```ts
import { projectedSeed } from '../../engine/schedule'
import { partialMvpRace, projectedNpcLines, projectedStandings } from '../../engine/league'
import { RaceBars } from '../components/LeaguePanels'
```

Dentro de `SeasonAdvanceBody`, trocar o bloco final (linha 61-67):

```tsx
      {/* nota de escopo: corrida de prêmios de fato só existe em fim de temporada (simAwards);
          aqui exibimos a linha de stats do jogador. ponytail: simplificação com teto conhecido —
          upgrade = corrida parcial recalculada por trecho (novo contrato de rng), avaliar no C3. */}
      <div style={{ borderLeft: '2px solid var(--red)', paddingLeft: 12, display: 'flex', flexDirection: 'column', gap: 4 }}>
        <span className="mono" style={{ fontSize: 11 }}>{t(lang, 'advance.note', { ppg: reg.ppg, rpg: reg.rpg, apg: reg.apg })}</span>
        <span className="mono-label">{t(lang, 'advance.stops')}</span>
      </div>
```

por:

```tsx
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <div className="mono-label">{t(lang, 'advance.raceLabel')}</div>
        <RaceBars races={[mvpRace]} lang={lang} />
      </div>

      <div style={{ borderLeft: '2px solid var(--red)', paddingLeft: 12 }}>
        <span className="mono-label">{t(lang, 'advance.stops')}</span>
      </div>
```

E, no corpo da função (antes do `return`, junto às outras variáveis derivadas como `reg`), adicionar o cálculo da corrida:

```ts
  const mvpRace = partialMvpRace(
    state.league!, projectedNpcLines(state.league!), projectedStandings(state.league!),
    { ppg: reg.ppg, rpg: reg.rpg, apg: reg.apg, teamWinPct: cal.ticker.length ? wins / cal.ticker.length : 0.5 },
  )
```

(`wins`, `cal` e `reg` já existem no escopo da função, definidos nas linhas 14/12/19 respectivamente.)

- [ ] **Step 3: Typecheck**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
```

- [ ] **Step 4: Paridade de i18n**

```bash
rtk npx vitest run tests/i18n.test.ts
```
Esperado: PASS (chave nova presente nos dois idiomas, chave removida dos dois).

- [ ] **Step 5: Commit**

```bash
rtk git add src/ui/screens/SeasonAdvance.tsx src/data/i18n/pt.json src/data/i18n/en.json
rtk git commit -m "feat(c3): SeasonAdvance mostra corrida de MVP projetada (resgata partialMvpRace)"
```

---

### Task 10: Cerimônia (3b) — sub-view local em `SeasonResult` + remove `CeremonyPanel`

**Files:**
- Modify: `src/ui/screens/SeasonResult.tsx` (componente inteiro)
- Modify: `src/ui/components/LeaguePanels.tsx` (remove `CeremonyPanel`, linha 78-109)
- Modify: `src/data/i18n/pt.json`, `en.json`
- Test: sem vitest (UI pura) — verificado por typecheck + e2e (Task 13)

**Interfaces:**
- Consumes: `outcome.winners`, `outcome.lines` (`NpcLine[]`, já usado em outros lugares), `championTeam`, `season.playoffRun` — todos já disponíveis em `SeasonResult.tsx` hoje.
- Produces: `SeasonResult` ganha `useState<'ceremony' | 'balance'>('ceremony')` local — não persiste, não é `Phase`, não é dado do reducer.

- [ ] **Step 1: i18n — linha de médias do MVP + CTA**

```json
"ceremony.mvpLabel": "MVP DA TEMPORADA",
"ceremony.line": "{ppg} PTS · {rpg} REB · {apg} AST POR JOGO",
"ceremony.viewBalance": "Ver o balanço"
```

en:
```json
"ceremony.mvpLabel": "SEASON MVP",
"ceremony.line": "{ppg} PTS · {rpg} REB · {apg} AST PER GAME",
"ceremony.viewBalance": "View the recap"
```

- [ ] **Step 2: `useState` + view de cerimônia em `SeasonResult.tsx`**

No topo do arquivo, trocar o import:

```ts
import type { Dispatch } from 'react'
```

por:

```ts
import { useState, type Dispatch } from 'react'
```

Dentro de `SeasonResult` (logo após `const championTeam = teamById(outcome.championTeamId)`, linha 25), adicionar:

```ts
  const [view, setView] = useState<'ceremony' | 'balance'>('ceremony')
  const mvpId = outcome.winners.mvp
  const mvpName = mvpId === 'you' ? t(lang, 'races.you') : mvpId ? state.league!.players.find(p => p.id === mvpId)?.name ?? '—' : '—'
  const mvpLine = mvpId === 'you'
    ? { ppg: season.ppg, rpg: season.rpg, apg: season.apg }
    : outcome.lines.find(l => l.playerId === mvpId) ?? { ppg: 0, rpg: 0, apg: 0 }
```

Logo no início do `return` (antes do `<div className="screen">` atual, que passa a ser só o corpo da view `balance`), estruturar:

```tsx
  if (view === 'ceremony') {
    return (
      <div className="screen">
        <CareerBar state={state} dispatch={dispatch} heavy />
        <div style={{ display: 'flex', flexDirection: 'column', gap: 20, alignItems: 'center', textAlign: 'center', flex: 1, justifyContent: 'center' }}>
          <div className="mono-label">{t(lang, 'ceremony.title')} · {year}</div>
          <hr className="rule--double" style={{ width: '100%' }} />
          <div className="mono-label mono-label--red">{t(lang, 'ceremony.mvpLabel')}</div>
          <div className="mono-ring" style={{ width: 64, height: 64, fontSize: 20 }}>{mvpName.slice(0, 2).toUpperCase()}</div>
          <div className="headline headline--red" style={{ fontSize: 34 }}>{mvpName}</div>
          <div className="mono">{t(lang, 'ceremony.line', { ppg: mvpLine.ppg, rpg: mvpLine.rpg, apg: mvpLine.apg })}</div>
          <hr className="rule--double" style={{ width: '100%' }} />

          <div className="mono-label">{t(lang, 'result.honors')}</div>
          <div className="result__honors" style={{ width: '100%' }}>
            <div className="result__honor-row">
              <span>{t(lang, 'result.champion')}</span>
              <span style={{ color: 'var(--red)', fontWeight: 700 }}>{championTeam.city} {championTeam.name}</span>
            </div>
            {RACE_AWARDS.map(award => (
              <div key={award} className="result__honor-row">
                <span className="hint">{t(lang, 'award.' + award)}</span>
                <span>{nameOf(award)}</span>
              </div>
            ))}
          </div>

          <div className="strip strip--red" style={{ width: '100%' }}>
            <span className="headline" style={{ fontSize: 17 }}>{t(lang, 'run.' + season.playoffRun)}</span>
          </div>
        </div>
        <button type="button" className="btn btn--ink" onClick={() => setView('balance')}>
          {t(lang, 'ceremony.viewBalance')}
        </button>
      </div>
    )
  }
```

O resto do componente (o `return` original, com `.result`, banner, colunas etc.) fica exatamente como está hoje — só passa a ser o `return` alcançado quando `view === 'balance'`.

- [ ] **Step 3: Remover `CeremonyPanel` de `LeaguePanels.tsx`**

Apagar a função inteira (linha 78-109, de `export function CeremonyPanel` até o `}` de fechamento) e a constante `RACE_AWARDS` local dessa função **se** ela não for usada por mais nada no arquivo (`StandingsTop4`/`RaceBars`/`TrajectoryBars` não a usam — conferir com `rtk grep -n "RACE_AWARDS" src/ui/components/LeaguePanels.tsx` antes de apagar; se sobrar só a linha 76, apagar junto).

Conferir que nada mais importa `CeremonyPanel`:
```bash
rtk grep -rn "CeremonyPanel" src/
```
Esperado: nenhum resultado após a remoção.

- [ ] **Step 4: Typecheck + paridade i18n**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
rtk npx vitest run tests/i18n.test.ts
```

- [ ] **Step 5: Commit**

```bash
rtk git add src/ui/screens/SeasonResult.tsx src/ui/components/LeaguePanels.tsx src/data/i18n/pt.json src/data/i18n/en.json
rtk git commit -m "feat(c3): cerimônia como sub-view própria (3b), remove CeremonyPanel órfão"
```

---

### Task 11: Aposentadoria retemada (3c)

**Files:**
- Modify: `src/ui/screens/Season.tsx` (`RetireDecision`, linha 257-289)
- Modify: `src/styles/base.css`
- Test: sem vitest (UI pura) — verificado por typecheck + e2e (Task 13)

**Interfaces:**
- Consumes: `effectiveOverall(overall, age, physical)` de `../../engine/season` (já importado no arquivo, usado por `performanceRatio`).

- [ ] **Step 1: CSS novo — papel de carta + barras de declínio**

Em `src/styles/base.css`, ao final do arquivo, adicionar:

```css
/* aposentadoria (3c) — papel de carta, única manchete não-Black do app */
.retire-screen {
  background: var(--paper-2);
}
.retire-letter {
  font-family: var(--sans);
  font-weight: 500;
  text-transform: none;
  letter-spacing: 0;
  line-height: 1.15;
}
.retire-decline {
  display: flex;
  align-items: flex-end;
  gap: 3px;
  height: 56px;
}
.retire-decline__bar {
  flex: 1;
  background: var(--track);
}
.retire-decline__bar--peak {
  background: var(--ink);
}
.retire-decline__bar--late {
  background: var(--red);
}
```

- [ ] **Step 2: Retheme do componente**

Trocar `RetireDecision` inteiro (linha 257-289) por:

```tsx
function RetireDecision({ state, dispatch }: Props) {
  const lang = state.lang
  const n = state.career.seasons.length + 1
  const ratio = performanceRatio(state.career.seasons)
  const heavyDecline = ratio !== null && ratio < 0.55
  const build = state.build!
  const eovs = state.career.seasons.slice(-10).map(s => effectiveOverall(build.overall, s.age, build.attributes.physical))
  const peakIdx = eovs.indexOf(Math.max(...eovs))
  const current = eovs.at(-1) ?? build.overall
  const peak = Math.max(...eovs)

  return (
    <div className="screen retire-screen">
      <CareerBar state={state} dispatch={dispatch} />
      <div
        style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', gap: 16, textAlign: 'center' }}
      >
        <div className="mono-label">
          {t(lang, 'retire.season', { n })} · {t(lang, 'season.age', { age: state.age })}
        </div>
        <div className="retire-letter" style={{ fontSize: 34 }}>{t(lang, 'retire.title')}</div>
        <div style={{ fontSize: 16, lineHeight: 1.6, color: 'var(--body-dim)' }}>
          {t(lang, heavyDecline ? 'retire.desc.pressure' : 'retire.desc', { age: state.age })}
        </div>

        <hr className="rule" style={{ width: '100%' }} />
        <div style={{ display: 'flex', justifyContent: 'space-between', width: '100%' }}>
          <span className="mono-label">{t(lang, 'retire.effOverall')}</span>
          <span className="mono">{peak} → {current}</span>
        </div>
        <div className="retire-decline" style={{ width: '100%' }}>
          {eovs.map((eov, i) => {
            const h = Math.max(4, Math.round(((eov - 40) / (99 - 40)) * 56))
            const cls = i === peakIdx ? 'retire-decline__bar retire-decline__bar--peak'
              : i >= eovs.length - 3 ? 'retire-decline__bar retire-decline__bar--late'
              : 'retire-decline__bar'
            return <div key={i} className={cls} style={{ height: h }} />
          })}
        </div>
        <hr className="rule" style={{ width: '100%' }} />
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
        <button type="button" className="btn btn--primary" onClick={() => dispatch({ type: 'RETIRE_DECISION', retire: false })}>
          {t(lang, 'retire.continue')}
        </button>
        <button type="button" className="btn btn--outline btn--outline-red" onClick={() => dispatch({ type: 'RETIRE_DECISION', retire: true })}>
          {t(lang, 'retire.stop')}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: i18n — rótulo novo**

```json
"retire.effOverall": "OVERALL EFETIVO"
```
en:
```json
"retire.effOverall": "EFFECTIVE OVERALL"
```

- [ ] **Step 4: Typecheck + paridade i18n**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
rtk npx vitest run tests/i18n.test.ts
```

- [ ] **Step 5: Commit**

```bash
rtk git add src/ui/screens/Season.tsx src/styles/base.css src/data/i18n/pt.json src/data/i18n/en.json
rtk git commit -m "feat(c3): aposentadoria retemada (3c) — papel de carta + declínio de overall"
```

---

### Task 12: Veredito retemado + revelação em camadas (3d, parte 2)

**Files:**
- Modify: `src/ui/screens/Verdict.tsx` (componente inteiro)
- Modify: `src/styles/base.css`
- Test: sem vitest (UI pura) — verificado por typecheck + e2e (Task 13)

**Interfaces:**
- Consumes: `state.verdict` (Task 1, não-nulo em `phase: 'verdict'`).

- [ ] **Step 1: CSS — fundo vermelho full-bleed, grid de totais, stagger**

Em `src/styles/base.css`, ao final:

```css
/* veredito (3d) — única tela integralmente vermelha */
.verdict-screen {
  background: var(--red);
  color: var(--on-red);
}
.verdict-screen .btn--primary {
  background: var(--paper);
  color: var(--red);
}
.verdict-screen .btn--outline {
  border-color: var(--on-red-dim);
  color: var(--on-red-dim);
}
.verdict-totals {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: var(--red-soft);
  width: 100%;
}
.verdict-total {
  background: var(--red);
  padding: 14px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.verdict-layer {
  opacity: 0;
  animation: verdict-fade-in 240ms ease-out forwards;
}
.verdict-layer--1 { animation-delay: 0ms; }
.verdict-layer--2 { animation-delay: 200ms; }
.verdict-layer--3 { animation-delay: 400ms; }
@keyframes verdict-fade-in {
  from { opacity: 0; transform: translateY(6px); }
  to { opacity: 1; transform: translateY(0); }
}
```

- [ ] **Step 2: Retheme + camadas**

Trocar o `return` de `Verdict` (linha 54-124) por (o resto do componente — imports, `useEffect` do canvas, `handleShare` — fica igual, só o JSX muda; `verdict` local passa a vir de `state.verdict!` em vez de `computeVerdict(state.career)`):

No topo da função, trocar:

```ts
  const verdict = computeVerdict(state.career)
```

por:

```ts
  const verdict = state.verdict!
```

(O import `computeVerdict` de `../../engine/verdict` deixa de ser usado neste arquivo — remover do import no topo.)

Trocar o `return` inteiro por:

```tsx
  return (
    <div className="screen verdict-screen">
      <CareerBar state={state} dispatch={dispatch} heavy />
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20, alignItems: 'center', textAlign: 'center' }}>
        <div className="verdict-layer verdict-layer--1" style={{ display: 'flex', flexDirection: 'column', gap: 12, alignItems: 'center', width: '100%' }}>
          <div className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.title')}</div>
          <div className="headline" style={{ fontSize: 64 }}>{t(lang, 'tier.' + tier)}</div>
          <div className="mono" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.score', { n: score })}</div>
          <hr className="rule--double" style={{ width: '100%' }} />
        </div>

        <div className="verdict-layer verdict-layer--2 verdict-totals">
          <div className="verdict-total">
            <span className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.points')}</span>
            <span className="headline" style={{ fontSize: 26 }}>{totals.points}</span>
          </div>
          <div className="verdict-total">
            <span className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.seasons2')}</span>
            <span className="headline" style={{ fontSize: 26 }}>{totals.seasons}</span>
          </div>
          <div className="verdict-total">
            <span className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.rings')}</span>
            <span className="headline" style={{ fontSize: 26 }}>{counts.ring}</span>
          </div>
          <div className="verdict-total">
            <span className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.mvps')}</span>
            <span className="headline" style={{ fontSize: 26 }}>{counts.mvp}</span>
          </div>
        </div>

        <div className="verdict-layer verdict-layer--2" style={{ display: 'flex', flexWrap: 'wrap', gap: 8, justifyContent: 'center' }}>
          {AWARDS.filter(a => counts[a] > 0).map(a => (
            <span key={a} className="chip" style={{ borderColor: 'var(--on-red-dim)', color: 'var(--on-red)' }}>
              {counts[a]}× {t(lang, 'award.' + a)}
            </span>
          ))}
        </div>

        <div className="verdict-layer verdict-layer--3" style={{ display: 'flex', flexDirection: 'column', gap: 10, width: '100%' }}>
          {iconicMoments.length > 0 && (
            <>
              <div className="mono-label" style={{ color: 'var(--on-red-dim)' }}>{t(lang, 'verdict.moments')} · +{verdict.iconicPoints}</div>
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8, justifyContent: 'center' }}>
                {iconicMoments.map((id, i) => (
                  <span key={i} className="chip" style={{ borderColor: 'var(--on-red-dim)', color: 'var(--on-red)' }}>{t(lang, 'iconic.' + id)}</span>
                ))}
              </div>
            </>
          )}
          {verdict.chokes > 0 && (
            <span className="chip" style={{ borderColor: 'var(--on-red-dim)', color: 'var(--on-red-dim)', opacity: 0.72 }}>
              {t(lang, 'verdict.chokes', { n: verdict.chokes })}
            </span>
          )}

          <canvas
            ref={canvasRef}
            style={{ width: '100%', maxWidth: 280, aspectRatio: '1080 / 1350', border: '1px solid var(--on-red-dim)', borderRadius: 8, margin: '0 auto' }}
          />

          <button type="button" className="btn btn--primary" style={{ width: '100%' }} onClick={handleShare}>
            {t(lang, copied ? 'share.copied' : 'share.button')}
          </button>
          <button type="button" className="btn btn--outline" style={{ width: '100%' }} onClick={() => dispatch({ type: 'RESET' })}>
            {t(lang, 'share.again')}
          </button>
        </div>
      </div>
    </div>
  )
```

- [ ] **Step 3: Typecheck**

```bash
rtk npx tsc -p tsconfig.app.json --noEmit
```

- [ ] **Step 4: Commit**

```bash
rtk git add src/ui/screens/Verdict.tsx src/styles/base.css
rtk git commit -m "feat(c3): veredito retemado (fundo vermelho, grid de totais, botões onred) e revelação em camadas (3d, parte 2)"
```

---

### Task 13: e2e playthrough atualizado + rodagem completa

**Files:**
- Modify: `tests/e2e-playthrough.mjs` (branch da cerimônia antes do balanço)

**Interfaces:**
- Consumes: nada novo — só ajusta a navegação do script pro toggle `ceremony → balance` da Task 10.

- [ ] **Step 1: Adicionar o branch de cerimônia antes da detecção de `seasonResult`**

Em `tests/e2e-playthrough.mjs`, imediatamente antes do bloco (linha 271):

```js
      if (await page.locator('text=A TEMPORADA EM CINCO LINHAS').count() > 0) {
```

adicionar:

```js
      if (await page.locator('text=Ver o balanço').count() > 0) {
        log('seasonResult: cerimônia → balanço')
        await page.locator('button.btn--ink', { hasText: 'Ver o balanço' }).click()
        continue
      }

```

- [ ] **Step 2: Rodar o playthrough completo**

```bash
rtk node tests/e2e-playthrough.mjs
```
Esperado: `exitCode` 0, screenshots gerados em `SHOTS_DIR` incluindo `04-season-result.png` (já pós-cerimônia) e `05-verdict.png` (com o novo fundo vermelho e botões retemados). Conferir visualmente os PNGs: veredito com fundo vermelho, botão "Compartilhar" em bloco creme, aposentadoria (se o playthrough passar por ela) com papel `--paper-2`.

- [ ] **Step 3: Commit**

```bash
rtk git add tests/e2e-playthrough.mjs
rtk git commit -m "test(c3): e2e playthrough atualizado pro toggle cerimônia→balanço"
```

---

### Task 14: HANDOFF.md — fecha o ciclo

**Files:**
- Modify: `HANDOFF.md`

**Interfaces:** nenhuma — só documentação.

- [ ] **Step 1: Atualizar o estado no topo**

Atualizar a linha 3 (**Estado:**) pra incluir "redesign C3 entregue" e ajustar a contagem de commits à frente de `origin/master` se relevante.

- [ ] **Step 2: Remover as duas linhas de débito já obsoletas**

Remover de "Débitos deferidos":
- `Botão compartilhar é no-op silencioso sem clipboard/Web Share (...)`
- `(C2) Game.tsx: botão "simular" duplicado no desktop (...)`

- [ ] **Step 3: Remover os débitos fechados neste ciclo**

Remover (ou mover pra uma nova linha "Fechados no C3: ..." se preferir manter histórico curto):
- `Validação rasa do loadState (...)`
- `Reducer sem guards de fase (...)`
- `Verdict.tsx: computeVerdict no corpo do render (...)`
- `CeremonyPanel em LeaguePanels.tsx ficou órfão (...)`
- `StandingsTop4/StandingsTable ordenam por wins puro (...)`
- `Verdict.tsx: botão "jogar de novo" (RESET) usa .btn puro sem variante (...)`
- `(C2) SeasonAdvance.tsx (tela 6b) mostra uma linha de stats (...)`
- `(C2) GameResult.tsx só renderiza iconics[0] (...)`
- `(C2) pendingEvents/pendingChoices (...) pendingChoices não tem leitor (...)` — vira só `pendingEvents` (ainda em uso), remover a parte de `pendingChoices`.
- `(C2) Comentário-contrato de RNG do avanço de playoffs (...) não lista advancePlayer (...)`

Manter: `(C2) tests/state-calendar.test.ts (...) ancorado na seed 42` (agora só com o comentário explicativo adicionado no código, não removido — é uma característica permanente do teste, não um bug).

- [ ] **Step 4: Adicionar decisão-chave 14 (Redesign C3)**

Adicionar ao final da seção "Decisões-chave", seguindo o formato das entradas 12/13:

```markdown
14. **Redesign C3** (2026-07-30): ciclo final do redesign — cerimônia (3b) virou sub-view local (`useState`) dentro de `seasonResult`, sem fase nova; aposentadoria (3c) retemada com papel `--paper-2` e 10 barras de declínio de overall efetivo; veredito (3d) ganhou `GameState.verdict` computado uma vez nos dois pontos que entram em `phase: 'verdict'` (`RETIRE_DECISION` e aposentadoria por idade em `case 'ADVANCE'`), com backfill em `loadState` pra saves antigos (sem bump — mesmo padrão do `hubOpen` no C1), fundo vermelho full-bleed e revelação em camadas via CSS (`~200ms` de stagger). `SeasonAdvance` trocou a linha de stats por uma corrida de MVP projetada: duas funções determinísticas novas (`projectedNpcLines`/`projectedStandings`, sem rng) alimentam o `partialMvpRace` — achado morto no levantamento deste ciclo, definido e calibrado mas nunca chamado, agora resgatado. Fechados: `pendingChoices` (campo persistido nunca lido de volta — a variável local que ele guardava continua em uso normal em `simRegularSeason`), guardas de fase faltantes em `CONFIRM_BUILD`/`CHOOSE_OFFER` (o resto do reducer já tinha; não virou mapa central), whitelist de `phase`/`resumePhase` em `loadState` + `ErrorBoundary` novo em `main.tsx`, tiebreak determinístico de standings (`sortStandings`, por `teamId`), `GameResult` mostra todos os `iconics` (não só o primeiro). Dois débitos do HANDOFF já estavam obsoletos antes deste ciclo (corrigidos só na documentação): botão de compartilhar (já tinha clipboard/Web Share) e o botão "simular" duplicado do `Game.tsx` (já corrigido no commit `8e721a0`, fix wave final do C2). Fora de escopo: histórico local de runs (depende de leaderboard/Supabase, backlog v2).
```

- [ ] **Step 5: Commit**

```bash
rtk git add HANDOFF.md
rtk git commit -m "docs(c3): handoff atualizado — redesign C3 entregue, débitos fechados"
```

---

## Ordem de execução

Tasks 1-9 são independentes de UI (engine/state) e podem ser feitas em sequência sem risco de conflito; Tasks 10-12 tocam telas (dependem de Task 1 pro veredito); Task 13 depende de 10-12 estarem prontas; Task 14 é sempre a última.
